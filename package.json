{
  "name": "ashare-radar",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run check:py && npm run check:js && npm test",
    "check:js": "node --check static/app.js && node --check static/js/*.js",
    "check:py": "${PYTHON:-/opt/anaconda3/bin/python3} -m compileall -q app tests tools && ${PYTHON:-/opt/anaconda3/bin/python3} -m pyflakes app tests tools",
    "test": "${PYTHON:-/opt/anaconda3/bin/python3} -m pytest -q"
  }
}
