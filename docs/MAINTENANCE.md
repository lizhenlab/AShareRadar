# Maintenance and Refactor Guide

## 1. Engineering Rules

- Keep HTTP routes thin. Route files should validate inputs and call workflows/services.
- Resolve production domain services from `AppContainer.domain_services`. API routes and scheduler tasks must not use `datahub.cache.*_service`; keep those cache properties only as temporary compatibility entrypoints while consumers migrate.
- Keep provider logic behind `DataHub` and `provider_registry`.
- Keep SQLite access in repositories or `SQLiteCache`; do not write SQL in route handlers.
- Preserve the checked layer direction: `db`, `repositories`, `models`, and `utils` must not import API, workflows, or services. Production modules import domain models directly, not `app.models.schemas`; that facade exists only for compatibility. Run `tests/test_architecture_boundaries.py` after moving contracts or changing imports.
- Keep `app/config.py` as a stable facade. Put settings/default/environment ownership in `config_settings.py`, shell-profile parsing and permission checks in `config_shell.py`, and endpoint/security validation in `config_validation.py`. Do not add eager shell reads at module import or let adapters read environment variables directly.
- Use `app/utils/clock.py` for every current-time or duration source. Market/calendar decisions use `market_now()` and `Asia/Shanghai`; audit/persistence text uses `audit_now_text()` and UTC `Z`; TTLs/deadlines/throttles use `monotonic_now()` and measured latency uses `performance_now()`. Never subtract wall clocks to calculate a duration, and never introduce direct `datetime.now()` outside the adapter.
- Treat the audit-time migration as a one-way, offline compatibility boundary. New audit fields must be UTC-aware, added to `AUDIT_TIMESTAMP_COLUMNS` when legacy databases may contain Shanghai-naive text, and tested against aware ISO plus fractional legacy values. Existing databases with a pending migration must be opened with explicit `Settings`, under the runtime-leader migration lease, after a verified backup and with the documented disk-space reserve; invalid values must roll back without a success marker. Do not support rolling old/new writers across this boundary. Market-event fields must not be added to that migration merely because they contain a timestamp.
- Treat every schema upgrade as an offline ordered boundary. When seal columns are absent, drop stale market-scan triggers before base DDL; run compatibility work under one `BEGIN IMMEDIATE`, with the v2 snapshot backfill last; then apply advice/Paper schema; then apply strategy execution source migration. The snapshot migration must reset every pre-existing published graph to `legacy_backfill`, seal/verify it, install all five triggers, and write its marker last. Strategy migration must verify surviving source seals, preserve missing-source orphans as null/null/`legacy_unverified`, validate the exact two-state relation, restore its guards, and write its marker last. Marker-present startup must still verify safe state and guard presence. Never reorder the phases, manually edit a digest/origin/status, reseal history, or upgrade an orphan in place.
- Keep provider routes behind `run_api()`. Async handlers that call synchronous repositories, scheduler state, or diagnostics must use `run_sync_api_async()`; reserve `run_sync_api()` for synchronous boundaries. Client `RequestValidationError` stays `422`, while internal Pydantic `ValidationError` must be logged and mapped to generic sanitized `503` detail.
- Preserve `SameOriginMutationMiddleware` ahead of route side effects for `POST`/`PUT`/`PATCH`/`DELETE` and truthy `GET ...?refresh=...`. Browser-origin requests must separately validate the Host-derived request origin and supplied Origin/Referer against configured origins so a matching hostile Host cannot bypass the boundary through Host/DNS rebinding. Keep default-port normalization, metadata-only cross-site rejection, ordinary read-only access, and non-browser clients without origin metadata covered when changing middleware or CORS settings.
- Keep user input/update Pydantic models strict about unknown fields unless a compatibility requirement explicitly says otherwise.
- Keep every committed user-data import behind `exclusive_local_data_operation()`, a fresh single-use preview claim bound to path/mode/bundle/current user state, and a verified rollback backup created before mutation. Do not move claim consumption, backup, or import outside that serialized boundary.
- Keep browser file reads, import previews/commits, and cleanup previews under explicit latest-request ownership. A stale completion must not revoke or repaint a newer selection, token, feedback message, or action state. Only a successful current import may invalidate browser caches, reload runtime-owned panels, and rebuild the SSE subscription.
- Keep scheduled cleanup on `cleanup_regenerable_runtime_rows()`. Preserve the monotonic `ASHARE_RADAR_RUNTIME_MAINTENANCE_INTERVAL_SECONDS` throttle and window-function candidate selection. Ordinary runtime tables use set-based deletion; a sealed market-scan graph must use only the explicit verified graph-retention gateway. User-history retention belongs only to the explicit previewed cleanup flow, which must back up before deletion and preserve advice referenced by review plans.
- Keep current-day review and watchlist-scan requests free of a synthetic end-of-day `as_of`; let the server use current Shanghai time. Past dates use Shanghai-local end of day, future dates fail before market/storage I/O, and review actions remain bound to the symbol that owns the rendered panel during a pending stock switch. `evaluated_at` is server-owned and must never be accepted from a public client.
- Treat advice-review plan revisions and evaluation attempts as immutable ledgers. Never update or delete an old revision/result to implement current state. Current projection, active reads, paper strategy creation, portability and summary must all bind the same canonical plan digest. Update/archive/evaluate use revision CAS; archive is a tombstone, not a cascade. Legacy or corrupted results stay physically auditable but must project as metric-free insufficient evidence.
- Review and paper source windows use the trusted exchange calendar plus complete PIT/execution metadata. A suspended session may fill calendar coverage but cannot touch a barrier, fill a trade, mark an open position to a carried close, or silently clear a pending exit. Never repair missing sessions by shifting dates or synthesizing a trading bar.
- Keep trading-calendar decisions on validated runtime or bundled snapshots. Never restore a Monday-Friday positive fallback. A runtime snapshot at least as new as the bundle must govern their overlap, with bundle used beyond runtime bounds. Implicit automatic refresh must remain non-blocking/single-flight; explicit API refresh stays off the event loop. Runtime writes must stay atomic and must never rewrite `app/resources/trading_calendar.json`; bundle replacement is an explicit reviewed maintenance change.
- After an alert write succeeds, reconcile the affected rule locally before any readback. A failed readback must report synchronization degradation without restoring stale row content or claiming the write failed.
- Add or update tests before changing trend, alert, provider fallback, or data-quality behavior.
- Do not store API keys in project files.
- Keep the app runtime isolated from user-level Python packages; optional provider stacks such as pandas/numpy must resolve from the project runtime, not `~/.local`.
- Keep direct runtime libraries in `requirements.txt` and direct engineering tools in `requirements-dev.txt`. Rebuild both Python 3.12 hashed locks when the runtime input changes, and rebuild the development lock when only the development input changes. Reproducible installs must use `--require-hashes` against a lock file.
- Preserve the executable runtime declaration: Python 3.12.x, Node.js 22.x or 24.x, and npm 10.x or 11.x. `.python-version`, `.node-version`, `package.json`, locks, workflow setup, and `tools/runtime_contract.py` must move together. Node 22 is the default quality runtime; Node 24 has a separate compatibility smoke job.
- Keep GitHub Actions pinned to immutable full SHAs. The current `checkout` 6.0.2, `setup-python` 6.2.0, and `setup-node` 6.4.0 releases use the Node 24 action runtime; do not regress them to Node 20-based majors when editing `.github/workflows/ci.yml`.
- Preserve `persist-credentials: false`, least-privilege workflow permissions, checksum verification for downloaded scanners, fully redacted current-tree plus full-history secret scans, locked dependency audits, and two-run byte comparison for normalized CycloneDX SBOMs. A green audit is point-in-time evidence, not proof that dependencies are vulnerability-free forever.
- Keep the live provider canary optional and outside required pull-request CI. It must use temporary runtime state, separate bounded market-request and stock-pool waits under one overall deadline, bounded cleanup, direct non-cached quote and daily-K requests, explicit SH/SZ/BJ plus stock-pool contract validation, sanitized JSON output, and stable exit codes `0`/`2`/`1` for complete/partial/unavailable.
- Preserve liveness/readiness separation. Liveness may not resolve the application container or touch SQLite/network providers. Readiness must follow the lifecycle admission flag and a bounded read-only SQLite check; provider outages and standby runtime role must remain observable without making the process unready. Health responses stay `no-store` and failures expose no exception or local path.
- Reliability observations must stay low-cardinality UTC-hour aggregates. Do not add symbols, request IDs, exception text, or arbitrary URL labels. Preserve sample floors and `insufficient_data`, exclude cancelled work, exclude retry scans from duration percentiles, and update implementation, SLO documentation, retention, and tests together when changing a metric or target.
- Keep one Uvicorn worker as the supported runtime topology, and keep every documented supported start on `--timeout-graceful-shutdown 5`. That server bound limits graceful draining of SSE and in-flight data requests; it is distinct from provider-runtime shutdown and must not be presented as force-cancelling a running SDK thread. Preserve the real-process smoke test that holds a quote SSE connection open while sending `SIGINT`; its two-second test setting must prove bounded, traceback-free exit. Preserve the single `<SQLite path>.runtime-leader.lock` lease: scheduler and scanner must activate, stand by, take over, stop, and release as one unit. A bounded stop may defer release, but the lease must remain held until both services report quiescence; partial activation rollback must leave the scanner restartable, and coordinator-owned standby state must remain visible. The `.scheduler.lock` and `.market-scan.lock` paths are restore-time legacy guards, not independent runtime ownership. Keep cancellation-dominant task history; `cancelled` may replace a racing `success`, but late `success`/`failed` updates must not replace `cancelled`.
- Keep tests hermetic: use the active project runtime with `PYTHONNOUSERSITE=1`, temporary SQLite paths, fake providers/clients, and no required live network calls.
- When adding a user-facing recommendation, include reason, risk, invalidation condition, or missing-data note.
- When research logic consumes prices, volumes, forward returns, confidence, or factor scores from provider/cache/calibration inputs, guard non-finite values before text or statistics are assembled.
- Treat feature snapshots as a shared research boundary: clean non-finite or non-positive prices, amount, turnover, volume ratio, ATR/volatility, MA/support/resistance, and research score fields before leadership, Alpha, factor, or diagnosis text consumes them.
- Rule matches, minute-analysis plans, and market-regime evidence must turn invalid numbers, insufficient samples, and blank names into missing-data/fallback text instead of actionable or empty evidence. Minute rows must be timestamp-sorted/de-duplicated and restricted to the latest Shanghai-local market date; unavailable reports may retain filtered rows only as audit data and must clear executable ranges.
- Treat persistence/logging after a successful provider response as a local side effect. Cache writes, capability-status writes, runtime-event writes, and workbench local-state reads should degrade visibly or log best-effort instead of converting usable provider data into a loading failure. Market-sampling logs, quote-confirmation/profile persistence, and quote-stream watchlist fallback reads must use the SQLite offload helpers rather than running on the event-loop thread.
- Keep synchronous SQLite work off async event-loop threads through `run_sync_api_async()`, `run_cache_io()`, or scheduler offload helpers. Blocking provider SDK calls belong in the `ProviderRuntime`-owned bounded `DaemonThreadPoolExecutor`, and every DataHub lifecycle path must cancel shared workbench builds before reaching idempotent `DataHub.aclose()`. Shutdown cancels only work that has not started; never imply that Python can kill a running SDK thread. A timed-out close must reuse one tracked deferred task so provider clients close automatically only after runtime quiescence, while daemon workers ensure a permanently stuck call cannot hold interpreter exit open.
- Keep interactive workbench concept membership cache-only. The workflow must call the concrete `cached_stock_concepts_result` boundary directly and must not fall back to provider-capable `stock_concepts_result`/`stock_concepts` when that API is absent. Filter static-local rows in repository SQL with trimmed-source equality before ordering and `LIMIT`; then admit only fresh non-static provider-cache rows. Miss, stale, static-only, read-error, timeout, or cancellation stays optional/unavailable with zero provider call, runtime worker, cache/event write, and score contribution. Do not call this cache trusted or use it to authorize probability, actions, scans, or publication.
- Sanitize provider errors at both persistence directions: before request/status write output and again in status row mappers. Tests must cover URL userinfo, token/query credentials, quoted credentials in JSON/Python-style mappings, and historical dirty rows.
- Keep full-market business decisions on structured provenance fields, never display tags. Persist and validate `mode`, `quote_date`, and `data_date` independently: `data_date` is the completed daily-K cutoff, while `quote_date` is the required quote session. `official` requires both dates to match the latest completed session; `preopen` is admitted only on a Shanghai trading day in `[00:00, 09:15)`, requires both dates to match the previous completed session, and validates the completed quote close against that K-line close; `intraday` requires the current quote date plus the previous completed daily-K date and validates quote previous-close against that K-line close. `MarketScanRetryPlan` must be computed once and checked again inside transactional retry creation; scan and linked task terminal states must commit atomically. Mode belongs in the rule fingerprint, history and rank-comparison cohort, and a retry that needs market data must retain the source mode, `quote_date`, `data_date`, and fingerprint. Do not admit `preopen` to official probability-source capture, future-range research, or automatic scheduling.
- Keep the full-market scope byte-exact as `沪市 + 深市 + 北交所当前上市A股` wherever a consumer claims complete-market identity. `market-scan-snapshot-digest-v2` must cover every public persisted run field except its own digest and every public persisted result field including full public metrics/score details; adding a persisted public field requires adding it to the digest contract and tests in the same change. Preserve strict finite canonicalization, symbol ordering, publication/result audit-time ordering, and the five SQLite sealed-graph triggers. The verifier must feed the exact historical v2 bytes to SHA-256 from a symbol-ordered cursor one row at a time, with additional memory bounded by the largest canonical row; do not reintroduce result `fetchall()`. Streaming is not permission to bump the contract, create a migration, rewrite stored digests or reseal existing graphs.
- Treat `snapshot_seal_origin` as one input to authorization, not presentation metadata or sufficient authority. Digest-valid `legacy_backfill` may be browsed/audited through run/results/publication summary, breadth/screen evaluation, offline evaluation and backup verification. Discovery queue/apply/rank, delta, screen-alert persistence, strategy execution/simulation/executable Shadow/evidence/automation, probability outbox/capture/filter/managed publication, future range, degraded retry-copy and TOP100 refresh must call the complete shared action-source guard: original publication, exact full scope and internally consistent diagnostics-v1 score gate; two-run consumers guard both. Do not infer authority from status `success|degraded`, a valid digest, origin `publication`, `source_snapshot_verification_status=verified`, a visible pass label, or a newer run ID alone.
- Preserve PIT v4 absolute time ownership: quote event epoch `<= quote_observed_at <= run.as_of` decision; every bar's Shanghai-local date midnight `<= bar.as_of <= decision`; bar observation times nondecreasing. The bar `as_of` field is part of the 61-row evidence digest. V1/v2/v3 readers are audit compatibility only and may not authorize current context, capture, probability, future evidence or action.
- Keep `ScreenSpecV2` and screening/delta responses strict and canonical. One Python/SQLite condition compiler owns query, Discovery, evaluation, screen alert and Excel semantics. Breadth must conserve population/status/market/score/change/industry; evaluation must conserve spec/funnel/page/matches/near-misses/disjoint reasons; delta must read current plus immediate compatible previous shared-action-gate-eligible runs in one transaction and conserve timing/cohort/Top-N/exposure/reasons/counts. Never add an unverified parallel predicate or digest only a projection of the semantic response.
- Keep `/api/market-scans`, `/api/discovery`, and `/api/strategy-lab` non-cacheable on every success and failure path. Snapshot seal errors and broader artifact integrity errors use their two generic non-disclosing `409` categories; do not leak the failed invariant or describe `no-store` as integrity evidence.
- Keep authoritative stock-pool normalization in `app/utils/stock_pool.py`. Coverage checks and atomic repository replacement must receive the same normalized set, and replacement must verify its committed count under `BEGIN IMMEDIATE`.
- Serve static assets with revalidation and keep one scan-module import-map version. Scan recovery must retain one timer, bounded backoff, `404`/repeated-failure fallback to latest, online recovery, new-run page reset, strict response validation, and synchronized ARIA state.
- Treat `ASHARE_RADAR_LLM_TIMEOUT_SECONDS` as one total budget shared by initial generation, validation, and the optional correction; never reset the outer budget for the correction or add SDK retries.
- Keep primary business functions short and stage-oriented; production Python functions in `app/` and `tools/` are test-guarded at 60 lines and 12 branch points, so split data preparation, rule decisions, persistence, and response assembly before crossing those limits.
- Keep cross-module private dependencies below all three ratchets: total count, accesses by source module, and accesses against target module. When a private helper becomes cross-domain API, publish a narrow public contract; do not move the same private dependency to another module to satisfy only the total count.
- Put browser-global `online`, `visibilitychange`, and `pagehide` ownership in disposable controllers with explicit dependencies. A controller that registers listeners must expose idempotent cleanup before it can be reused by a remountable surface.

- Current v6 writes only `full-market-score-v5`; historical
  `full-market-score-v4` is exact read/replay-only and must never be rewritten.
  Preserve the v5 formula exactly: continuous trend maps linearly to `[-4,+4]`,
  enters `base=clamp(leader-quality+adjustment,0,100)`, and `raw_score=base`;
  rounding is display-only. `score-layer-distribution-v4` remains the publication
  policy. Every start/retry/TOP100/result/publication must register or verify the
  full rule JSON and production rule/hash in immutable `market_scan_rule_contract`.
- Preserve `publication.canonical_replay.v1` and its
  `market-scan-publication-replay-v1:<sha256>` detail as the only current action
  receipt. It must replay the registered rule, fixed thresholds, mode, complete
  capture/observation/event/systemic-stale/coverage summary, distribution and
  exact passed audit. Never accept a caller-constructed replacement.
- Current official skip classification has exactly two justified outcomes:
  `official_session_gap` and `new_listing_insufficient_history`. Both require
  `market-scan-skip-evidence-v1` plus replayable `market-scan-skip-pit-v1`, with
  exact run/decision/rule/quote/bar/calendar binding. Every other unavailable
  symbol is `missing`; never broaden skip classification or synthesize a bar.
- Preserve the skip-PIT-only K-line snapshot compatibility rule: exact
  `YYYY-MM-DD` means `Asia/Shanghai` 00:00 for bar/decision parity. Do not apply
  that normalization to quote, run, audit, artifact or general timestamp
  parsing. Future date-only values, non-canonical spellings and any ordering
  violation must fail closed.
- Keep failed/unpublished run `#86` immutable as the regression witness. Its
  persisted 5,389/154/0 success/missing/skipped counts stay untouched. The
  read-only replay result—146 typed skips (38 new-listing + 108 session-gap),
  7,673 bound bars, and expected new-run conservation 5,389/8/146—is a test
  oracle only, never permission to edit results, publish, migrate or reseal it.
- Keep request reads fused through one request-local verified-read transaction:
  one complete seal hash, one canonical receipt replay from gathered score
  observations, one page read, and no second SQL score read. Closing, cross-thread
  use, transaction restart, DML, DDL, schema or `query_only` mutation must revoke
  the capability and fail closed.
- Current probability capture writes source artifact v3/snapshot v3 with coverage
  v2 and v5 score binding; readers preserve source v1/v2/v3 and score v4/v5
  without upgrading legacy evidence. Outbox state is authoritative. A succeeded
  archive is usable only when outbox `archive_digest`, source
  `run_binding.source_integrity_digest`, and any calibrated artifact binding are
  identical. Keep pending UI polling at 60 attempts including failed requests,
  and stop on terminal state or run change. Do not lower fixed-session, OOS,
  joint-estimand, long-sample or deployment gates.
- Preserve the source-v3 official-session time binding: quote/bar dates identify
  the source session, while `run.as_of` must resolve that exact latest completed
  session through `market_scan_temporal_contract(..., "official")`. Weekend and
  holiday capture is therefore valid for the last completed session; pre-15:15,
  stale-after-next-session, future/reversed and calendar-uncovered inputs fail
  closed. `captured_at` is availability time (`captured_at >= as_of`) and may be
  later. Never apply this current rule while reading frozen source v1/v2 or
  previous-v2 assessment identities, which keep their historical same-date rule.
- Keep probability UI terminal states explicit. Fresh unpublished
  failed/cancelled/interrupted runs must perform no results read and end
  non-busy with null values and filtering disabled; queued/running may remain
  busy. A results-read error must replace stale loading copy and recover on a
  later successful read. Render direct `ineligible_run_contract` generically—it
  may be a published intraday/TOP100/legacy-seal batch—and never poll it as
  capture pending.
- Frozen run `#82` is immutable and action-ineligible. Never repair/reseal it,
  edit its score diagnostics/results, fabricate an outbox row, or bind a later
  archive. Recovery means a new official after-close v6/v5 run; read-only
  reclassification tests are projections, not migrations.

### Engineering Quality Change Protocol

Use the smallest applicable focused checks while editing, then run the complete acceptance sequence before delivery:

```bash
$PYTHON tools/runtime_contract.py
$PYTHON -m ruff check app tests tools
$PYTHON -m mypy
$PYTHON -m pytest -q -p no:cacheprovider \
  tests/test_architecture_boundaries.py \
  tests/test_clock_modules.py \
  tests/test_exception_safety.py \
  tests/test_reliability_modules.py \
  tests/test_supply_chain.py \
  tests/test_typing_contract.py
```

When a change crosses one of these boundaries:

1. **Runtime declaration:** update all declaration files and both CI runtime paths; never weaken `runtime_contract.py` to accept the developer's current machine.
2. **Timestamp storage:** classify the field as market event, audit event, or elapsed duration before choosing a representation. Include an old-SQLite migration fixture when changing persisted audit semantics.
3. **Module movement:** move lower-layer contracts before their consumers and let architecture tests reject reverse imports or cycles. Compatibility facades may re-export downward; lower layers may not import the facade owner upward.
4. **Health/SLI:** state the user-visible good event, failure event, exclusions, window, sample floor, target, and retention effect. Do not infer `met` from too little evidence.
5. **Supply chain:** regenerate locks only from their inputs, run the relevant audit, produce SBOMs from locks, and keep workflow actions immutable. Rotating or removing a leaked secret is an incident task; deleting it only from the latest tree is insufficient because history is scanned.
6. **Provider canary:** use it only as live environment evidence after hermetic tests pass. Record the timestamp, provider configuration class, and exit status without checking the generated JSON or temporary database into source control.

### Trading Calendar Annual Baseline

The bundled AKShare baseline currently contains 8,797 dates from `1990-12-19` through `2026-12-31`, sourced from `akshare.tool_trade_date_hist_sina` and updated at `2026-06-11 15:01:53`. It is a code resource, not runtime or user data. Before operations need a date beyond that boundary:

1. Refresh `data/trading_calendar.json` from AKShare in a controlled environment and verify the announced exchange holiday schedule independently.
2. Regenerate `app/resources/trading_calendar.json` from only the canonical trade-date list plus `schema_version`, source, `updated_at`, minimum/maximum date, and exact count metadata. Keep dates sorted and unique.
3. Run `tests/test_trading_calendar_modules.py`, updating the asserted bundle boundary/count/timestamp in the same reviewed change. Do not make the test derive expected metadata from the bundle itself. Preserve the inclusive listing-session contract: the minimum catalog date is valid session 1, and only a start before that date is out of coverage.
4. Exercise a missing-runtime start, the exact left boundary, and a truly out-of-coverage date. The first must use `bundled_baseline`; the boundary must count inclusively; the last must report `out_of_coverage`, keep trading closed, and make concrete date derivation fail explicitly.

If this maintenance is missed, the application intentionally stops scheduling or inferring new trading dates after trusted coverage ends. Enabling automatic fetch can rebuild a writable runtime cache, but it does not remove the need to ship a future bundled baseline for fresh clones and offline recovery.

## 2. Refactor Roadmap

### Priority 1: Documentation and Inventory

- Keep README as project entry.
- Keep SRS, SDD, API reference, operations guide, test plan, and function inventory current.
- Regenerate inventory after any code movement; review the Python Function Health section for the current longest and branchiest functions. Use both generators' `--check` mode in CI and review so drift checks never rewrite the worktree.

### Priority 2: Split Large Services

Completed splits:

- `app/services/stock_insights.py` is now a thin compatibility facade and bundle assembler.
- Insight behavior lives in `stock_activity.py`, `stock_abnormal_events.py`, `stock_abnormal_context.py`, `stock_abnormal_rules.py`, `stock_abnormal_summary.py`, `stock_lhb.py`, `stock_event_sources.py`, `stock_event_summary.py`, `stock_overview.py`, `stock_strategy.py`, `financial_health_components.py`, `financial_health.py`, `valuation_anchors.py`, `valuation_components.py`, `valuation_analysis.py`, and `stock_rules.py`.
- `app/services/datahub.py` now delegates provider source/status wording to `datahub_status.py`, read-only status assembly plus explicit provider enabled/capability synchronization to `datahub_status_service.py`, and pure cache/source normalization helpers to `datahub_cache.py`, while owning the instance-scoped workbench context cache used by the API container.
- Quote HTTP/SSE endpoints live in `app/api/routes/quotes.py`; keep batch-size limits, canonical stream symbols, dirty-symbol skips, off-event-loop watchlist selection reads, pre-stream API-error mapping, refresh-interval lower bounds, JSON-safe SSE formatting, quote-error events, and disconnect behavior covered by `tests/test_quote_stream_modules.py`. Connection-disconnect or graceful-shutdown `asyncio.CancelledError` is normal SSE completion and must end quietly, while unrelated exceptions must retain their existing error behavior; the real-process boundary is covered by `tests/test_uvicorn_smoke.py`. Empty-query fallback must use active queue symbols, enable configured seeds only for a truly empty watchlist table, and return `422` for a non-empty all-excluded table; explicit symbol queries remain independent of queue exclusion.
- Provider call timeouts, timed call results, shared provider-attempt iteration, source-name fallback, best-effort capability success/failure recording, and short cooldowns live in `app/services/datahub_runtime.py`. The bounded daemon-worker implementation lives in `app/services/daemon_executor.py`; keep it separate from default SQLite offload and preserve positive worker-count validation, post-shutdown submit rejection, queued-future cancellation, and daemon thread creation. Every production call must supply a hashable key containing all arguments that affect the provider result: identical keys share one task, different keys enter the bounded two-slot provider/capability admission gate, and no work is submitted while queued. A timeout or cancellation marks unfinished work orphaned only after its last waiter leaves; new different keys fail over while a retry of the same key may rejoin. Keep `aclose()` bounded/idempotent and keep active-worker tracking until real completion even though daemon status allows process exit.
- Quote fetching, partial cache reuse, quote quality entry, and multi-source consistency checks live in `app/services/datahub_quotes.py`. Provider-priority loops must tolerate unregistered provider names by skipping them with readable diagnostics, single-symbol quote lookup must strictly validate the symbol before batch lookup, realtime provider rows must pass shared quote-validity filtering before they are returned or cached, partial provider hits should stay healthy while logging coverage gaps and asking backup providers only for missing symbols, cached quote rows must preserve `from_cache`/`fallback_used` semantics for downstream clients, and cache write failures after provider success must not trigger provider fallback or cooldown.
- Daily K-line and minute K-line fetching live in `app/services/datahub_klines.py`; keep fresh-cache request-limit coverage, provider-attempt fallback behavior, invalid-row filtering, parsed date/time sorting, latest-window selection, minute business-timestamp freshness checks, best-effort cache writes after provider success, provider-limit caps, malformed max-limit fallback, unregistered-provider skips without status noise, missing-capability failure recording, cancellation propagation, and empty-response downgrade tests in sync with any source priority changes. Daily cache reuse and K-line quality must share `latest_expected_daily_kline_date()`, including the 15:15 Shanghai publish threshold. Full-market intraday scans must use the previous completed trading date as `data_date`; they must never admit the unfinished current daily bar.
- Minute interval aliases and minute cache business-time freshness live in `app/services/datahub_cache.py`; update `tests/test_datahub_cache_modules.py`, `tests/test_cache_freshness_modules.py`, and `tests/test_trading_calendar_modules.py` together whenever supported aliases, canonical interval labels, or session freshness windows change. Quote and minute freshness share the session policy, including the 11:25-11:30 lunch snapshot and its 13:00-13:15 reopen grace.
- `app/services/trading_calendar.py` owns strict runtime/bundle validation, target-aware snapshot selection, bounded AKShare refresh, atomic runtime replacement, Shanghai clock normalization, and fail-closed coverage behavior. `app/resources/trading_calendar.json` is the read-only fresh-clone/offline baseline. Keep source labels (`runtime_cache`, `bundled_baseline`, `out_of_coverage`, `unavailable`), warning de-duplication, and annual coverage tests together.
- `app/services/datahub_metadata.py` is now a compatibility facade. Stock-pool/profile state and coverage live in `datahub_metadata_stock_pool.py`, provider attempts/health/error/cache-write helpers in `datahub_metadata_provider.py`, plate/concept coordination in `datahub_metadata_coordinator.py`, and pure cleaning/mapping in `datahub_metadata_mapping.py`. Keep `StockPoolResolution` states explicit, keep coverage and persistence on one normalized candidate set, and preserve authoritative-empty, stale-fallback, local-profile, provider-health, and best-effort cache-write semantics.
- `app/services/market_scan_manager.py` is the public scan facade; `market_scan_modes.py` owns the `official`/`intraday`/`preopen` temporal contract; `market_scan_execution.py`, `market_scan_stock_evaluation.py`, `market_scan_completion.py`, and `market_scan_lifecycle.py` isolate batch orchestration, per-stock provider/scoring work, terminal persistence, and process lifecycle; `market_scan_export.py` owns XLSX structure, formatting, and cell-text safety for verified snapshots. Keep mode-window decisions centralized rather than duplicating 09:15/09:30/15:15 comparisons, and keep export free of provider/scoring calls. Keep pure scoring/universe logic in their existing modules. Terminal persistence may retry only SQLite `BUSY`/`LOCKED`, at most three times with bounded backoff. If it still fails, retain only locally observed run IDs and allow recovery to `interrupted` only after the local task exits while this lifecycle still owns unified leadership; another instance must not infer ownership from a database `running` row.
- `app/repositories/market_scan.py` is the repository facade; `market_scan_lifecycle.py` plus `market_scan_lifecycle_support.py`, `market_scan_results.py`, `market_scan_queries.py`, `market_scan_automatic_state.py`, `market_scan_mapping.py`, and `market_scan_context.py` isolate state transitions/retry, terminal transaction helpers, writes/ranking, reads/pagination, the scheduler-only non-authorizing header/database-identity mapper, ordinary row mapping, and shared repository capabilities. Keep the automatic-state mapper free of full snapshot verification or action admission: its output is only a duplicate-decision hint consumed by the manager. Repository modules may consume temporal/production-score/replay semantics only through the stable public `app/market_scan_repository_contracts.py` facade; do not reintroduce direct orchestration-service imports. Task-run creation plus scan attachment, publication seal plus terminal state, and retry planning/creation must each preserve their atomic transaction boundaries.
- `app/services/runtime_backup.py` serializes every operation for the same database or backup directory with sorted thread locks plus cross-process file leases. Keep one 30-second acquisition deadline across all required paths, hold leases through verification/staging/rotation/rollback, protect in-use bundles, and preserve sanitized timeout/permission errors. Any future split must keep those lock scopes explicit rather than reacquiring inside nested public calls.
- `app/services/scheduler.py` is the scheduler compatibility facade. Contracts/guard adapters, lifecycle, execution/status, refresh handlers, schedule transitions, health policy, safe helpers, and the composed service live in `scheduler_contracts.py`, `scheduler_lifecycle.py`, `scheduler_execution.py`, `scheduler_tasks.py`, `scheduler_schedule.py`, `scheduler_health.py`, `scheduler_helpers.py`, and `scheduler_service.py`. Preserve public imports and keep every module below the guarded size ceiling.
- Optional Futu order-book retrieval and ping checks live in `app/services/datahub_orderbook.py`; provider failures must be wrapped as data-source errors after recording order-book capability status.
- Source-plan assembly, primary-source selection, provider decision rules, warning priority, and recovery suggestions live in `app/services/datahub_source_plan.py`; provider source-key and capability-state wording rules live in `app/services/datahub_status.py`; read-only DataHub status assembly and explicit provider enabled/capability synchronization live in `app/services/datahub_status_service.py`. Keep provider/source/kind/error text cleaning, duplicate status ranking, unique non-demo provider counts, missing-primary warnings, finite success-rate counts, provider/capability sync order, legacy provider capability fallback, no-write status reads, and warning/suggestion de-duplication covered when changing source-plan or status wording.
- Provider/capability persistence and aggregation live in `app/repositories/provider_status.py` and `app/repositories/provider_status_aggregation.py`; keep explicit select columns, stable provider/capability ordering, enabled-state preservation, invalid count normalization, pre-write error sanitation/length caps, and mapper-level read sanitation covered by `tests/test_provider_status_aggregation_modules.py` and `tests/test_provider_status_repository_modules.py`.
- Data-quality score components live in `app/services/data_quality_components.py` and K-line assessment lives in `app/services/data_quality_kline.py`. Keep quote-field sanitation ahead of derived range/change-percent diagnostics, keep high/low/change_pct boundaries strict, keep quote cache/fallback flags authoritative over source wording while retaining legacy source parsing, keep K-line latest-date/source selection based on the newest parsable row instead of the input tail, keep short-cache/normal-cache/fallback-cache wording distinct, avoid duplicate insufficient-count notes when K-lines are completely missing, clamp consistency penalties so they cannot reverse into score bonuses, and add stable anomaly text when anomaly-level consistency results have no notes. Update `tests/test_data_quality_modules.py` with any rule or wording change.
- Optional provider adapters now live in source-specific modules: `akshare_provider.py`, `tushare_provider.py`, `baostock_provider.py`, `futu_provider.py`, and `local_metadata_provider.py`; keep API transport/return-code handling in providers and row parsing in mapper modules. Critical quote fields must use strict parsing instead of falling back to zero, missing change-percent values should be recomputed from price and previous close, and core volume/amount fields must not accept negative values. Eastmoney request symbols and HTTP JSON/object shapes must fail with readable data-source errors instead of leaking low-level type errors. Preserve the industry-plate path as one bounded Eastmoney HTTPS page: no redirect, exact `200`, no more than 100 complete advertised rows or 512 KiB, strict deadline/row/code/name/order validation, and no AKShare SDK import or pagination. Do not loosen that boundary to accommodate upstream paging; treat a breach as source failure and preserve the previous cache. K-line/minute rows must pass the shared validity helpers in `app/utils/market_data.py` before they enter analysis or cache persistence; the rule rejects non-finite values, non-positive bars, open/close values outside high/low bounds, negative volume, and non-finite or negative optional minute amount/turnover values. Futu order books must reject fully empty depth after cleaning. `provider_utils.pick()` should ignore only expected missing-field errors; unexpected row-adapter failures must surface. For AKShare K-lines, keep import/request failures eligible for Eastmoney fallback but let returned-row schema errors surface. For AKShare concept sources, update candidate field maps, code-column detection, and all-candidate loader-failure behavior together with `tests/test_data_sources.py`.
- Tencent quote parsing in `app/services/providers.py` has stricter quote-level invariants: payload extraction must tolerate trailing whitespace while rejecting unclosed payloads, required fields are stripped before minimum-count checks, stock code/name must be present, timestamps must parse as real datetimes, and open/current prices must stay within high/low. Demo provider quotes should preserve rounded containment invariants, and demo K-lines should keep returning the requested number of previous weekdays even for Monday/small-limit calls. Keep these covered in `tests/test_tencent_provider_modules.py` when changing Tencent parsing or demo generation.
- `app/services/optional_providers.py` remains as a backward-compatible re-export layer only.
- Factor lab logic now separates the public assembly facade (`research_factors.py`), current factor construction (`research_factor_current.py`), report aggregation (`research_factor_report.py`), current scoring (`research_factor_scoring.py`), dynamic weights (`research_factor_weights.py`), cross-report text/impact helpers (`research_factor_text.py`), immutable factor registration/proxy scores (`research_factor_specs.py`), and historical calibration (`research_factor_calibration.py`). Aggregate sample support is the minimum participating per-factor sample count, never the sum of overlapping factor histories; the calibration minimum is derived from scan start plus forward horizon. When changing factor IDs/order, registration, scoring, window, sample, calibration, or text thresholds, update the focused factor tests together.
- Fund-flow output in `stock_activity.py` is a rule-table volume/price heat estimate, not formal main-inflow data; threshold, order-book boundary, shared depth cleaning, crossed-spread handling, invalid-depth, turnover/amount sanitation, zero-volume baseline, or current-volume date-policy changes should update the score/relation/pressure rules and `tests/test_stock_activity_modules.py` together.
- Finance and valuation logic now separates rule-table metric interpretation and finite-number guards (`financial_metrics.py`), financial-health metric components (`financial_health_components.py`), financial-health response assembly (`financial_health.py`), valuation anchors/percentiles with malformed-history filtering, latest-snapshot-per-day selection, bounded percentile inputs, and ordered anchor-label bands (`valuation_anchors.py`), valuation percentile delta rules and score components with finite wording and core/enrichment missing-data separation (`valuation_components.py`), valuation response assembly (`valuation_analysis.py`), and the compatibility facade (`stock_finance.py`).
- Analysis logic now separates assembly (`analysis.py`), signal/risk/action submodules (`analysis_signal_*.py` with `analysis_signals.py` kept as a facade), and strong-stock ranking (`strong_stocks.py`). `build_analysis()` should keep trend metrics, gated signal-point sets, optional history/peer input copying, and final result composition separated. Signal snapshots and legacy direct summary helpers must sanitize scores, labels, quality views, contribution text/impact, and user-facing notes before grouping or summary generation. Strong-stock ranking requires K-line evidence; workflow-level K-line failures should be logged and excluded from ranking rather than scored as neutral.
- Workflow logic now separates the public compatibility facade (`individual.py`), stock lookup (`stock_lookup.py`), optional-data timeouts (`optional_data.py`), base analysis and minute analysis (`stock_analysis.py`), full workbench research assembly (`workbench_pipeline.py`), and market overview sampling (`market_overview.py`). Symbol parsing must reject malformed codes such as all-zero values and conflicting market prefix/suffixes before provider calls; minute analysis must call the same stock-confirmation path as main analysis before fetching minute rows, so confirmed nonexistent symbols return 404 instead of a misleading source-unavailable report. Confirmed minute-source failures should degrade into the conservative unavailable report even when fallback event logging cannot write to local storage. Stock lookup may use a matching quote as a temporary profile when the stock pool is unavailable or misses a code, but it must still reject mismatched quote code/market, require a displayable quote name, swallow only runtime source unavailability for quote-confirmation fallback, and surface unexpected quote parsing/caller errors. Main analysis treats plate rank, peer quotes, local quote-history enrichment, and advice-snapshot persistence as optional local context; failures or optional-timeout expiry there should log fallback events and return the quote/K-line-backed analysis instead of 503. `stock_workbench` should stay staged as non-blocking advice snapshot persistence, normalized-symbol local-state reads, and final response assembly; local-state helpers own the fixed read limits for chart marks, alert rules, alert events, and notes, while optional market breadth, order-book, and concept enrichment should time out into missing-data context instead of blocking the provider-backed workbench. Market samples must normalize and dedupe request symbols plus stock-pool metadata before provider calls, clean market/industry text, skip invalid literal industries, and log skipped invalid/duplicate values for diagnostics; explicit custom strong-stock lists with no valid symbol, too many unique symbols, or no requested quote after batch/single fallback must be rejected instead of returning a misleading empty ranking or overloading providers.
- Keep optional-data provenance explicit. `StockWorkbench.local_data_warnings` must identify advice snapshot, chart marks, alert rules/events, and notes separately without exposing raw exception text. `AnalysisResult.peer_sample` and `PeerComparisonReport.sample_status` must preserve unavailable/degraded/insufficient/not-applicable distinctions. `MarketBreadthSnapshot.warnings` must distinguish a valid empty sample from source failure; source degradation stays non-blocking but reduces positive risk credit and appears in regime evidence/suggestions.
- Keep quote-sampling terminology exact. `QuoteSampleResult.fallback_batch_count` counts batches that required single-symbol fallback, including incomplete batch responses; do not rename this to “batch failures.” Market/strong-stock API metadata must keep requested, successful, and missing counts consistent, preserve partial data, label default all-failure, and surface K-line ranking loss. Explicit custom-list all-quote-failure remains an API error.
- Minute-analysis logic keeps strict minute-row cleaning, finite non-negative volume/amount/turnover checks, support/resistance candidate filtering, T-plan zone validation, conservative no-price/no-range/insufficient-sample wording, confidence downgrades, and warning priority in `minute_analysis.py`; update `tests/test_minute_analysis_modules.py` when changing these boundaries or the data-quality/action-disabled/volume-pullback/narrow-space warning order.
- Market data persistence now separates shared `Asia/Shanghai` event-time parsing (`app/utils/market_time.py`), the compatibility composition (`market_data.py`), column-list driven quote snapshots/history (`market_quotes.py`), daily/minute K-lines (`market_klines.py`), cache statistics (`cache_stats.py`), and stock metadata/plates/concepts (`market_metadata.py`). Quote snapshot and daily-history upserts must preserve the epoch-based SQL predicate registered by `SQLiteConnectionFactory`: newer normalized market-event epoch wins; equal events prefer non-fallback, then optional-field completeness, then fetched-at recency. Keep offset/UTC ISO legacy rows comparable while storing new event timestamps as fixed `YYYY-MM-DD HH:MM:SS` Shanghai text. Quote and K-line repositories filter invalid rows both before writing and after reading so late/old or dirty local rows cannot overwrite/re-enter analysis; quote validation requires finite change fields, positive price/previous close/OHLC, OHLC containment, and non-negative volume/amount. Cache stats must keep daily and minute K-line counts/timestamps separate while preserving aggregate compatibility fields. Metadata repositories should keep explicit column lists, stable ordering, concept-name de-duplication, finite optional numeric-field cleaning, and all-invalid/empty metadata-write preservation of previous good rows in sync with metadata tests.
- User-data persistence keeps new input validation in Pydantic/API layers and legacy-row tolerance in mappers/repositories. Advice history must sanitize dirty display fields, only dedupe against finite trend/support/resistance values, and retain `BEGIN IMMEDIATE` around latest-row lookup plus update/insert so identical concurrent saves across cache instances are atomic. Alert rules keep unsupported/non-finite legacy rows readable but disabled, normalize trigger counts, bound cooldowns, sanitize event numbers, and compare the evaluated rule snapshot before state/event writes. Recoverable provider failures are isolated per rule; SQLite failures are not swallowed. Stock-note mappers keep dirty legacy rows displayable while finite positive price rules remain enforced for new writes.
- Research Q&A logic now separates the compatibility facade (`research_qa.py`), free-form topic answer strategies (`research_qa_answer.py`), fixed QA report generation (`research_qa_report.py`), topic routing (`research_qa_topics.py`), and shared helpers (`research_qa_utils.py`). Add new free-form topics by registering a complete `TopicAnswerStrategy` entry so evidence/actions/invalidations/conclusion/prefix stay together; topic lookup should normalize whitespace, strategy outputs must pass the shared clean/dedupe/limit fallback path, and numeric text or invalid literals such as `None/null/nan/inf` must use conservative display fallbacks. Fixed FAQ cards must also pass shared evidence sanitation, normalize report items before response assembly, re-check risk/reward target/stop side, validate support/resistance around current price, and use conservative T-plan/theme fallbacks so missing or invalid levels stay as `待确认`.
- Research market-regime logic keeps cleaned context metrics, market-label priority, named risk-adjustment components, factor-lab risk contributions, blank industry/factor filtering, breadth-summary fallback wording, and sanitized breadth-source warnings in rule helpers inside `research_regime.py`; update `tests/test_research_breadth_modules.py` and `tests/test_research_regime_modules.py` when changing breadth availability, partial-source risk credit, tailwind/headwind thresholds, evidence, or missing-price wording.
- Research chip logic keeps valid-K filtering, finite positive price/volume gates, feature-price fallback, bucket construction, and support/pressure band selection in `research_chip.py`; update `tests/test_research_chip_modules.py` whenever bucket boundaries, invalid-row filtering, current-price fallback, or band ordering changes.
- Research replay logic now separates candidate scanning, K-line/volume context construction, pattern detection, case-note templates, bounded completed-sample counts, and pattern-note wording in `research_replay.py`; update `tests/test_research_replay_modules.py` whenever replay window, volume-ratio, target-day validity, forward-return finite filtering, pending-outcome wording, note priority, or maturity-count rules change.
- Research validation logic keeps finite fallbacks for risk multiplier, confidence adjustment, base confidence, factor score, and stability in `research_validation.py`; price-dependent validations additionally require finite positive price/MA/support/resistance inputs. Timeframe alignment keeps requested 20/60/120-day identities and excludes incomplete windows. Update the focused validation/timeframe tests whenever these boundaries or status priorities change.
- Research Alpha logic keeps non-finite impact filtering, non-displayable evidence filtering, evidence strength sorting, non-positive limit guards, cleaned title/reason de-duplication before limits, bounded 0-100 confidence components and summary display, verdict-context finite fallbacks, cleaned data-quality notes, dirty-text/whitespace/`N/A` filtering, empty rule-match tolerance, and explicit missing factor/regime/timeframe/risk-reward or abnormal feature-field notes in `research_alpha.py`; update `tests/test_research_alpha_modules.py` whenever bucket priority, confidence composition, verdict context, text cleaning, or missing-data wording changes.
- Research risk/reward logic keeps staged report assembly, non-finite/non-positive input cleaning, capped upside targets, stale structural-stop filtering, target/stop side validation before distance math, finite reward/risk ratios, pending summary text for missing or wrong-side price/target/defense/ATR/volatility/risk multiplier, wrong-side level rejection around current price, integer scenario-probability normalization with a neutral floor, decision-state probability caps for cautious ratings/validation/timeframes, blank/non-finite action/status/timeframe defaults, and missing-level scenario probability downweighting in `research_risk_reward.py`; update `tests/test_research_risk_reward_modules.py` whenever target/stop inputs, ratio formatting, summary wording, level-validity rules, rating boundaries, or scenario probability rules change.
- Research feature and leadership logic keeps shared feature cleaning, leader-score input sanitation, concept evidence filtering, stable concept sorting by change/rank/input order, and missing `公司画像` notes in `research_features.py`; update `tests/test_research_leadership_modules.py` whenever feature-field cleaning, leadership score inputs, concept evidence filtering, or missing-data wording changes.
- Stock event logic now separates abnormal-event assembly (`stock_abnormal_events.py`), abnormal-event metrics/rules/summary (`stock_abnormal_context.py`, `stock_abnormal_rules.py`, `stock_abnormal_summary.py`), LHB candidate checks (`stock_lhb.py`), event source adapters plus external-checklist rules (`stock_event_sources.py`), event panel aggregation (`stock_event_summary.py`), and the compatibility re-export layer (`stock_events.py`). Keep context metrics zero-base-safe and keep current-session volume based on quote/K-line date alignment before adding new detectors.
- Pydantic models now live in domain modules: `app/models/market.py`, `app/models/analysis.py`, `app/models/research.py`, `app/models/user_data.py`, `app/models/reviews.py`, `app/models/local_data.py`, `app/models/system.py`, and `app/models/workbench.py`.
- `app/models/schemas.py` remains as a backward-compatible re-export layer for existing imports.

Completed test splits:

- `tests/test_rules_alerts.py`: rules, factor calibration, alert validation/cooldown, alert transition decisions, unbounded enabled-rule evaluation, compare-and-set state conflicts, event-ID cursor ordering, and chart marks.
- `tests/test_api_alert_routes.py`: alert route/repository contracts including Chinese type-error rendering, finite threshold/cooldown guards, unknown-field rejection, ID-only and legacy cursor validation, and local read failure mapping.
- `tests/test_api_notes_routes.py`: note route/repository contracts including content trimming, finite positive price guards, date normalization, blank-date clearing, stable same-day ordering, and local read failure mapping.
- `tests/test_api_security_modules.py`: same-origin mutation/refresh enforcement, side-effect prevention, independent Host-derived and Origin/Referer allowlist checks, Host/DNS-rebinding rejection, fetch-metadata rejection, default-port normalization, read-only access, and non-browser compatibility.
- `tests/test_data_sources.py`: provider fallback, capability health, optional adapters, strict quote parsing, Eastmoney request dedupe, missing change-percent fallback, non-negative core quote fields, AKShare K-line fallback/schema separation, AKShare quote/concept mapping, one-page/no-redirect/exact-200/size/deadline/total-row bounded industry-plate retrieval without SDK pagination, AKShare plate capability failure recording, AKShare minute OHLC filtering, source plans, unregistered quote-priority skips, and stock-pool semantics.
- `tests/test_provider_utils_modules.py`: provider row-field picking, NaN skipping, expected missing-field fallback, unexpected adapter-error propagation, positive-limit validation, and finite/positive/bounded OHLC validity rules.
- `tests/test_datahub_quotes_modules.py`: quote coordinator fallback-cache event-log failures, partial provider success staying healthy while backup fills missing symbols, concurrent different-symbol requests without false background-call rejection, blank single-symbol rejection, consistency-warning monitor write failures, and `use_cache=false` quality checks that must not read cached K-lines.
- `tests/test_datahub_runtime_modules.py`: keyed sharing/admission/orphan behavior, runtime-owned bounded daemon workers, queued-future cancellation, close idempotence, provider-client close ordering, and subprocess proof that a permanently blocked SDK worker cannot hold process exit open.
- `tests/test_datahub_klines_modules.py`: K-line/minute coordinator fallback, provider-success cache-write degradation, fresh-cache request-limit coverage, minute stale-business-time refresh, provider invalid-row rejection, cache read/write invalid-row filtering, parsed date/time latest-window ordering, daily/minute cache-stat separation, huge provider-limit caps, capability-failure recording, cancellation propagation, cache TTL, future fetched-at guards, and non-positive limits.
- `tests/test_datahub_metadata_modules.py`: metadata coordinator fallback, provider-success cache-write degradation, unregistered providers, explicit missing-capability errors when all selected metadata sources lack a requested method, explicit no-provider configuration errors, stock-pool/profile limits, local empty concept coverage, future timestamps, stable cache ordering, all-invalid metadata-write preservation, invalid metadata rows, concept de-duplication, and optional numeric-field cleaning.
- `tests/test_provider_status_aggregation_modules.py`: provider/capability status aggregation, disabled stale activity handling, invalid count normalization, tie-break ordering, and repository status ordering.
- `tests/test_provider_errors_modules.py`: quoted JSON/Python-style mapping credential redaction while preserving non-sensitive mapping values.
- `tests/test_provider_status_repository_modules.py`: provider error sanitation before general/capability writes and again while mapping historical dirty rows.
- `tests/test_config_modules.py`: dynamic settings reads, no hardcoded LLM endpoint/model defaults, fail-fast malformed environment handling, secure LLM endpoint validation, cache-path resolution, secret-free representations, and explicit boolean parsing.
- `tests/test_datahub_metadata_structure.py`, `tests/test_market_scan_repository_structure.py`, `tests/test_scheduler_structure.py`: facade compatibility, dependency direction/no cycles, normalized-set consistency, and production module size boundaries after the service/repository splits.
- `tests/test_market_scan_modes.py`, `tests/test_market_scan_automation.py`, `tests/test_market_scan_execution.py`, `tests/test_market_scan_lifecycle.py`, `tests/test_market_scan_pressure.py`, `tests/test_market_scan_retry.py`, `tests/test_market_scan_repository.py`, `tests/test_market_scan_scoring.py`, `tests/test_market_scan_validation.py`: all three mode windows, distinct rule fingerprints, completed-session `preopen` quote/K-line alignment and 09:15 runtime stop, current-quote/previous-close intraday validation, official same-date validation, automatic-official-only behavior, retry date/mode preservation, adaptive provider pressure, unified retry plans, atomic scan/task terminal state, structured degradation, stock-pool recovery, deterministic scoring, and lifecycle release on persistence failure.
- `tests/test_market_scan_retention.py`: configured published keep-window convergence, explicit transaction requirement, pre-delete seal verification, exact whole-graph deletion, five-trigger restoration and rollback on tamper/injected failure, dynamic and semantic database-reference families, retained/active/directly-protected retry roots with cross-candidate multilevel parent closure, outward task ownership that does not pin, exact recursive file-artifact run binding, fixed future-summary schema/count/item-digest/offline-replay verification, canonical-project-only empty-primary fallback ownership, bounded-memory probability verification under legacy/current scopes with long/singleton/tampered manifests, sparse fit-member pinning, and fail-closed catalog/TOCTOU behavior.
- `tests/test_llm_explainer.py`: LLM settings, incomplete configuration fallback, exact authoritative bindings, structured prompt serialization and repair instructions, at-most-one validation-correction request within one shared total timeout, no SDK retry, deterministic second-failure fallback, grounded answers, API-key/message separation, redacted diagnostics, and factual guardrails.
- `tests/test_analysis_research.py`: indicators, data quality, analysis, research reports, market sampling normalization/fallback logging, strong-stock K-line degradation, valuation history, and alert quality gates.
- `tests/test_minute_analysis_modules.py`: strict minute-row cleaning, represented-instant sorting/de-duplication, latest Shanghai-local date isolation, finite non-negative volume/amount/turnover handling, response availability/reason/audit-row contracts, support/resistance candidate filtering, T-plan zone validation, conservative no-price/no-range/insufficient-sample fallbacks, warning priority, and confidence downgrades.
- `tests/test_cache_freshness_modules.py`, `tests/test_cache_stats_modules.py`, `tests/test_trading_calendar_modules.py`: quote/daily/minute business freshness, stock-pool and plate metadata freshness/availability, separate daily/minute cache statistics, lunch/reopen/close boundaries, holidays, and the 15:15 daily publish threshold.
- `tests/test_api_watchlist_research_queue.py`, `tests/test_watchlist_research_queue.py`, `tests/test_research_conclusion_change.py`: local-only queue updates and validation, deterministic queue order, viewed-through mark-viewed semantics, comparable changed-advice unread counts, versioned conclusion identities, and comparable/non-comparable timeline changes.
- `tests/test_api_local_data_routes.py`, `tests/test_local_data_portability.py`, `tests/test_runtime_backup.py`: state-bound single-use import claims, serialized backup-before-commit behavior, immutable review-ledger/digest/relationship portability across surrogate collisions, stale/conflict rollback, SQLite integrity plus foreign-key verification, unified/legacy restore guards, database-only restore rejection when managed side artifacts exist, missing-target artifact-lease serialization, managed-backup rotation, set-based cleanup, retry-lineage convergence, explicit history cleanup, and active/archived review-linked retention protection.
- `tests/test_frontend_advice_timeline.py`, `tests/test_frontend_chart_context.py`, `tests/test_frontend_chart_workspace.py`: timeline rendering/staleness, 240-row daily context with local 20/60/120/240 ranges, minute interval request budgets, independent companion request scopes, and explicit unavailable state for no-content/empty/mismatched minute responses.
- `tests/test_research_regime_modules.py`: cleaned market-regime context metrics, blank industry/factor/breadth-summary filtering, fallback breadth wording, ordered environment labels, named risk adjustments, factor-risk contributions, and missing-price guardrails.
- `tests/test_stock_rule_modules.py`: rule-spec metadata, complete valid 20-high breakout windows, finite/positive current price/MA20/support/volume-ratio/fund-score/valuation-score gates, anomaly missing-data/evidence, high-valuation chase boundaries, cautious data-quality gates, and stable sorting.
- `tests/test_data_quality_modules.py`: ordered quote-field sanitation, stricter high/low/change_pct boundaries, cache-source wording, latest parsable K-line date/source selection, missing-K-line de-duplication, and consistency penalty/anomaly guardrails.
- `tests/test_research_leadership_modules.py`: feature snapshot sanitation, concept evidence filtering and stable sorting, leadership missing-data notes, score-summary thresholds, data-quality downgrades, and tag limits.
- `tests/test_individual_workflow_modules.py`: `stock_workbench` stage boundaries, non-blocking advice snapshot persistence, local-state symbol normalization, fixed chart-mark/alert/note/event read limits, and response assembly guardrails.
- `tests/test_stock_analysis_modules.py`: peer stock-pool failure and optional-timeout propagation into the stable analysis sample-status contract without exposing provider details in user warnings.
- `tests/test_research_qa_answer_modules.py`: free-form Q&A topic strategies, registry coverage including `综合判断`, confidence penalty boundaries, conservative fallbacks, cleaned/deduped answer lists, non-finite wording guards, and repeated-action de-duplication.
- `tests/test_research_qa_report_modules.py`: fixed FAQ Q&A risk/reward evidence wording, report-item exit sanitation, support/resistance fallback wording, T-plan fallback evidence, and theme-concept invalid-literal/non-finite cleaning.
- `tests/test_research_chip_modules.py`: chip invalid-row filtering, finite positive price/volume gates, valid-sample gates, bucket boundaries, current-zone support/pressure bands, nearest-band ordering, and feature-price fallback to the latest valid close.
- `tests/test_research_replay_modules.py`: replay sample/window gates, invalid entry and target-day pending handling, finite price/volume/forward-return filtering, mature-sample success rates, bounded completed counts, and replay outcome/pattern note wording.
- `tests/test_research_validation_modules.py`: signal-validation rule priority, non-finite risk/confidence/factor fallbacks, confidence penalties, T-range strict open interval wording, defensive grouping, and overall-status priority.
- `tests/test_symbol_modules.py`: SH/SZ symbol normalization, provider symbol formatting, malformed-code errors, all-zero symbol rejection, and conflicting market marker rejection.
- `tests/test_indicator_volume_modules.py`: shared positive-volume ratio and average-volume boundaries.
- `tests/test_market_quotes_modules.py`: quote snapshot/history column mapping, Shanghai event-time normalization, mixed-timezone epoch ordering, equal-event fallback/completeness/fetched-at priority, legacy UTC ISO upgrades, trade-date persistence, invalid quote write filtering, and dirty cache read filtering.
- `tests/test_market_sampling_modules.py`, `tests/test_stock_lookup_modules.py`, `tests/test_quote_stream_modules.py`: market-sampling event writes, quote-confirmation persistence, and stream watchlist fallback reads stay off the event-loop thread.
- `tests/test_chart_marks_modules.py`: chart-mark visible-limit categories, regular-event filtering, malformed-date visibility/alignment guards, note/event text and price sanitation, and internal limit guards.
- `tests/test_local_lifecycle.py`: local persistence, guarded SQLite compatibility migrations, concepts/themes/events, legacy malformed note-date handling, dirty advice/alert legacy-row sanitation, atomic advice de-duplication, cancellation-dominant task-run terminal updates, workbench cache, advice history, and replay confidence.
- `tests/test_workbench_context_cache_modules.py`: cache expiry, cancelled in-flight cleanup, clear-during-build writeback prevention, concurrent request coalescing, and DataHub-owned cache isolation.
- `tests/test_api_container_modules.py`: API container/DataHub runtime object sharing, including workbench context cache ownership.
- `tests/test_static_assets.py`: static CSS entrypoint, CSS module guardrails, JS function-size guardrails, UI symbol all-zero rejection, escaped workbench review rendering, Node-based research-panel render/Q&A-submit/market/factor smoke coverage, diagnostics-panel smoke coverage, and fake-canvas chart mark plus dirty-K-line filtering smoke coverage.
- `tests/test_frontend_diagnostics.py`: diagnostics polling freshness, degraded-task and standby-instance rendering, and failed manual-monitor-task error preservation.
- `tests/test_frontend_notifications.py`: persisted enable/disable preference, event-ID cursor requests, first-enable/re-enable baselines, disabled-period no-backfill behavior, in-flight poll invalidation, ordered retry, and permission-error recovery.
- `tests/test_advice_reviews.py`, `tests/test_advice_review_window_contract.py`, `tests/test_active_research_review_backend.py`, `tests/test_api_review_routes.py`, `tests/test_frontend_review_scan.py`: append-only A-B-A plan revisions and evaluation attempts, CAS update/archive/evaluate, canonical digest/tamper/legacy fail closure, strict PIT fixed-session and suspension behavior, current-revision latest-as-of summary, no-store/generic error contracts, strict browser response/owner/count validation, partial dashboard availability, and stale local-request invalidation after archive.
- `tests/test_paper_trading.py`, `tests/test_api_paper_trading_routes.py`, `tests/test_frontend_paper_trading.py`: plan revision/digest CAS inside the write transaction, strict PIT/execution evidence, suspension and latched-exit behavior, T+1/locked-limit/board/cost semantics, trading-session benchmark/equity, immutable fingerprints/runs, no-store API, and explicit no-broker UI.
- `tests/test_api_stock_routes.py`: stock route-level contract guardrails for not-found behavior before expensive data fetches.
- `tests/test_stock_abnormal_events.py`: abnormal-event quiet state, mixed risk/positive priority, downside-risk detection, context metric calculation, and zero-base fallbacks.
- `tests/test_stock_event_summary.py`: event-summary quiet state, external-checklist triggers, and event-source ordering.
- `tests/test_valuation_modules.py`: valuation missing-field policy, percentile score direction/rule order, out-of-range percentile ignoring, latest daily history snapshot selection, finite valuation wording, valuation-anchor band priority, price-position fallback, and malformed valuation-history filtering.
- `tests/test_financial_health_modules.py`: financial-health missing-field policy, metric-card ordering, and liquidity wording.
- `tests/test_provider_failure_status_modules.py`: shared recent provider/capability failure window used by monitoring diagnostics and scheduler health events.
- `tests/test_scheduler_modules.py`, `tests/test_scheduler_structure.py`, `tests/test_runtime_coordinator.py`: scheduler task/state guardrails, facade compatibility and dependency boundaries, unified scheduler/scanner leadership exclusivity, standby takeover, cancellation-safe shutdown, task-definition ordering, manual/background/cancelled semantics, refresh coverage/degradation, cleanup summaries, and rescheduling. Full-market automatic no-action suppression must remain a non-authorizing lightweight header/database/leadership identity with a five-minute full-audit ceiling; market-scan identity changes invalidate it, unrelated monitor writes do not, and all API reads retain immediate whole-graph verification.
- `tests/test_tencent_provider_modules.py`, `tests/test_futu_provider_modules.py`, `tests/test_optional_kline_parsing_modules.py`: source-specific quote/K-line parser guardrails for strict price fields, Tencent payload matching/minimum fields, Tencent code/name/timestamp/OHLC containment, computed missing change-percent values, finite non-negative core volume/amount fields, malformed payload shapes/OHLC rows, Futu minute rows/order-book depth, Tushare/BaoStock daily rows, and demo-provider local random state plus rounded quote/K-line containment including Monday small-limit backfill.
- `tests/test_datahub_orderbook_modules.py`: order-book coordinator timeout wrapping, order-book capability failure recording, and cooldown behavior.

Candidate improvements:

- Continue expanding route-level contract tests for stock response models and any newly added API surface.
- When a route reads SQLite-backed state, add at least one failure-contract test that proves `sqlite3.DatabaseError` maps to a 503 `detail` response rather than a raw 500.
- When touching a route/service for a meaningful change, prefer importing models from the domain module instead of adding new imports to `app.models.schemas`.

### Priority 3: Frontend Modularization

Completed splits:

- `static/app.js`: app orchestration, core-workbench-first cold-load dispatch ahead of global panels, separate load/render/companion failure states, invalid-search load invalidation, symbol synchronization, immediate timeline loading ownership with A-B-A sequence guards, minute response symbol/interval/empty validation before state assignment, independent advice/minute abort scopes, local daily-range redraws, stale main/companion guards, delayed-redraw guards, sanitized warnings, duplicate-action guards, failed-load stream shutdown, post-import full-state/SSE refresh, fresh-session SSE backoff reset, reconnect/frame/stale-callback guards, ARIA tab/validation/filter state, keyboard tab navigation, mobile feedback reveal, and event binding.
- `static/js/api.js`: fetch wrapper and readable API error detail normalization.
- `static/js/alerts.js`: alert rule CRUD, manual evaluation with duplicate-click prevention, independent write scopes, local successful-write reconciliation, cancellable stale reload tails with explicit readback degradation, partial-failure counts, and event rendering.
- `static/js/advice-review-contracts.js`: strict plan/evaluation/due/batch/summary/snapshot identity, evidence-v2/current-versus-v1-conservative digest, time, count, metric, status/conclusion, evidence and denominator contracts before render.
- `static/js/advice-reviews.js`: snapshot-bound plan create/edit/archive, revision-CAS current-time versus historical evaluation, independently settled summary/due/list projections, Research Shadow boundary, lazy history loading/retry, and rendered-symbol/plan/sequence ownership with archive invalidation.
- `static/js/paper-trading.js`: exact plan revision/digest freeze, full dashboard/run/compare response validation including output-digest shape, child run/strategy/symbol ownership and count conservation before state/DOM mutation, immutable rendering, generic 5xx degradation, and explicit simulation/no-broker semantics.
- `static/js/chart.js`: canvas K-line drawing, frontend finite/positive OHLC filtering before coordinate math, moving-average lines, chart mark filtering/date matching, and mark rendering helpers.
- `static/js/diagnostics.js`: provider/cache/source-plan diagnostics, request-scoped scheduler/monitor polling, provider-detail helpers, degraded/standby scheduler status helpers, source-plan rendering helpers, task controls with failed-run error preservation, and monitor event rendering.
- `static/js/format.js`: numeric formatting and neutral tone handling for missing/non-finite values.
- `static/js/local-data.js`: bounded portable JSON handling, generation-owned file/read/preview/commit state, server preview-claim ownership, rollback-backup feedback, and latest-request-only cleanup preview/commit controls.
- `static/js/market-scan.js`: stable scan re-export facade.
- `static/js/market-scan-contracts.js`: strict start/run/history/result contracts and run/status classification; no DOM or network ownership.
- `static/js/market-scan-controller.js`: global-task/selected-mode state ownership, requests, history binding, visibility/online events, pagination reset, and UI binding.
- `static/js/market-scan-filters.js`: the only browser normalizer for repeated markets/industries, six ranges, and three sort levels; keep ordinary results, export, and editable presets on this contract.
- `static/js/market-scan-history.js` and `market-scan-history-view.js`: mode/status/date history lifecycle and rendering.
- `static/js/market-scan-polling.js`: the only scan timer, bounded exponential backoff, and latest-run recovery.
- `static/js/market-scan-polling-identity.js`, `market-scan-latest-sync.js`, and `market-scan-latest-loader.js`: exact non-authorizing token validation, bounded before/trusted/after synchronization, and trusted `latest`/mode-scoped `latest-published` staging. Keep opaque identity headers out of rendered/authorized run state; unchanged ticks may skip trusted reads, but every initial/changed/forced sync must use both existing fully verified selectors before reading results from their published ID.
- `static/js/market-scan-progress-view.js`: stage/ETA/throughput/per-market progress and actionable terminal diagnostics.
- `static/js/market-scan-snapshot-view.js` and `market-scan-row-actions.js`: persisted-only evidence rendering and current-analysis intent separation.
- `static/js/market-scan-view.js`: escaped display formatting, result rendering, normalized URL construction, and ARIA announcements; no requests or timers.
- `static/js/notes.js`: stock note CRUD, independent write scopes, cancellable stale reload/chart-mark tails, and note-list rendering.
- `static/js/notifications.js`: explicit enable/disable and permission handling, persisted preference, fresh no-backfill baselines, event-ID keyset draining, disabled-period/in-flight suppression, and successful-prefix cursor advancement that preserves failed/later events for retry.
- `static/js/primary-navigation.js`: top-level work-area visibility and button state. Keep work-area membership in `workspace-preferences.js`; cross-area actions should call the shared setters instead of toggling DOM classes directly.
- `static/js/research-panels.js`: AI dashboard, Q&A submit flow with stale-request and duplicate-submit guards, evidence chain, Alpha/timeframe/risk-reward view helpers, factor lab, theme, chip, replay, panel-level optional-card failure isolation, and card-level view helpers.
- `static/js/research-render-utils.js`: shared safe array/object/text coercion, escaped list, metric-pair, missing-data, signed-value, and threshold-tone helpers for research panels.
- `static/js/advice-timeline.js`: versioned recommendation timeline, conclusion-change rendering, provenance, and legacy/version-change fallback states.
- `static/js/watchlist.js`: watchlist research-queue add/delete/patch/mark-viewed, response-shape guardrails, scoped read freshness, independent write scopes, duplicate-submit prevention, last-known watchlist preservation on readback failure, and queue rendering.
- `static/js/watchlist-scan.js`: fixed-condition current/custom universe selection, historical cutoff validation, bounded symbol normalization, stale-result ownership, and result/missing-data rendering.
- `static/js/workbench.js`: main stock card, insight panels, valuation/minute-analysis view helpers, minute-analysis detail helpers, stock-event/evidence rendering helpers, quality/review panels, market strip, and quote-list rendering.
- `static/js/workbench-render-utils.js`: shared safe array/object coercion and escaped list/tag rendering helpers for workbench panels.
- `static/styles.css`: CSS import manifest.
- `static/css/base.css`: design tokens, global reset, top bar, base layout, search controls, and shared hover behavior.
- `static/css/sidebar.css`: watchlist, data-source health, scheduler tasks, and monitor-event styles.
- `static/css/workspace-core.css`: market strip, workspace tabs, stock header, metrics, summary, quality, and core research panels.
- `static/css/research-panels.css`: AI Q&A dashboard, evidence chain, insight panels, theme/chip/replay/finance panels.
- `static/css/interactions.css`: rule cards, strategy cards, events, review/timeline, alerts, notes, chart marks, and minute-analysis styles.
- `static/css/side-footer.css`: right-side quote/leader lists, leader-scope wrapping, footer, empty states, and error states.
- `static/css/responsive.css`: responsive layout, touch-target, overflow, and narrow-screen rules. Keep source DOM order (`query`, `workspace`, `controls`, side column) aligned with mobile visual/focus order instead of reordering focusable regions in CSS.
- `static/css/primary-navigation.css`: top-level navigation, work-area grid overrides, sticky offsets, and mobile navigation sizing.

Candidate improvements:

- Add browser-level visual regression screenshots for the main workbench and mobile breakpoint.

### Priority 4: Remaining DataHub Decomposition

`DataHub` already delegates to `QuoteCoordinator`, `KlineCoordinator`, `MetadataCoordinator`, `OrderBookCoordinator`, `DataStatusService`, source-plan helpers, and runtime/status helpers. `ProviderRuntime` owns the shared provider-attempt iterator and timed calls, and `StockPoolResolver` owns the stock-pool/profile cache/provider/fallback state machine. Remaining decomposition work should focus on the wide cache facade and cross-cutting status projections:

- Keep quote/K-line/metadata coordinators responsible only for domain row validation, fallback-cache policy, coverage semantics, and response shaping.
- Split narrow cache protocols such as quote cache, K-line cache, metadata cache, runtime-event sink, and provider-status store for easier fake objects in tests, while keeping `SQLiteCache` as the composition root.
- Move deeper provider status projection policy out of persistence CRUD if provider/capability state grows again.

### Priority 5: Current Maintainability Ratchets

The 2026-08-12 audit now records 368 application Python files, 54 modules above 500 lines, 298 cross-module private dependencies, and 214 directly listed mypy application files. Treat these as migration baselines, not targets:

- expand mypy by high fan-in/high-risk modules and raise both the exact protected set and count floor in the same change;
- extract one cohesive pure-computation section from a large module at a time, preserve re-exports, prove normalized reports/digests equivalent, and immediately set a lower module budget; ordinal calibration is the first completed `market_scan_evaluation_metrics.py` slice;
- keep browser `ScreenSpecV2` acceptance in Python↔Node parity, including defaults, partial objects, extra fields, finite numeric bounds, uniqueness and sort semantics;
- do not expand `MarketScanCacheProtocol`; introduce query, command, publication and capture ports for new consumers;
- keep full-market/Discovery flows in `market-scan-flow.spec.js` and the remaining workbench flows in `frontend-flow.spec.js`; preserve both per-file limits and the combined 2,747-line pre-split ceiling;
- after the current Strategy evidence styles stabilize, move their selector block byte-for-byte to a single-owner stylesheet and verify computed desktop/mobile geometry.

See `docs/research/ENGINEERING_MAINTAINABILITY_AUDIT_2026_08_12.md` for measurements, completed slices, exclusions and the ordered P1/P2 plan.

## 3. Change Checklist

For any functional change:

1. Identify the affected SRS requirement.
2. Update design docs if boundaries or data flow change.
3. Add or update unit tests.
4. Run targeted backend/frontend checks while iterating.
5. Run Ruff, incremental mypy, JavaScript syntax, `pip check`, and the coverage-enabled pytest suite before delivery.
6. Regenerate `docs/FUNCTION_INVENTORY.md` when Python functions/classes move, then run `tools/architecture_inventory.py --check`.
7. Regenerate `docs/API_REFERENCE.md` when routes change, then run `tools/api_inventory.py --check`.
8. Rebuild both runtime/development Python 3.12 locks after a runtime dependency change, the development lock after a development-only change, or the isolated security lock after an audit/SBOM-tool change; verify clean `--require-hashes` installs.
9. Smoke test `/api/health` and at least one stock workbench request.

## 4. Code Ownership Map

| Feature | Primary Files |
| --- | --- |
| App startup, runtime leadership, API security, and error mapping | `app/main.py`, `app/api/container.py`, `app/services/runtime_coordinator.py`, `app/services/instance_guard.py`, `app/api/security.py`, `app/api/errors.py`, `app/config.py` |
| Quote routes and streaming | `app/api/routes/quotes.py`, `app/services/datahub_quotes.py`, `app/services/datahub_cache.py`, `app/services/datahub_runtime.py`, `app/services/eastmoney_client.py`, `app/services/providers.py` (Tencent URL/text/payload helpers), `app/services/akshare_provider.py`, `app/services/akshare_mappers.py`, `app/services/futu_provider.py`, `app/services/futu_mappers.py` |
| K-lines and minute data | `app/utils/market_data.py` (finite/OHLC/K-line validity), `app/services/datahub_klines.py`, `app/services/datahub_cache.py`, `app/services/datahub_runtime.py`, `app/repositories/market_klines.py`, `app/services/provider_utils.py`, `app/services/eastmoney_client.py`, `app/services/providers.py`, `app/services/akshare_provider.py`, `app/services/tushare_provider.py`, `app/services/baostock_provider.py`, `app/services/futu_provider.py`, `app/services/futu_mappers.py` |
| Stock pool, plate, and concepts | `app/services/datahub_metadata.py`, `app/services/datahub_metadata_stock_pool.py`, `app/services/datahub_metadata_coordinator.py`, `app/services/datahub_metadata_provider.py`, `app/services/datahub_metadata_mapping.py`, `app/utils/stock_pool.py`, `app/repositories/market_metadata.py`, `app/services/datahub_cache.py`, `app/services/datahub_runtime.py`, provider adapters/mappers |
| Order book and Futu ping | `app/services/datahub_orderbook.py`, `app/services/datahub_runtime.py`, `app/services/futu_provider.py`, `app/services/futu_mappers.py` |
| Provider call runtime, daemon executor, cooldowns, and error sanitation | `app/services/datahub_runtime.py`, `app/services/daemon_executor.py`, `app/services/provider_errors.py`, `app/repositories/provider_status.py`, `app/repositories/provider_status_aggregation.py`, `app/db/system_mappers.py` |
| Provider source plan and status wording | `app/services/datahub_status_service.py` (DataHub status assembly, provider enabled/capability synchronization), `app/services/datahub_source_plan.py` (provider-name/status de-duplication, missing-primary warnings), `app/services/datahub_status.py` (clean status text, duplicate capability status ranking, finite success-rate counts), `app/services/provider_registry.py`, `app/repositories/provider_status.py`, `app/repositories/provider_status_aggregation.py` |
| Provider capability state and priority mapping | `app/services/provider_registry.py`, `app/repositories/provider_status.py`, `app/repositories/provider_status_aggregation.py` (enabled/active classification, disabled stale-activity guards, and runtime update enabled-state preservation) |
| SQLite schema initialization and compatibility migrations | `app/db/schema.py`, `app/db/schema_definitions.py`, `app/db/schema_migrations.py` (tables first, compatibility columns/backfills second, dependent indexes last; partial legacy guards and idempotent records) |
| SQLite row mapping | `app/db/mappers.py`, `app/db/market_mappers.py`, `app/db/system_mappers.py`, `app/db/user_mappers.py` |
| Quote event time, snapshots, and valuation history | `app/utils/market_time.py` (Shanghai normalization and event epoch), `app/db/connection.py` (SQLite function registration), `app/repositories/market_quotes.py` (column-list persistence and epoch freshness guards), `app/db/schema_definitions.py`, `app/db/schema_migrations.py` |
| Daily and minute K-line persistence and freshness | `app/repositories/market_klines.py`, `app/services/datahub_cache.py`, `app/services/cache_freshness.py`, `app/services/data_quality_time.py`, `app/services/data_quality_kline.py`, `app/services/trading_calendar.py`, `app/db/schema_definitions.py` |
| Stock pool, plate rank, and concepts persistence | `app/repositories/market_metadata.py`, `app/db/schema_definitions.py`, `app/db/schema_migrations.py` |
| Market data repository compatibility | `app/repositories/market_data.py`, `app/services/cache.py` |
| Full-market scan orchestration, scoring, exact rule/skip/publication contracts, and evaluation | `app/services/market_scan_manager.py`, `app/services/market_scan_execution.py`, `app/services/market_scan_stock_evaluation.py`, `app/services/market_scan_completion.py`, `app/services/market_scan_lifecycle.py`, `app/services/market_scan_scoring.py`, `app/services/market_scan_skip_contract.py`, `app/services/market_scan_skip_pit.py`, `app/services/market_scan_replay.py`, `app/services/market_scan_evaluation.py`, `app/repositories/market_scan_rule_contracts.py`, `app/repositories/market_scan_verified_read.py`, `app/db/market_scan_action_source.py`, `app/api/routes/market_scan.py`, `tools/evaluate_market_scan.py`, `tools/benchmark_market_scan.py` |
| Full-market snapshot seal and startup migration | `app/db/market_scan_integrity.py` (byte-compatible cursor-streamed v2 SHA with largest-row memory bound), `app/db/schema.py`, `app/db/schema_migrations.py`, `app/db/strategy_lab_schema.py`, `app/models/market_scan_snapshot.py` |
| Full-market scan persistence/filter/export | `app/market_scan_repository_contracts.py`, `app/repositories/market_scan.py`, `app/repositories/market_scan_lifecycle.py`, `app/repositories/market_scan_lifecycle_support.py`, `app/repositories/market_scan_results.py`, `app/repositories/market_scan_queries.py`, `app/repositories/market_scan_automatic_state.py`, `app/repositories/market_scan_filtering.py`, `app/repositories/market_scan_mapping.py`, `app/repositories/market_scan_context.py`, `app/repositories/market_scan_screening.py`, `app/repositories/market_scan_delta.py`, `app/repositories/market_scan_screen_alert.py`, `app/services/market_scan_export.py` |
| Full-market bounded graph retention | `app/repositories/maintenance.py`, `app/repositories/market_scan_retention.py`, `app/repositories/runtime_research_artifact_retention.py`, `app/db/market_scan_integrity.py`, `app/config_settings.py` (keep-window candidates; database/retry/file reachability; explicit-transaction whole-graph deletion; five-trigger restoration; fail-closed artifact fingerprints) |
| Discovery presets and queue provenance | `app/api/routes/discovery.py`, `app/services/discovery.py`, `app/repositories/discovery.py`, `app/repositories/discovery_sql.py`, `app/db/discovery_schema.py`, `app/models/discovery.py`, `static/js/discovery.js`, `static/js/market-scan-filters.js` |
| Analysis assembly | `app/services/analysis.py` (trend metrics, signal bundle, result composition), `app/services/strong_stocks.py`, `app/services/leader_scoring.py` |
| Analysis signal rules | `app/services/analysis_signals.py`, `app/services/analysis_signal_points.py` (ordered risk/buy/sell/strength rules, T-plan invalid-zone pending wording), `app/services/analysis_signal_quality.py` (kind-specific quality gates), `app/services/analysis_signal_snapshot.py`, `app/services/analysis_signal_advice.py` |
| Indicator calculations | `app/services/indicators.py`, `app/services/indicator_trend_components.py` (MA/volume contribution rules), `app/services/indicator_trend.py`, `app/services/indicator_levels.py`, `app/services/indicator_volume.py` (positive-volume ratio helpers), `app/services/indicator_math.py` |
| Data quality scoring and gates | `app/services/data_quality_components.py` (ordered quote-field sanitation, K-line/cache wording, consistency penalties/anomalies), `app/services/data_quality.py`, `app/services/data_quality_time.py`, `app/services/data_quality_kline.py`, `app/services/analysis_signal_quality.py` |
| Workflow compatibility facade | `app/workflows/individual.py` (`stock_workbench` advice snapshot persistence, normalized-symbol local-state reads, response assembly) |
| Stock lookup and validation | `app/workflows/stock_lookup.py` (stock-pool confirmation, strict quote-confirmation fallback during stock-pool outage or miss, mismatched quote rejection, unexpected quote-error propagation, and industry matching), `app/utils/symbols.py` |
| Base stock analysis and minute workflow | `app/workflows/stock_analysis.py` (optional local enrichment and best-effort minute fallback logging), `app/services/analysis.py`, `app/services/review.py`, `app/services/minute_analysis.py`, `app/services/datahub_cache.py` |
| Workbench research pipeline | `app/workflows/workbench_pipeline.py`, `app/services/workbench_context.py`, `app/services/research*.py`, `app/services/stock_insights.py` |
| Market overview and strong-stock workflow | `app/workflows/market_overview.py` (custom-list empty-quote errors), `app/services/market_sampling.py` (sample normalization, stock-pool code/market/industry consistency filtering, duplicate-safe sampling, request-symbol quote filtering, missing-symbol fallback logging, skipped-value logging, cancellation-safe partial quote fallback), `app/services/strong_stocks.py`, `app/services/leader_scoring.py` |
| Research report facade | `app/services/research.py` |
| Research alpha evidence | `app/services/research_alpha_points.py`, `app/services/research_alpha.py` (finite-impact and display-text filtering, strength sorting, non-positive limit guards, cleaned title/reason de-duplication, dirty-text/`N/A` filtering, explicit missing-data merge, cleaned data-quality notes, bounded confidence adjustments, verdict finite fallbacks) |
| Research market breadth | `app/services/research_breadth.py` |
| Research chip analysis | `app/services/research_chip.py` (daily-K chip buckets, finite positive price/volume filtering, feature-price fallback, boundary clamps, current-zone support/pressure bands) |
| Research diagnosis | `app/services/research_diagnosis.py`, `app/services/research_diagnosis_decisions.py`, `app/services/research_diagnosis_sections.py` |
| Research evidence chain | `app/services/research_evidence.py` |
| Research event digest | `app/services/research_events.py` |
| Research factor lab assembly | `app/services/research_factors.py`, `app/services/research_factor_current.py`, `app/services/research_factor_report.py` |
| Research factor scoring | `app/services/research_factor_scoring.py` (volume/risk/chip rule tables), `app/services/research_factor_specs.py` (read-only registration snapshot, blank/whitespace ID validation, complete metric windows, trigger score sanitation) |
| Research factor weights | `app/services/research_factor_weights.py` |
| Research factor text and impact helpers | `app/services/research_factor_text.py` |
| Research factor calibration | `app/services/research_factor_calibration.py`, `app/services/research_factor_specs.py` |
| Research feature snapshot and leadership | `app/services/research_features.py` (feature metric cleaning, leadership input sanitation, concept evidence filtering/sorting, company-profile missing-data notes) |
| Research peer comparison | `app/services/research_peer.py` |
| Research Q&A facade | `app/services/research_qa.py` |
| Research free-form Q&A | `app/services/research_qa_answer.py` (confidence penalty rules, whitespace-normalized topic lookup, shared answer cleaning/limits/fallbacks, invalid-literal and finite-number wording, and `TopicAnswerStrategy` registry), `app/services/research_qa_topics.py`, `app/services/research_qa_utils.py` |
| Research fixed QA report | `app/services/research_qa_report.py`, `app/services/research_qa_utils.py` (fixed FAQ generation, report-item normalization, risk/reward pending-level wording, support/resistance guards, T-plan fallback evidence, and theme-concept evidence cleaning) |
| Research market regime | `app/services/research_regime.py` (cleaned context metrics, ordered market labels, named risk adjustments, blank industry/factor/breadth-summary filtering, finite evidence/suggestions) |
| Research replay analysis | `app/services/research_replay.py` (pattern rules, target-day pending handling, finite forward-return statistics, note templates, mature-sample boundaries) |
| Research risk radar | `app/services/research_risk.py` |
| Research risk/reward | `app/services/research_risk_reward.py` (staged report assembly, finite/non-positive input cleaning, capped upside targets, structural-stop filtering, target/stop side validation before distance math, finite reward/risk ratio, pending summary wording, downside-stop adjustment rules, rating rules, decision-state scenario probability caps, integer scenario probability normalization with neutral floor, scenario plans with missing/wrong-side-level downweighting and wording guards) |
| Research theme context | `app/services/research_theme.py` (theme stats with finite industry/concept cleaning, ordered evidence rules, opportunity/risk rules, report caps after full-input scoring) |
| Research timeframe alignment | `app/services/research_timeframe.py` |
| Research T strategy | `app/services/research_t_strategy.py` (style/suitability rule tables, low/high zone fallbacks, stop-condition wording) |
| Research signal validation | `app/services/research_validation.py` (central status/timeframe constants, finite risk/confidence/factor fallbacks, strict status/confidence/overall rule tables, T-range open interval) |
| Insight bundle assembly | `app/services/stock_insights.py` |
| Fund flow and order pressure | `app/services/stock_activity.py` (fund-flow score rules, order-book threshold rules, shared valid-depth cleaning, invalid-depth/crossed-spread filtering, zero-baseline volume guards) |
| Local abnormal event detection | `app/services/stock_abnormal_events.py`, `app/services/stock_abnormal_context.py`, `app/services/stock_abnormal_rules.py`, `app/services/stock_abnormal_summary.py` |
| LHB candidate checks | `app/services/stock_lhb.py` |
| Stock event source adapters and panel aggregation | `app/services/stock_event_sources.py`, `app/services/stock_event_summary.py` |
| Stock event compatibility imports | `app/services/stock_events.py` |
| Overview scoring and risk triggers | `app/services/stock_overview.py` (score helpers, positive valuation guards, finite/clean fundamental fields, shared support/resistance normalization, visible-event de-duplication, and ordered main-conflict rules) |
| Strategy cards | `app/services/stock_strategy.py` (data-quality status downshift maps, fallback signal handling) |
| Financial metric interpretation | `app/services/financial_metrics.py`, `app/services/scoring.py` |
| Financial health diagnostics | `app/services/financial_health_components.py`, `app/services/financial_health.py`, `app/services/scoring.py` |
| Valuation anchors, score components, and response assembly | `app/services/valuation_anchors.py` (latest daily snapshots, bounded percentiles), `app/services/valuation_components.py` (percentile delta rules, finite wording, core/enrichment missing-data policy), `app/services/valuation_analysis.py`, `app/services/scoring.py` |
| Finance compatibility imports | `app/services/stock_finance.py` |
| Rules and rule matches | `app/services/stock_rules.py` (`RULE_SPECS`, `RULE_CONFIDENCE`, spec-derived match metadata, complete valid 20-high windows, finite/positive input gates, rule states, missing-price/anomaly evidence guards, break-MA20 helpers, high-valuation chase helpers, cautious quality-gate decisions, sort keys), `app/services/analysis_signal_quality.py` |
| Alerts | `app/services/alerts.py`, `app/api/routes/alerts.py`, `app/repositories/alerts.py`, `app/db/user_mappers.py`, `app/repositories/update_fields.py` (central transitions, per-rule provider-failure isolation, snapshot-guarded state writes, finite thresholds/cooldowns, disabled legacy rules, sanitized events, and explicit-column persistence) |
| Notes and marks | `app/api/routes/notes.py`, `app/repositories/notes.py`, `app/repositories/update_fields.py`, `app/services/chart_marks.py` (date normalization, finite positive price guards, blank-date clearing, stable note ordering, malformed-date visibility guards, text/price sanitation, visible-limit categories, and event-to-mark helpers) |
| Watchlist queue and conclusion timeline | `app/api/routes/watchlist.py`, `app/repositories/watchlist.py`, `app/repositories/advice.py`, `app/services/research_conclusion_change.py`, `app/db/user_mappers.py` (local queue updates/order, viewed-through mark-viewed state, comparable changed-advice unread counts, versioned conclusion provenance/comparison, dirty-row display fallbacks, and immediate-transaction dedupe) |
| Advice review plans and evaluation history | `app/api/routes/reviews.py`, `app/models/reviews.py`, `app/services/advice_review.py`, `app/repositories/advice_reviews.py`, `app/db/advice_review_schema.py`, `app/db/advice_review_evidence_schema.py`, `app/services/research_replay.py`, `static/js/advice-review-contracts.js`, `static/js/advice-reviews.js` (canonical append-only plan revisions/evaluation attempts, CAS archive/update/evaluate, digest-verified PIT/session evidence, conservative legacy/tamper reads, current-revision summary, strict browser contracts, independent dashboard sections, and stale-response ownership) |
| Review-plan paper simulation | `app/api/routes/paper_trading.py`, `app/models/paper_trading.py`, `app/services/paper_trading.py`, `app/services/paper_trading_rules.py`, `app/services/paper_trading_costs.py`, `app/repositories/paper_trading.py`, `app/repositories/paper_trading_metrics.py`, `app/db/paper_trading_schema.py`, `static/js/paper-trading.js` (transactional plan revision/digest CAS, immutable-revision rebind on later reads/runs, completed fixed-session PIT evidence, T+1, suspension/locked-limit events, trading-session valuation/benchmark, frozen-run initial-cash performance, immutable run provenance, full-output digest v3, strict browser ownership/count contracts, and no-broker boundary) |
| Watchlist scans | `app/api/routes/watchlist_scan.py`, `app/services/watchlist_scan.py`, `static/js/watchlist-scan.js` (versioned fixed rules, active/custom universes, historical completed-bar cutoff, bounded concurrency/symbols, and missing-data isolation) |
| Local-data portability, backup, and retention | `app/api/routes/local_data.py`, `app/services/local_data_import_guard.py`, `app/services/user_data_portability.py` (immutable review-ledger validation, digest-aware surrogate remapping and target-side paper-run output-digest recomputation), `app/services/runtime_backup.py` (integrity/foreign-key verification, rotation and unified/legacy restore guards), `app/repositories/maintenance.py`, `app/repositories/market_scan_retention.py`, `app/repositories/runtime_research_artifact_retention.py` (set SQL, protected graph/file reachability, throttle), `app/services/cache.py` (exclusive local-data lock), `tools/runtime_data.py` |
| Browser alert notifications | `app/api/routes/alerts.py`, `app/repositories/alerts.py`, `static/js/notifications.js` (event-ID keyset pages, persisted enable/disable preference, fresh no-backfill baselines, disabled-period suppression, successful-prefix cursor advancement, and failed/later event retry) |
| Scheduler, health, and monitoring | `app/services/scheduler.py`, `app/services/scheduler_contracts.py`, `app/services/scheduler_lifecycle.py`, `app/services/scheduler_execution.py`, `app/services/scheduler_tasks.py`, `app/services/scheduler_schedule.py`, `app/services/scheduler_health.py`, `app/services/scheduler_helpers.py`, `app/services/scheduler_service.py`, `app/services/runtime_coordinator.py`, `app/services/cache_freshness.py`, `app/services/system_diagnostics.py`, `app/api/routes/health.py`, `app/api/routes/monitoring.py`, `app/repositories/runtime.py` |
| Reliability measurement | `app/models/reliability.py`, `app/repositories/reliability.py`, `app/services/reliability.py`, `app/workflows/individual.py`, `app/repositories/provider_status.py`, `app/services/cache.py` |
| Runtime, time, configuration, and engineering gates | `app/runtime_environment.py`, `app/utils/clock.py`, `app/utils/audit_time.py`, `app/config.py`, `app/config_settings.py`, `app/config_shell.py`, `app/config_validation.py`, `requirements*.txt`, `package*.json`, `pyproject.toml`, `.python-version`, `.node-version`, `.github/workflows/ci.yml`, `.github/workflows/security.yml`, `.github/dependabot.yml`, `tools/runtime_contract.py`, `tools/provider_canary.py`, `tools/generate_sbom.py` |
| Frontend | `app/api/static_assets.py`, `static/app.js`, `static/js/*`, `static/styles.css`, `static/css/*`; scan-specific contracts/controller/polling/view are `static/js/market-scan-*.js` |

## 5. Invariants

- `standard_symbol` format should remain `CODE.MARKET`, for example `600519.SH`.
- Supported engineering runtimes remain Python 3.12.x, Node 22.x/24.x, and npm 10.x/11.x; declaration drift is a failing test.
- Market time is Shanghai, audit time is UTC `Z`, and elapsed duration is monotonic. Host timezone and wall-clock jumps must not change trading rules, TTLs, or recorded durations.
- Lower layers do not import API/workflows/services, application imports remain acyclic, and production code does not depend on the aggregate model compatibility facade.
- Liveness is process-only; readiness is lifecycle admission plus read-only SQLite, not a live-provider check. A standby process can be ready.
- Reliability status remains `insufficient_data` below its explicit sample floor. Bucket dimensions remain bounded and never contain symbols, request identifiers, exception text, credentials, or URLs.
- Required CI stays hermetic. Live provider canary results are optional environment evidence and must never become a required pull-request gate.
- Workflow actions remain SHA-pinned, checkout credentials are not persisted, current and historical secret scans stay redacted, and SBOM reproducibility is verified by generating twice from locked dependency manifests.
- Cache fallback must be visible through source or quality notes.
- Demo provider must remain disabled by default.
- Alert triggers must honor cooldown.
- Recovery events must not spam when state is unchanged.
- Advice history should dedupe repeated same conclusions within the configured window.
- Advice comparison, de-duplication/insertion, and any watchlist unread increment must remain one `BEGIN IMMEDIATE` transaction across cache instances. Only a new, comparable, changed conclusion increments unread; first, unchanged, legacy/version-changed, and de-duplicated snapshots do not. Any unread failure rolls back the insert.
- Mark-viewed must clear only through a positive advice ID that exists for the same symbol, then recompute comparable changed conclusions after that watermark in the same transaction. Missing watermarks preserve unread state so a client cannot erase changes it did not display.
- Empty-query SSE fallback must not turn an all-excluded watchlist back into configured seeds. Seeds apply only when the watchlist table has no rows; explicit symbol queries may still request an excluded symbol.
- Daily cache reuse, cache diagnostics, and K-line quality must use the same 15:15 current-day publication threshold; quote and minute freshness must use the same Shanghai session boundaries. The trading-day scan windows are `preopen [00:00, 09:15)`, intraday `[09:30, 15:15)`, and official from 15:15 onward. They never overlap, and 09:15-09:30 deliberately admits neither `preopen` nor intraday.
- Trading-session ranges are inclusive. Paper price-limit rules count the listing session itself as session 1, accept a listing date equal to the trusted calendar's left boundary, and fail closed only when the requested interval actually starts before trusted coverage.
- Minute analysis must never merge rows across Shanghai-local dates, and `availability=unavailable` must not expose actionable levels or T-plan ranges even when filtered audit `klines` are present.
- Daily range switching is local over the 240-row workbench payload. Advice timeline and minute requests use independent abort/sequence state; a new minute interval costs one request and an unchanged interval costs none.
- A current timeline request must claim the panel with its loading state immediately; symbol plus load sequence must reject every older A-B completion after an A-B-A switch.
- Minute no-content/empty/non-object responses and report symbol/interval mismatches must clear minute state and render unavailable rather than preserve loading or stale chart data.
- Older quote events must never overwrite newer snapshots or same-day history. Compare normalized market epochs. For an equal event, prefer non-fallback, then optional-field completeness, then fetched-at recency; keep legacy UTC ISO rows comparable while persisting new Shanghai fixed-format timestamps.
- Browser mutations and refresh writes must pass both Host-derived-origin and Origin/Referer allowlist checks before side effects, rejecting Host/DNS rebinding; ordinary read-only and non-browser requests remain available.
- Internal model `ValidationError` responses must stay generic `503` and must not expose rejected provider/database values.
- Blocking provider work stays in the bounded runtime-owned daemon executor, SQLite work stays off the event loop, and DataHub shutdown reaches `aclose()` on normal and failed startup paths. Queued provider futures are cancelled at close; a running SDK thread remains tracked and cannot be force-killed, but must not block process exit.
- Provider errors, including quoted secrets in JSON/Python-style mappings, must be sanitized both before persistence/response assembly and after reading historical status rows.
- Scheduler task-run cancellation is terminal: a cancellation may replace a racing success, while late success/failed updates cannot replace `cancelled`.
- Scheduler fallback or incomplete-but-usable refreshes must persist `degraded`, not `success`; a process without runtime leadership must report live `standby` through scheduler and scanner views and must not activate either service independently.
- Dirty local user rows should stay displayable but must not become executable signals, absorb fresh advice snapshots, or write non-finite values back into alert events.
- A failed local side effect after successful data acquisition must not clear previous good metadata/chart-mark cache or turn a usable workbench response into a global load failure.
- Runtime cleanup must keep historical market tables by partition: `quote_history` per symbol, `kline_daily` per symbol and adjustment, `kline_minute` per symbol and interval, and `stock_concept` per symbol. Ordinary candidate selection remains set-based. `max_market_scan_runs` is a newest-run keep-window target: delete every unreferenced sealed graph outside it, but protect active work, every real database/semantic reference, explicit run IDs recursively bound by the closed verified probability/future/individual artifact catalog, and every candidate ancestor reachable from a noncandidate or directly protected retry root. Parent closure must cross candidate/noncandidate nodes, use visited-set cycle protection, and never permit `ON DELETE SET NULL` to detach a retained lineage. Strictly enumerate the runtime-local individual primary and inspect the tracked fallback only when that primary has no files; validate the fixed future summary's schema/count/item digests/offline-replay flags. Never infer a numeric fit range from `through_run_id`; never let a run's outward `task_run_id` pin it. Snapshot supported artifact directories/files before the write transaction and recheck them immediately before published deletion and before commit; unknown, unsafe, malformed, tampered, changed or unverifiable state fails closed. Published graph deletion must stay in one explicit transaction, verify the v2 seals and five triggers, remove result then run only while the exact trigger set is temporarily suspended, restore/reverify it in `finally`, demand exact counts, and roll back on any failure. The digest is not an archive. Scan cleanup precedes task cleanup so newly unreferenced tasks can leave immediately. Scheduled cleanup uses only regenerable specifications and obeys its maintenance throttle; explicit cleanup previews user-history candidates, creates a verified rotated backup before deleting any, and excludes advice referenced by review plans. After a committed deletion, physical compaction is best-effort and gated by both free bytes and free-page ratio; a busy or failed `VACUUM` must never undo logical retention.
- Artifact retention and all six managed research writers must share one canonical-database lease across the whole file/SQLite boundary: cleanup from first catalog scan through commit, writers from the final complete action-source recheck through atomic publication. Keep the fixed 0700 `/tmp/ashare-radar-artifact-locks-<uid>` root, canonical non-link/single-link database identity, sidecar/parent rechecks, and no cross-database nesting. Same-UID filesystem authority is trusted; do not claim the lease resists a malicious process running as that UID.
- A managed artifact batch is all-or-nothing. It may narrow but not widen a parent batch, must not mix managed/external targets, journals only newly linked `(path, size, sha256)` identities, and must stream-verify/unlink every exact journal item after failure while preserving pre-existing immutable files. Large probability retention verification remains bounded-memory and must preserve both digest scopes, nested generation-time removal, singleton fallback, long sorted unique manifests and scalar run-ID conservation. Project docs fallback applies only to canonical `data/ashare_radar.sqlite3`; custom databases are isolated.
- Runtime backup is database-only. Restore must acquire the target artifact lease before replacement and fail closed while any applicable managed side directory, fixed future summary, or canonical-project fallback is populated. Do not weaken this into a warning or silently relabel artifacts after a database-only restore; database plus artifacts require a separately audited set restore.
- A full-market terminal transition must update its linked task in the same transaction, and retry validation/copy must use one repository plan. Structured degradation booleans/reasons, not localized tags, govern terminal and retry behavior. Every run persists `mode`, `quote_date`, and completed-daily-K `data_date`; retries cannot cross any of those boundaries.
- Mode-aware `latest`, `latest-published`, and history remain optional filters; omission must retain global compatibility. Browser start mode and browse mode are one selection, but the globally active task remains a separate labelled identity. Historical result/filter/export/discovery actions must never silently fall back to another run.
- A full-market ranking must not publish unless ALL/SH/SZ/BJ coverage, timestamp parseability, snapshot-span, and systemic-staleness gates pass. Whole-run timeout or temporal-contract drift must preserve pending rows for retry rather than manufacture misses. An intraday run that reaches 15:15 before completion must not be published as an official or same-session result; a `preopen` run that reaches 09:15 or ceases to be on a trading day must not be published.
- A published full-market graph is sealed by `market-scan-snapshot-digest-v2` over every public persisted run/result field and protected by exactly five run/result immutability triggers. Publication audit times and every child audit time must precede sealing. A field addition, migration, retry, backup or query change may not shrink this commitment or mutate a sealed graph.
- `publication` and `legacy_backfill` are not interchangeable, and publication origin alone is not action authority. The shared action-source helper must require the original seal, exact full-market scope, diagnostics v1 with no blockers, exactly one info `score_distribution.pass`, no score-distribution blocker/source-warning conflict and no other passed score gate. Discovery queue/apply/rank, delta/screen alert, strategy execution/simulation/evidence/automation, probability outbox/managed publication, future range, degraded retry-copy and TOP100 must use it at their actual boundary; two-snapshot actions guard both. V6 may retry through a complete fresh-universe recomputation without preserving source rows. Strategy execution `verified` means binding consistency; an orphan remains `legacy_unverified`, and neither state may manufacture action authority. Read-only browse/audit remains visible and must not imply authorization.
- Probability capture outbox retention pins only `pending` and `processing`. Reconciliation/claim converts a legacy or otherwise action-ineligible live row to terminal `skipped`; terminal `succeeded`/`skipped` status alone does not pin the run. Outbox is authoritative: `succeeded` requires its archive digest to equal source-v3 `run_binding.source_integrity_digest`, and any calibrated artifact must bind the same digest. Only a separately verified supported artifact file supplies the post-publication pin.
- Preserve the narrow outcome-v1 semantic-drift quarantine. Only the exact, fully verified historical degraded rule-profile shape may raise typed `ProbabilityOutcomeSemanticDriftError`; verify every sibling, quality summary, limitation, envelope and digest before classifying it, and carry exact run/source/digest/as-of/generated identity into the catalog entry. Maintenance must retain that typed entry across stable catalog refresh, reject equal-order digest conflict, and treat it as terminal only when source-bound and not older than the valid outcome: due 0, skipped +1, no K-line read, no outcome/fit publication, including after restart. A strictly newer valid outcome must recover normal maintenance. Source-research keeps the artifact immutable, excludes its run plus cumulative dependent fits, and exposes `legacy_semantic_drift_excluded` with selection false/source-only unfitted state. Mechanical or digest failure and every ordinary replay mismatch remain hard failures; never broaden the recognizer, silently accept the outcome, delete it, or re-sign it.
- Current full-market PIT evidence is v4 and must bind all 61 bar `as_of` values plus `quote event <= quote_observed_at <= decision` and bar-date/observation/decision order. V1/v2/v3 is audit-only. Current source-v3/coverage-v2 intake reconstructs population/eligible/success/missing/skipped for ALL/SH/SZ/BJ, verifies immutable `full-market-score-v5` plus canonical publication receipt, and replays every score-details/outer-score identity; source v1/v2 and score v4 remain read-only compatibility evidence.
- Breadth and evaluation may audit either digest-valid seal origin, but delta and screen-alert comparison require current and previous snapshots to pass the complete shared action-source gate. The strict screening spec, funnel/match/reason semantics, SQL/compiler projection, Excel metadata and canonical response digests must move together.
- Full-market/discovery/strategy response paths remain `no-store`, and integrity failures remain non-disclosing generic `409` responses. The cache header never substitutes for snapshot/artifact verification.
- Market-scan score/rank replay must use only persisted canonical details. Mode-specific quote/K-line rules belong in the rule fingerprint; a changed or unknown algorithm/specification/hash is a hard incompatibility, and retry must never reuse a success across a changed date or mode contract. Automatic scheduling, probability-source capture, and future-range research must retain their explicit `official`-only gates rather than treating `preopen` as an alias for a completed official session.
- Frozen row evidence and the scoring-detail export must use persisted `score_details`/provenance only. Expanding evidence costs zero requests, and opening current analysis must retain a visible current-versus-historical warning.
- Result paging, XLSX export, and UI-editable discovery plans share one repeated-market/industry, six-range, three-sort normalization. SQL stays parameterized, duplicate sorts/range inversions fail, and bulk enqueue stays within 100 symbols while preserving source run and preset revision.
- Discovery SQL may filter or sort only persisted allowlisted result columns. Queue provenance is a snapshot and must remain meaningful after scan-history retention; adjacent action-capable rank comparison must select only the prior shared-action-gate-eligible run with the same mode/exact scope/rule and must not skip an incompatible intervening batch.
- Scan observability must keep wall duration distinct from accumulated concurrent work, close the active stage at terminal transition, and withhold ETA until the sample floor. Cache prefetch may remove SQLite setup only; it must not increase provider concurrency, accept incomplete K-lines, remove provider-response requirements, or weaken publication/time budgets.
- Offline market-scan evaluation opens the source database read-only, starts from immutable published ranks, and may use only later complete persisted sessions. Every cohort honors its sample floor; no evaluation path may invoke production scoring, mutate weights, or overwrite an old rule version.
- Authoritative stock-pool coverage and persistence must use one canonical normalized set. Replacement is all-or-nothing and verifies its written count before commit.
- The market-scan browser controller must never own more than one polling timer. A missing run or repeated failure recovers through `latest`, a newly discovered run clears stale pagination/results, online recovery is immediate, response contracts are validated before state assignment, and one live region owns status announcements.
- Keep browser polling identity strictly non-authorizing. It may read only run headers in one query-only snapshot and must bind even empty slots to database file/schema identity. Do not add result SQL, snapshot receipts, probability state, action claims, or a reusable/global trust cache. Active `updated_at` is excluded to prevent per-batch churn; ID/status/terminal seal changes remain bound. Unchanged fingerprints make zero `latest`, `latest-published`, results, or probability calls. Initial/forced/changed identities require identity-before, both trusted selectors, trusted-published results when visible, then identity-after; commit only a stable pair and retry at most twice. All user-query, mode, history, surface, visibility, online, and mutation transitions must invalidate the same generation. A result-only mutation is detected at the next full trust boundary or the scheduler's independent five-minute audit, never authorized by the lightweight token.
- Keep the failed-fingerprint circuit exact. Contract/integrity/HTTP failures and dispatched timeouts suppress ordinary trusted retries for the unchanged fingerprint; only a fingerprint change or explicit force reopens it. The controlled market-scan admission-busy `503` is transient and must retain its exact detail, `Retry-After`, and no-store headers through the global sensitive-error handler. Do not broaden that exception to arbitrary `503` responses.
- Keep `MarketScanHeavyReadAdmission` container-owned and non-authorizing. Its capacity-one slot is shared only by the four ordinary dashboard snapshot reads, admits nonblockingly, never queues or reuses a result, and keeps a strong worker-task reference after caller cancellation. Slot release belongs exclusively to the worker `finally`; start/track/close share one guard, and `aclose()` must shield its drain and complete before runtime or storage shutdown. Tests must retain the cancel+GC, late-exception, busy-across-four-routes, fresh-next-read, real-handler, and shutdown-order counterexamples.
- Keep progress coverage parity exact across Python and JavaScript: `success / (total - skipped)`, zero for an empty eligible denominator. Legacy normalization is read-only and limited to frozen pre-canonical-receipt evidence such as run `#85`; never weaken current persistence or accept an arbitrary mismatched percentage.
- Import preview claims are single-use and expire; path, mode, exact bundle, and current user-data state must still match at commit. Claim consumption, verified backup creation, and transactional import stay serialized under the shared local-data lock, and backup failure must occur before mutation.
- Alert-event pagination must use event `id` as the authoritative order/cursor. A legacy `after_created_at` value may be accepted only alongside `after_id` and must not filter or reorder events.
- Notification pagination must fully drain or leave the persisted cursor unchanged. Delivery advances the cursor only through the successful prefix; a failed trigger and every later event remain eligible for ordered retry, while a later successful poll clears the visible delivery warning. Disabling must persist the preference, invalidate polling, and clear the cursor; re-enabling must establish a new baseline so disabled-period events are never backfilled.
- Advice-review history must retain every revision and attempt while current detail projects only the greatest `as_of`, attempt, trusted `evaluated_at`, then row ID for the current revision. Plan update/archive/evaluate and paper freeze must compare the expected revision; archive sets a tombstone without deleting its plan ledger, results, or source advice. Stale evaluation/history work must not restore archived UI state. UI-selected review and scan cutoffs remain non-future and never admit later daily bars; custom scans keep the fixed allowlist and 50-symbol cap.
- Every active plan read must verify canonical ledger JSON/digest and exact current projection equality. Every trusted evaluation read must recompute the plan/input/result bindings and require the source-window digest to remain sealed inside the input binding; evidence v2 binds attempt and the server audit time, while v1 cannot become metric-bearing current evidence. A bad row is unavailable evidence, never a reason to mutate the audit row. Summary count conservation and favorable-rate denominator must be derived only from the same verified current-revision projection.
- Paper-strategy creation must repeat the plan revision/digest/current-ledger CAS inside `BEGIN IMMEDIATE` and insert only values read in that transaction; later strategy reads and run admission rebind frozen fields to the immutable revision. Simulation admits only complete fixed-session PIT/execution evidence, fingerprints every execution/suspension/corporate-action field and effective configuration, never fills or revalues from a suspended carry row, keeps blocked exits latched, and appends a new run/results/trades/equity/events evidence set instead of rewriting an older run. Dashboard/export reads must recompute the complete output digest v3, reject any child outside the same run's strategy-result/symbol membership or declared-count conservation, and derive performance from the selected run's frozen initial cash before returning it; account principal cannot change once either a strategy or run exists.
- User-data import must reject a plan without its revision ledger, a stale plan rollback, a non-canonical/digest-mismatched revision, a conflicting immutable ledger row, and a result/paper row bound to another plan/advice/revision. Surrogate remapping must rewrite embedded advice identity and every dependent plan digest, then recompute target-side paper-run output digests only for a complete semantic-preserving graph remap; pure surrogate-ID remaps remain output-v3 stable. Partial merge must reject account changes after any Paper history and semantic changes to a strategy already referenced by a run—it may not re-sign changed history. Runtime backup/restore verification must run both `integrity_check` and `foreign_key_check`.
- Historical watchlist-scan output is read-only evidence with visible `as_of`, rule, conditions, and metrics. Current analysis requires a separate explicit action and must not overwrite that historical context in place.
- Active research refresh must stay bounded, non-LLM, after-close, and per-symbol isolated. Only current quality/contract-valid snapshots may be stored; mark-viewed must use the displayed advice watermark. Automatic structured review evidence must survive custom evidence, and due evaluation ordering must not starve short horizons behind a large long-horizon backlog.
- Cache diagnostics must evaluate stock-pool and plate freshness as first-class domains. Non-empty metadata without a usable timestamp remains a missing-freshness condition, while an empty cache remains an explicit availability problem.
- LLM output must pass shape, authoritative-binding, action, conclusion, and numeric-grounding checks before replacing the rule answer. The first validation failure may cause one correction request only, and both attempts share one total timeout; request/timeout failure or a failed correction must use the rule answer. Correction prompts must not echo the previous raw model output or expand the documented remote-data boundary.
- Mobile source/visual/focus order and ARIA states must stay synchronized. Watchlist, alert, and note writes use independent request scopes; only stale UI/readback tails may be cancelled or ignored. Queue metadata changes and mark-viewed actions are local SQLite writes and must not fetch a quote.
- Provider adapters must not turn malformed critical prices into zero-valued quotes or K-lines; Tencent quotes must also keep code/name/timestamp and open/current-vs-high/low containment valid.
- Research reports, rule matches, minute plans, and market-regime outputs must not serialize `nan` or `inf`; invalid feature snapshot fields, forward returns, target K-lines, Alpha verdict context, risk/reward ratios, rule evidence inputs, and intraday zones should stay pending or use bounded conservative fallbacks. Leadership concept evidence and market-regime evidence must drop blank names before stable sorting/text assembly, and doing-T validation must not be worded as a new-position recommendation.

## 6. Known Non-Blocking Issues

- Some external provider adapters use public endpoints that can change without notice.
- The recorded full-market scan median is provider-bound: safe cache prefetch materially improves the local cold path but yields an estimated sub-1% end-to-end ceiling. A new real scan is needed to populate the newly added per-stage fields before provider-specific attribution can be refined.
- Existing persisted runs may not yet provide enough later complete sessions for 10/20-day or narrow market/regime/quality cohorts; those results remain `insufficient_data` and are not evidence of predictive power.
- Initial SLO targets are engineering hypotheses with no burn-rate alerting or enforced error-budget policy; representative production-like local samples are needed before tuning them.
- CycloneDX SBOM generation and immutable action pins improve inventory and CI integrity, but release signing and SLSA provenance generation/verification are not implemented.
