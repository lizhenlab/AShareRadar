# Test Plan and Test Report

## 1. Test Objectives

- Verify stock lookup, quote/K-line acquisition, explicit daily adjustment provenance, analysis, research, and transparent degradation.
- Verify `stock-workbench-v2` is a single requested-symbol/observed-symbol/time-bound `interactive_shadow` cohort, binds every research child and strategy card owner/`updated_at`, rejects cross-panel, direct-child-route or stale-cache identity, uses completed-session inputs, excludes mismatched optional evidence, and never persists formal advice during an interactive read; verify concept membership is fresh non-static cache-only, miss/stale/read error/cancellation starts no provider worker or write and contributes no score, and only the after-15:15 active queue may publish from a 14:55:00–15:00:00 official quote and at least 60 valid bars.
- Verify unavailable volume, structure/chip, ATR, valuation, order-pressure, fund-flow, risk and calibration inputs are typed and excluded rather than converted into positive/current factor, target/stop or DOM evidence, and historical factor calibration fails closed unless its full continuous exchange-session/PIT/execution/corporate-action contract is present.
- Verify individual D+2/D+3/D+4 probability keeps the fixed no-shift D+1 official daily-K open proxy to D+2/3/4-close one/two/three-session targets, labels declared-cost daily-bar proxy net return without claiming a guaranteed fill, preserves independent typed horizons and strict artifact-child-to-public-status plus report-level selection gates, renders honest null/interval states, owns A-B-A request freshness, and has zero effect on existing scores, advice, ranks, screening, or automation. Verify assessment v4 exact source-intake identity, current source-v3/snapshot-v3 time/coverage/canonical-receipt/current-v5/per-row replay, unchanged 288 overall and 284/286/288 per-horizon authorization gates, and exact source-v2/score-v4 audit compatibility. Until locator-bound source replay exists, the public projection remains count 0, signal date null, selection false and all values/intervals null.
- Verify `/api/analyze`, `/api/stock/workbench`, `/api/stock/upside-probability`, `/api/reviews...`, advice-history/timeline, and `/api/paper-trading...` attach `no-store` on every response path and replace sensitive dependency, transport, response-validation or other 5xx details with one generic failure.
- Verify advice-review plans use canonical append-only A-B-A revision ledgers, revision-CAS update/evaluate/archive/paper freeze, append-only idempotent evaluation attempts, `advice-review-evidence.v2` binding of attempt/server audit time, latest-as-of/current-attempt current-revision projection, digest-bound PIT/source/result evidence, conservative legacy-v1/tamper reads, strict status/time/window/metric invariants, and summary count/denominator conservation.
- Verify review/paper evidence follows the 15:15 boundary and trusted fixed-session calendar; rejects future/cross-contract/conflicting/malformed/missing evidence; treats suspension as calendar coverage but never a price touch, fill, carried valuation update, or cleared pending exit; and preserves T+1, effective rule/cost, benchmark, immutable-run, Research Shadow and no-broker semantics.
- Verify local SQLite state, atomic legacy review-schema compatibility, current/custom historical scans, preview-claimed digest-aware review/paper imports including target-side paper-run output-digest recomputation after semantic-preserving identity remap and rejection of partial immutable-history rewrites, runtime backup integrity plus foreign-key protection, scheduler, and diagnostics.
- Verify browser request freshness, code/name autocomplete, exact chart inspection, local activity, review/scan reachability, notification retry, accessibility state, persistence isolation, and SSE behavior.
- Verify official-only future-range research binds frozen D evidence to fixed D+1/D+2/D+3 exchange sessions; separates HLC3/path/MFE/MAE diagnostics from T+1 executable net/net-excess outcomes; fails closed on missing, immature, suspended, conflicting, corrupt, or underpowered evidence; and keeps API, UI, XLSX, artifact replay, probability semantics, and production ranks aligned.
- Verify `ScreenSpecV2` is one strict, deterministic contract across result queries, saved-plan application, screening evaluation, screen-alert membership and XLSX metadata; frozen breadth/funnel/match/near-miss/delta evidence stays digest-bound, null-safe and provider-free; five-column/scalar projections plus at-most-300 selected-row hydration remain full-hydration equivalent; and probability remains an explicitly unsupported condition in this contract.
- Verify the retained v5.5 report remains a dated historical `full-market-score-v4` baseline and cannot authorize a current v5 execution. Verify its direct no-volume v5.4 residual path, bounded `[-6,+6]` interaction, replay evidence, PBO/DSR `not_computed`, evaluation gates and `remain-shadow` decision remain intact; a current v5 execution must be contract-incompatible until a separate v5 evaluation artifact exists.
- Verify the read-only full-market strategy-template catalog remains scoped to the full-market workspace, exposes exactly 14 globally unique and sorted templates in a 6 draft / 3 Shadow / 5 unavailable partition, preserves compiler-complete drafts, and fails closed on malformed content. Its frozen `full-market-score-v4` field is a historical dated identity; loading it cannot mutate current v5, save, scan, rerank or trade.
- Verify canonical official full-market probability sources are captured through the typed authoritative transactional outbox and current source-v3/coverage-v2 contract; verify pending/processing/succeeded/skipped semantics, three-way outbox/source/result digest equality, bounded 60-attempt same-run polling, ALL/SH/SZ/BJ population/eligible/success/missing/skipped conservation, exact v5 write plus v4/v5 read compatibility, current-v3 latest-completed-official-session binding across weekend/holiday capture, pre-close/stale/future/calendar-outage rejection, frozen v1/v2 same-date behavior, and unchanged long-sample/OOS/joint/deployment gates. Historical OHLCV replay remains read-only, bounded, non-official and isolated.
- Verify current `full-market-score-v5` continuous trend contributes exactly `[-4,+4]` to base, raw equals base, and display rounding is non-semantic; historical v4 is read-only. Verify immutable exact rule registration, repository-owned `publication.canonical_replay.v1` / `market-scan-publication-replay-v1:<sha256>`, exactly two replayable current official skip types with all other exclusions `missing`, and request-local verified reads with exactly one full hash and one canonical replay per request. Verify frozen run `#82` remains unmodified/action-ineligible and only a new official v6/v5 run can enter capture and sample accumulation.
- Verify idle market-scan UI polling uses a token-only, query-only, non-authorizing combined identity: 1,000 unchanged repository reads issue no result SQL or full verifier, 1,000 unchanged browser ticks and 100 coalesced ticks issue no trusted stage, and only an initial/forced/stable identity change runs both existing trusted selectors before reading the trusted published ID. Verify before/trusted/after bounded retry, active-batch `updated_at` stability with status/terminal sensitivity, mode/database/schema/null-slot identity, selection parity, result/header tamper boundaries, historical/surface/query cancellation, one-timer liveness, serial selector failure, and page/probability reset only when the trusted publication binding changes.
- Verify the configured full-market keep window strictly bounds unreferenced published graphs while every genuine database or supported file-artifact reference pins its exact graph; verify every noncandidate retry child and directly protected candidate roots a parent closure across candidate/noncandidate rows, preserving all reachable candidate ancestors for newest-window, `queued`, `running`, `cancelling` and referenced lineages while an unreferenced graph still converges. Verify the closed catalog, strictly empty-primary individual fallback, fixed future-summary schema/count/item-digest/offline-replay contract, bounded deep verification, single conservative oversized legacy filename-pin exception, sparse fit membership, non-pinning outward task ownership, and transaction-stable fail-closed fingerprints; verify sealed deletion requires an explicit write transaction and valid v2 digest, and complete result/run deletion restores all five SQLite triggers or rolls back atomically. Verify the digest is never treated as an archive.
- Verify Python 3.12 plus Node 22/24 runtime declarations, host-timezone-independent market/audit/duration semantics, backward-compatible SQLite migration, dependency direction, composition-root service ownership, source/target/total private-dependency ratchets, disposable browser lifecycle ownership, configuration boundaries, exception/cancellation safety, and ratcheted typing.
- Verify liveness/readiness separation, bounded SQLite admission checks, low-cardinality reliability aggregation, explicit SLI/SLO sample floors, and optional isolated provider-canary contracts.
- Verify process-environment-only LLM configuration, provider-free locked security tooling, locked dependency audits, current/history secret scanning, reproducible CycloneDX SBOMs, immutable GitHub Action pins, generated-document drift, and maintainability gates.

## 2. Test Commands

```bash
export PROJECT_ROOT="${PROJECT_ROOT:-$HOME/AShareRadar}"
source "$PROJECT_ROOT/.venv/bin/activate"
export PYTHON="$PROJECT_ROOT/.venv/bin/python"
export PYTHONNOUSERSITE=1
$PYTHON -m pip install --require-hashes -r requirements-dev-lock.txt
$PYTHON -m pip check
npm ci
$PYTHON tools/runtime_contract.py
$PYTHON -m ruff check app tests tools
$PYTHON -m mypy
npm run check:js
$PYTHON tools/api_inventory.py --check
$PYTHON tools/architecture_inventory.py --check
$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing
npx --no-install playwright install chromium firefox webkit
npm run test:e2e
```

The development lock contains runtime and engineering dependencies. Branch coverage fails below 90%. Tests run with Python 3.12, `PYTHONNOUSERSITE=1`, temporary SQLite files, and fake providers/clients; the automated suite must not require credentials, persistent runtime data, live providers, or outbound network access. `tools/provider_canary.py` is a separate optional live diagnostic and is not part of required pull-request CI.

The Security workflow additionally runs both hashed Python lock audits, `npm audit --audit-level=high`, checksum-verified Gitleaks over the current tree and complete history with fully redacted output, and two independent SBOM generations followed by a recursive diff. Focused engineering-contract regression is available without a network:

```bash
$PYTHON -m pytest -q -p no:cacheprovider \
  tests/test_architecture_boundaries.py \
  tests/test_clock_modules.py \
  tests/test_exception_safety.py \
  tests/test_provider_canary.py \
  tests/test_reliability_modules.py \
  tests/test_supply_chain.py \
  tests/test_typing_contract.py
```

Run the live canary only after required checks pass and only when network/provider evidence is wanted:

```bash
$PYTHON tools/provider_canary.py
```

## 3. Current Automated Coverage

The current test suite is split by domain:

- `tests/test_active_research_review_backend.py`: bounded after-15:15 watchlist refresh, inclusive 14:55:00–15:00:00 official quote-event gate, at least 60 valid completed bars, per-symbol failure isolation, market-date/K-line provenance, watermark persistence, and same-day data-revision handling.
- `tests/test_advice_reviews.py`: snapshot binding; canonical append-only A-B-A plan revisions; CAS update/archive; immutable evaluation attempts and identical replay; latest-as-of/current-attempt/current-revision selection; evidence-v2 attempt/evaluated-at binding; plan/input/result/source digests; legacy-v1/tamper redaction; strict evaluation cross-fields; no-lookahead/ambiguous/pending/insufficient behavior; atomic ledger migration; and automatic-evidence ownership.
- `tests/test_advice_review_window_contract.py`: fixed trading-session completeness, PIT/execution/source-digest binding, 15:15 publication boundaries, cross-contract rejection, suspension coverage without price touch, and weekend pending behavior.
- `tests/test_akshare_stock_metadata.py`: AKShare industry/list-date aliases, placeholder cleanup, and strict date normalization.
- `tests/test_analysis_research.py`
- `tests/test_analysis_signal_modules.py`
- `tests/test_architecture_boundaries.py`: lower-layer dependency direction, direct domain-model imports, application import acyclicity, and clock-adapter ownership of wall time.
- `tests/test_api_alert_routes.py`
- `tests/test_api_container_modules.py`
- `tests/test_api_data_routes.py`
- `tests/test_api_error_modules.py`: distinct non-disclosing market-snapshot versus research-artifact integrity `409` categories; sensitive individual/review/advice/paper plus full `/api/market-scans`, `/api/discovery`, and `/api/strategy-lab` no-store coverage across success, HTTP, dependency, internal and response-validation paths without changing non-sensitive FastAPI semantics.
- `tests/test_api_local_data_routes.py`: single-use preview claims, file/mode/database-state binding, expiry/replay rejection, verified pre-import backups, stable backup failures, and backed-up manual user-history cleanup.
- `tests/test_api_monitoring_routes.py`
- `tests/test_api_notes_routes.py`
- `tests/test_api_paper_trading_routes.py`: paper-account, exact plan revision/digest freeze, validation, pending-strategy deletion, no-store/generic failure, and OpenAPI contracts.
- `tests/test_api_review_routes.py`: revision-CAS review-plan archive success/not-found/conflict, no-store/generic failure, and OpenAPI response schema coverage.
- `tests/test_api_security_modules.py`
- `tests/test_api_strategy_lab_routes.py`: strict StrategySpec/parse/compile/execution/evidence/schedule/simulation HTTP contracts, pagination limits, no-store reads, and response schemas.
- `tests/test_api_stock_routes.py`
- `tests/test_api_watchlist_research_queue.py`
- `tests/test_api_watchlist_routes.py`
- `tests/test_app_lifecycle_integration.py`: startup/cleanup failure aggregation, separate container ownership, readiness, and shutdown ordering that drains a caller-cancelled admitted market-scan verifier before runtime, workbench, and DataHub resources close.
- `tests/test_artifact_io.py`: canonical bytes/digests, bounded regular-file reads, symlink rejection, and atomic content-addressed publication primitives shared by research artifacts.
- `tests/test_audit_epoch_repositories.py`: microsecond-preserving audit-time ordering for quote snapshot and history upserts.
- `tests/test_cache_migration_guard.py`: explicit legacy timezone binding, runtime-leader exclusion, and migration disk-space preflight.
- `tests/test_cache_freshness_modules.py`
- `tests/test_cache_stats_modules.py`
- `tests/test_chart_marks_modules.py`
- `tests/test_clock_modules.py`: Shanghai market time, UTC audit serialization/parsing, monotonic/performance adapters, mixed timestamp compatibility, and legacy audit-column migration without market-field rewriting.
- `tests/test_config_modules.py`: environment parsing/validation and exact `ASHARE_RADAR_*` operations-document coverage.
- `tests/test_container_settings_lifecycle.py`
- `tests/test_data_quality_modules.py`
- `tests/test_data_sources.py`: provider adapters and fallback ordering, including AKShare daily fallback through Eastmoney and final Sina `qfq` rows, plus bounded single-page Eastmoney industry plates with redirects disabled, exact HTTP `200`, body/deadline/row-total validation and no AKShare SDK pagination.
- `tests/test_datahub_cache_modules.py`
- `tests/test_datahub_klines_modules.py`: daily/minute cache and provider contracts, including the distinction between stock coverage misses and a typed temporarily unavailable daily provider chain.
- `tests/test_datahub_metadata_modules.py`: fresh/missing/stale/static-only concept-cache reads, SQL source exclusion before ordering/limit including whitespace-bearing legacy rows, provider-free workbench degradation, 100 concurrent/cancelled reads with zero provider/runtime/write side effects, and explicit refresh compatibility outside the ordinary interactive GET path.
- `tests/test_datahub_metadata_structure.py`: metadata facade compatibility, dependency acyclicity, module-size limits, and coverage/persistence use of the same normalized stock-pool set.
- `tests/test_datahub_orderbook_modules.py`
- `tests/test_datahub_quotes_modules.py`
- `tests/test_datahub_runtime_modules.py`: request-key sharing/admission/orphan isolation, provider-chain ready/temporary/permanent state and retry hints, bounded daemon executor ownership, queued-work cancellation, idempotent and cancellation-safe deferred close, active-worker/provider-client ordering, automatic post-quiescence cleanup, and stuck-SDK subprocess exit.
- `tests/test_datahub_source_plan_modules.py`
- `tests/test_datahub_status_modules.py`
- `tests/test_datahub_status_service_modules.py`
- `tests/test_db_mappers.py`
- `tests/test_discovery_api.py`: discovery preset, leaderboard, rank-change, and research-queue HTTP contracts.
- `tests/test_discovery_portability.py`: discovery preset export/import compatibility and transactional conflict behavior.
- `tests/test_discovery_presets.py`: versioned preset CRUD, optimistic concurrency, filtering, sorting, and queue provenance.
- `tests/test_discovery_rank_changes.py`: comparable-run selection and deterministic new/up/down/unchanged/exit rank movements.
- `tests/test_exception_safety.py`: reviewed `BaseException` ownership boundaries, cancellation propagation/terminal observers, provider sanitizer convergence, and static secret-output checks.
- `tests/test_exchange_calendar_contract.py`: bundled exchange-calendar manifest parsing, exact count/order/uniqueness/min/max consistency, attested-session lookup, and unreadable/inconsistent fail-closed behavior used by public probability signal/cutoff validation.
- `tests/test_fallback_logging.py`: allowlisted SQLite persistence-failure categories, stderr fallback visibility, and secret/raw-error suppression.
- `tests/test_financial_health_modules.py`
- `tests/test_financial_metrics_modules.py`
- `tests/test_frontend_advice_timeline.py`
- `tests/test_frontend_strategy_templates.py`: Python-to-JavaScript frozen-catalog parity, exact 6/3/5 state partition, strict unknown/status/digest/duplicate-ID/named-profile-weight-mismatch rejection, escaped server text, and disabled non-loadable template actions.
- `tests/test_frontend_audit_time.py`: host-timezone-independent parsing of fixed UTC, explicit offsets, and legacy Shanghai-naive audit timestamps, plus use by scan, alert, and diagnostics views.
- `tests/test_frontend_api_format_workbench.py`: fetch parsing for 204/205, zero-length/empty success payloads, structured errors, and workbench formatting.
- `tests/test_frontend_app_lifecycle.py`: explicit browser lifecycle dependencies, hidden/visible transitions, individual-probability abort forwarding, online-recovery de-duplication, page-hide cancellation, and idempotent listener disposal.
- `tests/test_frontend_app_flow.py`: strict `stock-workbench-v2` request/quote/cohort/panel identity and time validation before render, core-workbench-first cold-load order, stock/session orchestration, immediate advice-timeline loading ownership, A-B-A stale response rejection, SSE, persistence, companion request guards, and full browser-state/SSE refresh after user-data import.
- `tests/test_frontend_workbench_contracts.py`: raw v2 workbench requested/observed/quote/all-child/strategy-card/local-row identity, every research `updated_at`, Shanghai signal-date, decision/quote/generated-time and exact daily-cutoff rejection before render, plus proof that generic JSON fixtures never silently upgrade a legacy workbench and only the explicit compatibility helper may do so.
- `tests/test_frontend_chart_inspector.py`: immutable daily/minute inspection snapshots, plot-boundary hit testing, CSS-to-canvas pointer mapping, keyboard traversal, redraw clamping, and listener cleanup.
- `tests/test_frontend_chart_context.py`: visible-window clipping, moving-average warm-up context, stable mark limits, and edge-aware non-overlapping mark labels.
- `tests/test_frontend_chart_workspace.py`: daily/minute chart controls, request budgets, stale safety, unavailable audit rows, and explicit unavailable handling for 204/empty/null or mismatched minute responses.
- `tests/test_frontend_diagnostics.py`
- `tests/test_frontend_discovery.py`: saved-filter lifecycle, leaderboard paging, queue provenance, rank comparison, stale-response ownership, and escaped rendering.
- `tests/test_frontend_individual_probability.py`: strict Python-compatible D+2/D+3/D+4 response validation, mixed independently gated horizons, historical-diagnostic versus current-probability wording, finite ordered interval/null rendering, A-B-A abort ownership, local retry degradation, accessibility and mobile layout without score/advice/rank effects.
- `tests/test_frontend_local_activity_state.py`: notes/alert-event source synchronization, sanitized unavailable states, and stale/aborted read ownership.
- `tests/test_frontend_local_data.py`: latest file-read/import-preview/commit/cleanup-preview ownership, stale file/mode invalidation, server preview-token authority, rollback-backup feedback, portable download cleanup, and storage diagnostic rendering.
- `tests/test_frontend_local_data_security.py`: guarded POST semantics for local user-data export.
- `tests/test_frontend_market_scan_executable_shadow.py`: zero-call activation, explicit-submit-only request ownership, strict exact-key/nested-state/digest-compatible contracts, null preservation, production-rank/Shadow-order rendering, limitation copy, abort/stale/local-error behavior, accessibility and mobile overflow bounds.
- `tests/test_frontend_market_scan_future_range.py`: lazy independent-panel rendering, D+1/D+2/D+3 and Top-N switching, HLC3 non-VWAP language, fixed-session/null states, separate exploratory MFE/MAE and executable net/net-excess cards/details, metric/group/probability projections, exact-symbol paging, accessibility state, and no fake zero/50% evidence.
- `tests/test_frontend_market_scan_screening.py`: lazy trusted-screening workbench wiring, strict breadth/evaluation/delta/alert digests, null preservation, five accessible column views, and explicit unsupported-probability rejection.
- `tests/test_frontend_market_scan_reliability.py`: compact whole-run progress, aggregated failure examples, capability labels, and responsive leaderboard contracts.
- `tests/test_frontend_notes_alerts_requests.py`: independent note/alert write ownership, local successful-write reconciliation, stale readback protection, explicit readback degradation, and row-scoped failure feedback.
- `tests/test_frontend_notifications.py`: permission timing, first-poll baseline, keyset page draining, non-advancing-page safety, trigger de-duplication, burst summaries, and failed-delivery retry without skips.
- `tests/test_frontend_paper_trading.py`: strict dashboard/run/compare response identities and cross-fields, output-digest shape, child strategy/symbol membership and count conservation before state/DOM mutation, escaped strategy rendering, account/equity views, exact revision/digest frozen-plan writes, immutable run refresh, generic 5xx state, no-broker copy, and static workspace reachability.
- `tests/test_frontend_research_activity.py`: three-source normalization/order, limits and filters, escaped output, and distinct loading/empty/partial-or-total-unavailable states.
- `tests/test_frontend_research_panels.py`: typed unavailable volume/structure/valuation/order-pressure/fund-flow/risk/calibration rendering, canonical null/direction-free states, MA20 redraw guards, and removal of stale numeric/directional DOM evidence.
- `tests/test_frontend_review_scan.py`: strict plan/evaluation/due/batch/summary response contracts, digest/time/count/metric/status cross-fields, Research Shadow copy, independently settled global summary/due/list projections, escaped rendering, exact revision-CAS archive/update/evaluate/paper writes, snapshot/rendered-symbol ownership, server-current time for today, Shanghai end-of-day history, lazy/retryable attempt ownership, current/custom scan controls, strict prices/symbols, whitelisted conditions, and stale-response rejection.
- `tests/test_frontend_stock_search.py`: 250 ms debounce, abort/stale protection, bounded LRU behavior, payload validation, explicit states, keyboard selection, and destruction cleanup.
- `tests/test_frontend_stock_search_history.py`: recent-search persistence, de-duplication and size bounds, accessible history controls, and click-to-refresh stock research navigation.
- `tests/test_frontend_watchlist_requests.py`
- `tests/test_frontend_workspace_preferences.py`: persisted workspace selection, unsupported-value fallback, and storage failure tolerance.
- `tests/test_futu_provider_modules.py`
- `tests/test_indicator_levels_modules.py`
- `tests/test_indicator_trend_modules.py`
- `tests/test_indicator_volume_modules.py`: positive-volume-only ratios, insufficient-window typed unavailability, and compatibility neutral values that cannot become observed confirmation.
- `tests/test_individual_probability.py`: typed target/horizon/interval invariants; assessment-v4 exact estimator and full source-intake identity; current shared-action-gate-eligible source-v3/feature-v3/PIT-v4 records; `quote event <= quote_observed_at <= decision <= captured_at <= generated_at`, bar-as-of and official-session maturity; a 100-row three-market run-87-shaped Friday projection through source snapshot, assessment seal and content-addressed store; current-v3 weekend acceptance plus previous-v2 same-date/calendar-isolation closure; registered production score rule/spec plus deterministic row replay; ALL/SH/SZ/BJ population/eligibility/coverage/count envelopes and cross-date uniqueness; 288 report-level and 284/286/288 child floors, fold/OOS/bin/Brier identity/stability, exchange-signal/training-cutoff and digest/version gates; v1/v3 rejection and exact tracked v2 audit projection as 0/288 with `signal_date=null`; compact source/metric runtime-replay fail closure; non-official 279-session diagnostics; immutable store fingerprints/deep copies/conflict rejection; local-versus-baseline precedence and corrupt/symlink closure; manifest/sidecar/non-link/size/TOCTOU verified-byte in-memory SQLite boundaries; qfq/10万元/100-share-lot proxy boundaries; and null preservation without a replayable current-stock predictor.
- `tests/test_individual_probability_store_failures.py`: never-stable directory retry exhaustion, safe-fingerprint read-race translation, candidate type replacement, missing/symlink-ancestor directory rejection, missing assessment time, and current compact source-not-runtime-replayed limitation branches.
- `tests/test_individual_workflow_modules.py`
- `tests/test_joint_execution_probability_contract.py`: exact joint-v2 sample identity/session/maturity/digest contracts; all-decisions three-component estimand; official unadjusted entry/exit, effective-rule/reference-price, bilateral capacity, frozen-universe/benchmark and calibration evidence shapes; permanent observed-component/assessment-replay blockers; forced all-null probabilities; action qualification false; and rejection of caller-supplied flags, values, tamper, reseal or signal-time look-ahead.
- `tests/test_kline_contract.py`: legacy migration isolation, coexisting adjustment modes, mixed-batch rejection, and explicit provider provenance.
- `tests/test_leader_scoring_modules.py`
- `tests/test_llm_explainer.py`
- `tests/test_local_data_portability.py`: exact export allowlist; merge/replace dry runs; review-plan/revision/result/paper parent grouping; canonical revision/digest validation; stale-revision and immutable-ledger conflict rejection; collision remapping with embedded advice-ID and propagated plan-digest rewrite; stable pure-ID paper output v3 plus target-side output-digest recomputation after complete semantic-preserving identity remap; rejection/rollback of account updates after Paper history and strategy-only rewrites of immutable runs; post-write relationship checks; schema drift rejection; idempotence; and transactional rollback.
- `tests/test_local_lifecycle.py`: local persistence/migrations, proof that interactive analysis/workbench reads do not append advice history, after-close advice publication ownership, comparable advice-change unread counting, viewed-through watermark races, automatic user-history cleanup protection, and shared runtime invariants.
- `tests/test_market_overview_modules.py`
- `tests/test_market_quotes_modules.py`: quote persistence, fixed Shanghai event time, epoch ordering, equal-event fallback/completeness/fetched-at priority, and legacy UTC ISO compatibility.
- `tests/test_market_sampling_modules.py`
- `tests/test_market_scan_api.py`: asynchronous create/deduplication, lifecycle controls, no-store reads, strict non-authorizing polling-identity schema/order, four-route capacity-one heavy-read admission, real global-handler busy semantics, cancel/GC ownership, late-exception cleanup, fresh post-release verification, validation, pagination, and full filter/sort forwarding.
- `tests/test_market_scan_automation.py`: SH/SZ/BJ provider preflight, sanitized persisted diagnostics, bounded restart-safe cadence, structured retry eligibility, and automatic creation of a linked retry run.
- `tests/test_market_scan_architecture.py`: executor ownership, dependency direction, protocol boundaries, and split-module maintainability.
- `tests/test_market_scan_artifact_lease.py`: re-entrant thread and spawned-process publication/retention serialization; fixed owner-only lock-root, sidecar/database-parent identity and missing-target acquisition; canonical non-link/single-link database plus managed-path binding; trusted same-UID boundary; no cross-database nesting; shared original-publication/exact-scope/diagnostics-v1 action-source recheck; nested subset authorization; mixed/aliased target rejection; all-or-nothing multi-writer publication; exact path/size/SHA-256 streaming rollback after body/final-namespace/release failure; cleanup continuation after one rollback failure; and preservation of identical pre-existing artifacts.
- `tests/test_market_scan_execution.py`: full-universe batching, all-quote-chunk capture before K-line work, sealed-envelope persistence, row-level `quote_observed_at` scoring boundaries, bounded K-line concurrency, quote/K-line failure classification, and persistence behavior.
- `tests/test_market_scan_pipeline.py`: second-phase one-batch K-line preservation-cache prefetch overlap, cancellation propagation, speculative-task draining, and no leaked prefetch tasks after the sealed quote boundary.
- `tests/test_market_scan_query_integrity.py`: full-snapshot and page invariants, before/after cohort re-read binding around probability/future-range enrichment, TOCTOU rejection, and uniform non-disclosing integrity `409` mapping.
- `tests/test_market_scan_query_service.py`: request-local verified-read delegation; fused run/action/outbox/source/page snapshot; authoritative outbox state and three-way digest projection; action-ineligible, pending, skipped, missing-outbox and archived UI reasons; probability/future-range boundaries; and no recovery mutation from query paths.
- `tests/test_market_scan_evaluation.py`: read-only/no-look-ahead forward returns, final-run selection for duplicate cohort sessions, independent-session floors, Rank IC/deciles, board/market/quality/segment/liquidity/time strata, frozen-rank stability without future returns, T+1/cost/tradeability scenarios, paired five-day Top100 net-excess, five-session circular moving-block one-sided bootstrap, BH-FDR candidate/factor families, null underpowered inference, explicit PBO/DSR non-computation, regime/base-conservative-stress cost/10-50-100万元 capacity/stability gates, compact report projection and byte-for-byte database immutability.
- `tests/test_market_scan_delta.py`: deterministic same-mode/scope/rule frozen-run comparison, Top20/50/100 membership changes, rank/score and market/industry exposure deltas, source-evidence reason codes, explicit present-but-unrankable handling, and unavailable states without inferred exits.
- `tests/test_market_scan_screen_alert.py`: saved ScreenSpecV2 membership changes over strict same-cohort frozen runs, unrankable exit suppression, optimistic revision checks, and content-addressed idempotent event persistence.
- `tests/test_market_scan_screening.py`: strict ScreenSpecV2 compilation, Python/SQLite parity, published-full-market gates, five-column breadth summaries, scalar-only evaluation, page/near-miss-only hydration, the 300-detail ceiling, condition funnels, matched reasons, null-safe research dimensions, literal keyword wildcard handling, and deterministic digests.
- `tests/test_market_scan_probability.py`: core-v4/model-v2/feature-v3/label-v3/split-v3 1/5/20-session contracts; D+1 entry, D+2/D+6/D+21 exits and H+1 gaps/bootstrap; strict cohort isolation/fold replay and conditional-population disclosure; raw OOS/proper-score/ECE/BH-FDR/drift/session-economics authorization replay; opaque verified authorization tokens; separate deployment refit/purge/freshness artifact; joint-estimand rejection; signed bias versus adjusted interval; and current authorization/filter/deployment/prediction null closure.
- `tests/test_market_scan_probability_artifact.py`: canonical finite JSON, aware content-bound time/maturity and integrity-seal semantics, result-v4 explicit intervals, result-v2/v3 legacy adapters, strict schema/identity/status/cohort contracts, complete per-cohort artifact-set refit from full feature/label inputs, missing/duplicate/ambiguous-latest rejection, historical fold digest replay, atomic writes outside SQLite, corruption closure, and null/calibrated round trips without database mutation.
- `tests/test_market_scan_probability_capture.py`: transactional current official/FULL outbox enqueue; typed pending/processing/succeeded/skipped state; lease claim/retry/restart reconciliation; exact canonical action receipt; source-v3 archive digest persistence; action-ineligible terminal skip; sanitized last-error handling; and runtime-leader integration without publication downgrade.
- `tests/test_market_scan_probability_history.py`: verified runtime-backup source binding, WAL/SHM/journal sidecar rejection, deterministic balanced 360-session history acquisition, bounded concurrency/retries, short-history exclusion, market floors, source immutability, sanitized whole-batch provider failure closure, alias/overwrite guards, SQLite/manifest restart verification and tamper rejection, CLI handoff summary, and direct replay compatibility with at least 260 independent dates.
- `tests/test_market_scan_probability_labels.py`: label-v3 explicit-target-offset next-tradeable-open and T+1 holding-horizon-close semantics, shared round-trip costs, degraded/unavailable rule-profile failure closure, separate verified-profile `daily_bar_model_limited` evidence, locked-limit/suspension/capacity/incomplete-window handling, versioned contracts, and conflicting-bar rejection.
- `tests/test_market_scan_probability_maintenance.py`: canonical source selection; incremental valid/typed-drift catalog caching; mechanically bound drift identity and conflict closure; latest source-bound drift at or after valid outcome as restart-stable terminal due-0/skipped/no-K-line/no-outcome/no-fit state; strictly newer valid-outcome recovery; ordinary invalid-outcome hard failure; exact-date bounded K-line reads; pre-maturity and milestone publication; same-as-of idempotence; bounded missing-bar retry; directory-race failure closure; after-close scheduler audit; and 260-session threshold-gated compact sampled assessment publication/replay without a row-level probability projection.
- `tests/test_market_scan_probability_outcomes.py`: trusted fixed-session H1/H5/H20 grids, explicit not-mature and mature-unavailable records without forward shifting, compact source-digest references, semantic label replay, exact fully verified legacy degraded-profile drift classification only after sibling/quality/digest checks, ordinary mismatch/tamper hard failure, quality/coverage progress, deterministic gzip content addressing, immutable publication, corruption/duplicate rejection, strict source-outcome joins, and restart loading into probability samples/research rows.
- `tests/test_market_scan_probability_replay.py`: sidecar-free SQLite `immutable=1` input and unchanged-fingerprint enforcement, query-only historical `qfq` replay, point-in-time common OHLCV features, fixed D+1 entry and H1/H5/H20 exits, no-shift missing bars, registered costs, deterministic bounded market sampling, immutable artifact semantic replay, isolated null/calibrated fitting, forecast projection, and read-only CLI behavior.
- `tests/test_market_scan_probability_source.py`: strict source artifact-v3/snapshot-v3 with coverage-v2 population/eligible/success/missing/skipped conservation; current PIT-v4/current-score-v5 write; source-v1/v2/v3 and score-v4/v5 exact reads; stable v5 continuous-trend-to-`rank_refinement` mapping; registered rule/canonical receipt/per-row replay; canonical gzip, immutable digest/time closure; run-87-shaped Friday-to-weekend projection; pre-close, next-session stale and reversed-time rejection; and frozen source-v1/v2 same-date/calendar-isolation compatibility without upgrade or authorization.
- `tests/test_market_scan_probability_source_research.py`: canonical source/outcome/fit joins; typed legacy outcome-drift exclusion with `legacy_semantic_drift_excluded`, source-only/unfitted projection, selection false and dependent-fit removal; ordinary invalid-outcome refresh failure with last-good preservation; v3 224/232/262 split floors; compact assessment null states; scan/score double binding; non-blocking single-flight verification plus atomic last-good snapshot publication; unchanged warm-summary reuse; cold-versus-warm behavior; legacy-unbound projection; and no interactive deep read for a large irrelevant legacy artifact.
- `tests/test_market_scan_shadow_scoring.py`: deterministic input-order-independent Shadow v5 ranking, legacy v5.3/v5.4 compatibility, v5.5 schema/spec identity, unchanged direct v5.4 normalization/penalties, bounded residual/stability interaction, positive-branch crowding/capacity gates, conservative negative branch, explicit no-industry-breadth/no-peer-network/no-ML limitations, penalty-only confidence/special status, preregistered ablations, future-row exclusion, invalid/short inputs, completed-session `preopen` volume lifecycle, component/input-digest tamper rejection, and normalization/ranking replay.
- `tests/test_market_scan_snapshot_integrity.py`: v2 whole-graph digest and five sealed-graph triggers; current immutable rule registry and canonical publication receipt; request-local verified-read close/thread/transaction/DML/DDL/schema/query-only revocation; one full hash plus one fused canonical replay and no second SQL score read; exact official admission and tamper/reseal closure; scheduler audit caching; and the exclusive 5,382-row three-run CPU/wall regression bounds.
- `tests/test_market_scan_export.py`: valid XLSX generation, complete filtered snapshots, audit metadata, text/formula safety, empty exports, and published-run enforcement.
- `tests/test_market_scan_feature_windows.py`: exact 61-bar boundary, shared official/preopen `horizon+1` versus intraday `horizon` 1/5/20/60 and skip-return references, PIT-v4 identity/score/source/bar-plus-`as_of` binding, absolute `quote event <= observed <= decision`, bar-date/observation/decision order, trusted expected-session sealing with 5/20/60 coverage/gap penalty and no synthetic bars, legacy-v1/v2/v3 audit-only context closure, invalid-input rejection, and production/research/Shadow use of the common mode-aware contract.
- `tests/test_market_scan_future_range.py`: read-only fixed-session D+1/D+2/D+3 mapping, verified signal evidence and qfq overlap, HLC3/path limitations, T+1 execution costs and equal-weight net excess, suspension/no-forward-shift handling, TOP100-scope rejection, adjustment-rebase failure closure, moving-block/gap semantics, not-mature null preservation, final-run/date canonicalization, strict rule-cohort isolation, and evidence-bound OOS probability/original-calibration context.
- `tests/test_market_scan_future_range_artifact.py`: canonical finite JSON, semantic OHLC/HLC3/return/path/cost replay after resealing, integrity and duplicate-key rejection, immutable atomic publication, SQLite/hard-link target rejection, restart loading, CLI summary/artifact output, and byte-for-byte database immutability.
- `tests/test_market_scan_future_range_store.py`: strict immutable-artifact loading, directory/fingerprint cache invalidation, run identity and official eligibility, not-generated/insufficient projections, page/offset/exact-symbol filters, `include_research`, corruption failure closure, API status mapping, and future-range XLSX projection.
- `tests/test_market_scan_frontend.py`: split-module/version wiring; strict three-mode and polling-identity contracts; 1,000 unchanged/100 coalesced tick budgets; bounded before/trusted/after synchronization; trusted-selector recency; active-progress stability; stable failed-fingerprint circuit versus exact transient admission busy; eligible-denominator progress parity including frozen run `#85`; history/surface/query/mode supersession; one-timer liveness; query/probability binding reset; weekday time-based `preopen` default/request copy; independent mode history selection; bounded backoff; latest recovery; escaped rendering; ARIA state; and separate publication-blocker/passed-gate/source-warning presentation for new and legacy terminal messages.
- `tests/test_market_scan_lifecycle.py`: lifecycle release, cancel/restart recovery, terminal SQLite retry, owned post-worker recovery, and foreign-leader protection.
- `tests/test_market_scan_modes.py`: `preopen`/intraday/official Shanghai time windows, weekend/call-auction rejection, previous-completed-session `preopen` dates and close alignment, current source-v3 latest-completed-session matching across weekend/holiday plus pre-15:15/stale/future/reverse/calendar-coverage closure, separate intraday quote/K-line dates, previous-close consistency, independent rule fingerprints, and retry contract preservation.
- `tests/test_market_scan_performance.py`: legacy versus batch-prefetched cache equivalence, provider-free benchmark contract, source-database immutability, and CLI JSON output.
- `tests/test_market_scan_pressure.py`: AIMD concurrency, busy/timeout/retry-after/systemic pressure signals, wait-budget accounting, healthy recovery, per-symbol exclusions, and terminal pressure diagnostics.
- `tests/test_market_scan_repository.py`: transitions, exact quote-capture envelope, atomic task/scan writes, immutable v5 rule registration, canonical receipt generation, persisted skip revalidation, current v6 retry/TOP100 contract enforcement, historical v4 and pre-receipt `#85` progress read compatibility, current/mismatched progress rejection, stable ranks, filters, immutable snapshots, active-run uniqueness, and token-only polling identity with 1,000 query-only reads, zero result SQL/full verification, database/schema/slot parity, real active-batch stability, and fail-closed tamper boundaries.
- `tests/test_market_scan_repository_structure.py`: repository facade compatibility, dependency direction/acyclicity, production module-size limits, and the scheduler-only automatic-state mapper split that keeps the general query module within budget without granting snapshot authority.
- `tests/test_market_scan_release_boundary_coverage.py`: resealed probability/config/authorization counterexamples; exact rule/receipt binding; action-source admission; authoritative outbox lease/reconcile/terminal-skip and digest behavior; frozen snapshot invariants; strict finite canonical JSON; publication time order; and SQLite seal failure closure.
- `tests/test_market_scan_retention.py`: configured keep-window convergence for published graphs; artifact lease held from first catalog scan through commit; explicit transaction requirement; v2 seal verification; exact result/run deletion; five-trigger restoration and rollback under tamper/injected failure; dynamic and semantic database-reference families; pending/processing probability-outbox pins versus non-pinning terminal skipped/succeeded state; retained-rank, `queued`/`running`/`cancelling`, referenced and directly protected retry roots with multilevel candidate/noncandidate ancestry; non-pinning outward task ownership; exact recursive probability/future/individual run binding; fixed future-summary schema/count/item-digest/offline-replay verification; canonical-project-only empty-primary fallback ownership; legacy/current-scope bounded-memory large-probability verification with singleton, long-manifest, equal-length tamper, generation-time/scalar-ID/file-mutation closure; sparse fit-member pinning; and fail-closed unsafe/ambiguous/changed catalog handling.
- `tests/test_market_scan_retry.py`: within-run pending preservation and batch recovery, provider wait budgets, and resumable recovery plans; cross-run v6 full recomputation is covered at the repository/execution boundary.
- `tests/test_market_scan_scheduler.py`: publish-time guard, automatic-run suppression, stale-run recovery, and scheduler ownership.
- `tests/test_market_scan_scoring.py`: deterministic current v5 and historical v4 replay; exact continuous-trend `[-4,+4]` base adjustment, `raw_score=base`, display-only rounding; completed-date/mode boundaries; strict quote/liquidity inputs; and missing versus justified-skip boundaries.
- `tests/test_market_scan_skip_contract.py`: canonical current-v6 skip classification and PIT evidence, skip-PIT-only canonical date K-line `as_of` parity plus future/non-canonical closure, quote/liquidity precedence, exact new-listing/session-gap authorization, repository replay receipts, persisted-skip revalidation, tamper/reseal closure, and action-source publication gates.
- `tests/test_market_scan_trust_contract.py`: immutable full score-spec JSON/hash registration and replay; ALL/SH/SZ/BJ gates; v6 exact-count/sealed capture; capture-duration, observation/event span and systemic-stale blockers; canonical replay receipt inputs; legacy compatibility; trading-date drift; and whole-run deadlines.
- `tests/test_market_scan_terminal_recovery.py`: terminal-run recovery ownership, guarded lifecycle delegation, and no-op behavior when the active lease is not owned.
- `tests/test_market_scan_universe.py`: current-listed A-share filtering, future/unknown listing dates, new-stock boundaries, de-duplication, and precise ST-name recognition.
- `tests/test_market_scan_validation.py`: market coverage thresholds, systemic data-failure guards, `preopen` trading-day/09:15 runtime publication stop, degradation accounting, and terminal publication decisions.
- `tests/test_minute_analysis_modules.py`
- `tests/test_optional_kline_parsing_modules.py`
- `tests/test_optional_provider_concurrency.py`
- `tests/test_paper_trading.py`: deterministic no-lookahead fills, post-`as_of` isolation, complete PIT/execution/session evidence, malformed/conflicting/non-trading rows, suspended valuation and latched-exit behavior, T+1 and locked-limit lifecycles, in-transaction plan revision/digest/payload CAS plus later strategy rebind, effective-dated ST/board lots/price limits, inclusive listing-session count at the trusted calendar left boundary and fail-closed true underflow, versioned costs, trading-session open-aligned benchmark/excess metrics, complete evidence fingerprints, priority/expiry rules, immutable run persistence, frozen-initial-cash historical performance/account lock, output-digest v3 tamper/orphan/count closure, and pure-surrogate-ID stability.
- `tests/test_paper_trading_metrics.py`: pure position, equity, drawdown, return, exposure, and risk-metric edge cases extracted from the paper-trading repository.
- `tests/test_paper_trading_schema.py`: populated v1 ledger migration, cost/provenance compatibility backfill, referential integrity, and idempotent v2 schema application.
- `tests/test_provider_errors_modules.py`
- `tests/test_provider_canary.py`: SH/SZ/BJ quote plus five-row daily-K contracts, three-market stock-pool validation, independent market/stock-pool and overall timeout boundaries, cancellation/cleanup, temporary SQLite CLI wiring, sanitization, and complete/partial/failure exit codes.
- `tests/test_provider_failure_status_modules.py`
- `tests/test_provider_priority_settings.py`: environment-backed provider ordering, normalization/de-duplication, optional-source defaults, and preflight/automatic-retry setting validation.
- `tests/test_provider_registry_modules.py`
- `tests/test_provider_status_aggregation_modules.py`
- `tests/test_provider_status_repository_modules.py`
- `tests/test_provider_utils_modules.py`
- `tests/test_quote_stream_modules.py`
- `tests/test_reliability_modules.py`: UTC-hour aggregation, bounded dimensions, workbench/provider counters, scan/task exclusions, fixed windows/targets/sample floors, counter invariants, and deterministic nearest-rank percentiles.
- `tests/test_research_alpha_modules.py`
- `tests/test_research_breadth_modules.py`
- `tests/test_research_chip_modules.py`
- `tests/test_research_conclusion_change.py`
- `tests/test_research_diagnosis_modules.py`
- `tests/test_research_event_digest_modules.py`
- `tests/test_research_factor_calibration_modules.py`: continuous unique trusted-session grids, same-day PIT `qfq`/data versions, suspension/open-execution/corporate-action metadata, locked or missing entry evidence, non-overlapping samples, and `execution_evidence_unavailable` fail closure.
- `tests/test_research_factor_modules.py`: typed feature availability, unavailable volume/chip factor exclusion, calibration aggregation, and factor-report compatibility.
- `tests/test_research_factor_scoring_modules.py`: score normalization, explicit `participates_in_current_score` denominator, unavailable-factor neutrality, and historical aggregate admission.
- `tests/test_research_factor_specs_modules.py`
- `tests/test_research_factor_weight_modules.py`
- `tests/test_research_leadership_modules.py`
- `tests/test_research_peer_modules.py`
- `tests/test_research_qa_answer_modules.py`
- `tests/test_research_qa_report_modules.py`
- `tests/test_research_artifact_catalog.py`: bounded research-artifact classification, byte/count aggregation, symlink and non-regular-file handling, and preview-only retention summaries with no deletion authority.
- `tests/test_research_regime_modules.py`
- `tests/test_research_replay_modules.py`
- `tests/test_research_risk_modules.py`
- `tests/test_research_risk_reward_modules.py`: target/stop basis and availability flags, no fixed fallback levels from missing structure/ATR, withheld ratio/wait rating, finite metrics, scenario weights, and report rendering.
- `tests/test_research_t_strategy_modules.py`
- `tests/test_research_theme_modules.py`
- `tests/test_research_timeframe_modules.py`
- `tests/test_research_validation_modules.py`
- `tests/test_review_modules.py`
- `tests/test_review_paper_boundary_coverage.py`: strict review evaluation invariants, plan/evaluation ledger fail-closed reads, Paper account/run revision binding, and empty-dashboard/CAS boundary coverage.
- `tests/test_rules_alerts.py`
- `tests/test_runtime_backup.py`: snapshot hash/row/schema/`integrity_check`/`foreign_key_check` verification, tamper or broken-relationship rejection, unified/legacy guarded restore, target artifact lease including initially missing paths, pre-write database-only restore rejection for populated managed side directories/fixed summary/canonical-project fallback, custom-database fallback isolation, lease-release failure cleanup, fixed-order bounded operation leases, thread/process concurrent rotation, in-use bundle protection, set-based retention, retry/task lineage convergence, cleanup preview parity, and active/archived review-linked advice protection.
- `tests/test_runtime_coordinator.py`: repeated cross-process leadership exclusion, shared scheduler/scanner ownership, standby takeover/status, retryable partial activation, pre-leadership service guards, and delayed takeover while a non-cooperative old task remains alive.
- `tests/test_runtime_environment_modules.py`
- `tests/test_runtime_maintenance_regressions.py`: deep retained-retry ancestry preservation, unreferenced retry-lineage convergence, and threshold-gated best-effort SQLite compaction.
- `tests/test_scheduler_modules.py`: task execution/state, cancellation, degraded outcomes, bounded stop with quiescence-delayed guard release, maintenance throttling, persistence fallback, runtime-leadership integration, and cheap automatic-state query/decision wiring that never becomes snapshot authorization.
- `tests/test_scheduler_structure.py`: scheduler facade compatibility, internal dependency acyclicity, and production module-size limits.
- `tests/test_schema_compat.py`: legacy schema compatibility; atomic `market_scan_run`/result rebuild for `preopen`; atomic advice-review v4 current-revision backfill and append-only result rebuild; result-ID/retry preservation; foreign-key behavior; rollback on injected failure; index restoration; idempotent migration markers; and fresh-schema acceptance.
- `tests/test_scoring_modules.py`
- `tests/test_sina_client.py`: SH/SZ/BJ symbol mapping, bounded/rate-limited requests, safe JSON and adjustment-factor parsing without `eval`, forward-filled `qfq` calculation, strict OHLC/volume/date validation, and provider error classification.
- `tests/test_static_assets.py`
- `tests/test_strategy_automation.py`: exact-full-market version-pinned schedule claims, isolated retries, fingerprinted alert conditions, simulation-plan canonical digest plus source execution/identity seal, tamper/reseal rejection, readable broker-free plans, and archived-strategy execution/enable blocking.
- `tests/test_strategy_evidence.py`: verified execution binding plus shared original-publication/exact-scope/diagnostics action-source recheck, `legacy_backfill`/`legacy_unverified` rejection, pinned compact-v2 offline-baseline exact schema/digest, strict finite persistence/identity binding, newest-corrupt-no-fallback, reconstructed execution-result digest, candidate-gate promotion recomputation, legacy-artifact typed unavailability, no HTTP evaluator invocation, mode isolation, research boundary, execution/market coverage, Top20/50/100, rank delta, Shadow constraints/cost-capacity robustness/exposure/promotion gates, and explicit custom-strategy limitations.
- `tests/test_strategy_execution.py`: shared original-publication/exact-scope/diagnostics frozen-snapshot validation and in-transaction recheck, source digest/origin/status binding, latest exchange-session freshness/calendar/future/stale failure closure, historical decision-time replay without legacy authority, selector-independent freshness-bound fingerprints, production-rank preservation, executable equal/risk-adjusted/custom weighting, iterative non-custom constraint refill and underinvestment evidence, exact custom-symbol ownership, buy/hold hysteresis, source-provenance allowlists, A-share constraints, no-trade behavior, Pareto/counterfactual/sensitivity, pagination, and comparisons.
- `tests/test_strategy_execution_schema.py`: rollback-safe `20260813_strategy_execution_source_digest_v2` ordering, existing-source digest/origin backfill, missing-source null/null/`legacy_unverified` preservation, exact two-state safe-state validation, marker-last idempotence, source-run seal verification, append-only/source-insert trigger restoration, and no silent orphan upgrade when a run ID later reappears.
- `tests/test_market_strategy_templates.py`: strict 14-template global identity/order/state partition, 61-session available-draft formation, exact holding/rebalance and fixed-custom-weight preservation, compiler-complete required fields, recomputed canonical digests, field/period registration, proxy-limit honesty, crowding missing fields, and production-boundary invariants.
- `tests/test_strategy_lab.py`: deterministic Chinese parsing, metric/operator/period allowlists, normalized fingerprints, immutable optimistic revisions, copying, archiving, and version diffs.
- `tests/test_stock_abnormal_events.py`
- `tests/test_stock_activity_modules.py`
- `tests/test_stock_analysis_modules.py`: interactive market-phase mode selection, completed-daily-bar cutoff, pre-open/intraday volume neutrality, provider/optional-data isolation, and no formal advice persistence.
- `tests/test_stock_event_summary.py`
- `tests/test_stock_lhb_modules.py`
- `tests/test_stock_lookup_modules.py`
- `tests/test_stock_overview_modules.py`
- `tests/test_stock_pool_metadata.py`: total and SH/SZ/BJ industry/list-date completeness diagnostics without blocking price-only ranking.
- `tests/test_stock_pool_industry_enrichment.py`: one-shot BaoStock industry enrichment, source provenance, cache persistence, and non-fatal isolated capability failure.
- `tests/test_stock_rule_modules.py`
- `tests/test_stock_strategy_modules.py`
- `tests/test_symbol_modules.py`
- `tests/test_supply_chain.py`: SHA-pinned actions, disabled checkout credentials, runtime/dev Python and npm audits, redacted current/history secret scanning, reproducible Python/npm CycloneDX SBOMs, Dependabot coverage, and portable files.
- `tests/test_system_diagnostics_modules.py`
- `tests/test_tencent_provider_modules.py`: current Tencent quote behavior plus SH/SZ/BJ `newfqkline` requests, `qfqday`/unadjusted-day response handling, validation, and failure classification.
- `tests/test_tool_inventory_modules.py`: generated-document drift, test-plan completeness, machine-path guards, dependency layering, immutable action SHA pins, and Node 24 action-major guards.
- `tests/test_trading_calendar_modules.py`: trusted runtime/bundle selection and coverage, previous/next/gap derivation, inclusive exchange-session ranges, exact calendar-left-boundary session 1, true left-overflow failure, refresh/single-flight behavior, and conservative closed state outside coverage.
- `tests/test_typing_contract.py`: explicit existing mypy file scope, non-shrinking ratchet, sorted uniqueness, and prohibition of hidden type errors.
- `tests/test_uvicorn_smoke.py`: real loopback Uvicorn startup with isolated SQLite, API/static responses and cache headers, plus a deliberately held-open quote SSE connection and traceback-free `SIGINT` shutdown bounded by the test's two-second graceful-shutdown setting.
- `tests/test_valuation_modules.py`
- `tests/test_watchlist_research_queue.py`: queue validation/order, mark-viewed state, comparable changed-advice unread increments, and viewed-through watermark preservation of later changes.
- `tests/test_watchlist_scan.py`: explicit/current universes, as-of results, missing rows, versioned fixed rules, script rejection, and symbol caps.
- `tests/test_workbench_context_cache_modules.py`: symbol-bound single-flight, cross-panel identity rejection, market-phase/expected-date cohort cache invalidation, stale-entry eviction, cancellation and shutdown.
- `tests/test_workbench_pipeline_modules.py`: interactive analysis composition, requested/observed identity and time provenance, completed-bar cutoff, wrong-date/fallback breadth plus wrong-symbol/date order-book/concept exclusion, and a fail-closed cache-only concept API with no legacy provider-capable fallback.

Browser regression support is indexed separately:

- `tests/e2e/frontend-flow.spec.js`: desktop/mobile strict workbench-v2 cohort, non-persisted Shadow diagnosis, paper trading, exact chart inspection, local research activity, queue, timeline loading/ownership, stale-request, and request-budget flows.
- `tests/e2e/market-scan-flow.spec.js`: four-project full-market scan lifecycle, responsive result/history/mode isolation including `preopen`, cancellation/recovery, saved Discovery flows, and preserved navigation ownership.
- `tests/e2e/market-scan-executable-shadow.spec.js`: desktop/mobile Chromium explicit-submit-only Shadow loading, run/notional binding, production-rank versus Shadow order, risk/cost/capacity/exposure/turnover and limitation evidence, null rendering, stale/abort/local-error isolation, touch targets and local table overflow without eager requests.
- `tests/e2e/market-scan-screening.spec.js`: real-browser lazy trustworthy-screening funnel, breadth/delta evidence, filtered columns, reason codes, null states, alert idempotence, and desktop/mobile bounds.
- `tests/e2e/strategy-shadow-evidence.spec.js`: real-browser Strategy Lab Shadow v5.5 evidence-center research boundary, coverage, Top20/50/100, rank delta, constraints/exposure/gates, typed unavailable state, and no automatic-promotion control.
- `tests/e2e/strategy-template-catalog.spec.js`: real-browser full-market-only catalog activation, exact 6/3/5 state rendering, disabled non-loadable routes, mutual-exclusive draft selection, dry-run compile without save/scan, preserved holding/rebalance semantics, custom-source transition after editing, and fail-closed malformed catalog handling.
- `tests/e2e/stock-search-flow.spec.js`: main/watchlist code-or-name suggestions, keyboard selection, persisted search history, SSE ownership, and page-hide cleanup across desktop/mobile Chromium plus desktop Firefox/WebKit.
- `tests/e2e/market-scan-probability.spec.js`: desktop/mobile and cross-engine coverage for H1/H5/H20 null/calibrated rendering, action-ineligible versus pending/skipped/outbox-missing/source-archived states, pending-to-archived acceptance, bounded same-run polling and stop conditions, filter-qualified-only thresholds, no probability sorting, preserved v4/v5 production ranks, frozen evidence and responsive bounds.
- `tests/e2e/market-scan-future-range.spec.js`: real desktop/mobile lazy-panel coverage for fixed D+1/D+2/D+3 controls, HLC3 non-VWAP wording, MFE/MAE versus executable net/net-excess evidence, group metrics and Rank IC, probability-context limitations, details pagination, unavailable evidence, and responsive overflow bounds without changing the production leaderboard.
- `tests/e2e/individual-probability.spec.js`: real mobile browser coverage for the independent D+2/D+3/D+4 companion panel, mixed calibrated/insufficient/not-generated horizon states, null placeholders, exact daily-K proxy and no-guaranteed-fill target wording, expandable historical OOS diagnostics, minimum touch size, overflow safety and endpoint-local failure isolation while the core stock workbench remains usable.
- `tests/e2e/static-server.mjs`: local static fixture server used by Playwright.

The v6 snapshot regression anchors include `test_scoring_as_of_is_bound_to_frozen_quote_observation_time`, `test_v6_retries_recompute_every_symbol_without_copying_snapshot_provenance`, `test_v6_capture_duration_uses_an_exact_20_minute_boundary`, `test_v6_snapshot_contract_fails_closed_for_missing_or_invalid_capture_evidence`, `test_quote_snapshot_span_allows_cross_market_event_time_offsets`, `test_market_scan_message_summary_separates_snapshot_blocker_passed_distribution_and_source_warning`, and the Playwright case `snapshot publication failure keeps a passed score-distribution gate visibly separate`. These names register coverage targets; execution outcomes belong only in the result ledger after the corresponding command has actually run.

## 4. Manual Smoke Test Checklist

1. Start the app on `127.0.0.1:8010` with the documented single-worker `--timeout-graceful-shutdown 5` command. Confirm `/api/health/live` is process-only, `/api/health/ready` reports SQLite plus `leader`/`standby`/`single`, every health response is `no-store`, and readiness becomes `503` during shutdown; with an SSE stream open, stop it and confirm the listener exits within the bounded shutdown window.
2. Type a Chinese name or partial code in both stock inputs; confirm the 250 ms autocomplete can be navigated by pointer and keyboard, and that loading, empty, and unavailable messages are distinct.
3. Enter a complete valid 6-digit code and confirm it submits directly without `/api/stocks`; confirm a non-complete cache miss adds only its search request.
4. Switch A-B-A between valid SH/SZ symbols and confirm the timeline and advice-review list retain only the newest request's state.
5. Switch daily ranges through 20/60/120/240 and confirm no request is issued.
6. Inspect the workbench JSON and confirm every daily row declares `adjustment_mode=qfq` plus non-empty `as_of`, `data_version`, and `contract_version` values.
7. Inspect first/middle/last rows on both canvases by desktop hover, touch tap, and keyboard; confirm time, OHLC, change, volume, enabled MAs, source/cache/fallback/fetch metadata, and crosshair position match the selected row without a request.
8. Switch minute intervals and confirm one request for each new interval and none when reselecting the current interval.
9. Confirm minute 204/empty/`null`, wrong-symbol, wrong-interval, and unavailable reports clear stale chart data and withhold executable levels or T-plan ranges.
10. In Tools, confirm local activity merges recommendation changes, alert events, and notes with distinct loading, empty, partial, and unavailable states.
11. Create a review plan from a persisted advice snapshot with `target > snapshot > stop`; record its revision/digest; evaluate at a historical cutoff and current time; replay the identical request; then change evidence for the same cutoff, edit A→B→A, and expand history. Confirm identical evaluation is idempotent, changed evidence appends an attempt, all three plan revisions remain distinct, current detail/summary order only the current revision by `as_of`, attempt, trusted `evaluated_at`, then ID, and a stale update/evaluate/archive/paper-freeze returns `409`. Tamper attempt or `evaluated_at` and confirm evidence-v2 becomes metric-free insufficient; inject a valid-shape v1 evaluated row and confirm it cannot become a current metric-bearing result. Archive the current revision and confirm active/detail/due/summary views hide it while the source advice, revision ledger, and attempts remain in an export/backup.
12. Run each fixed watchlist condition and a combined scan against both the current watchlist and custom codes; repeat with a historical cutoff and confirm excluded symbols, all-selected matching, as-of provenance, and missing-data rows are handled explicitly.
13. Add a watchlist item, edit queue metadata, load its advice timeline, and mark it viewed through the newest displayed advice ID; create a later comparable change and confirm it remains unread. Confirm excluded items do not enter quote refresh.
14. Add/update/delete an alert and note, and confirm navigation does not cancel an accepted persistence write.
15. Enable desktop alerts, establish the first-poll baseline, then create a new trigger and confirm one notification while the page remains open. Simulate one notification-construction failure and confirm the failed and later events are delivered in order on the next poll without duplicating the successful prefix.
16. Export user data, load the JSON in merge mode, and confirm commit stays disabled until the matching server dry-run succeeds. On disposable databases, force advice/plan/run ID collisions and confirm revision JSON rewrites the mapped advice ID, all dependent digests/foreign keys follow it, and a repeated import is idempotent. Tamper canonical revision JSON/digest, omit the ledger, supply an older current revision, or conflict with an immutable result and confirm the full transaction rejects. Confirm commit reports a verified rollback backup; change file, mode, timezone, or target user state after preview and confirm rejection. Treat replace as destructive and use only disposable data.
17. Create and verify a runtime backup, then break one disposable foreign key while leaving ordinary SQLite page integrity intact and confirm verification/restore rejects it. Open cleanup preview and confirm user-history candidates trigger a verified pre-cleanup backup while advice referenced by active or archived review plans is excluded. Run scheduled health cleanup and confirm alert/advice/review history is unchanged.
18. Open diagnostics and confirm fetch/market freshness, storage budget, categorized rows, providers, scheduler, and trading-calendar guidance remain readable.
19. Open **全市场榜单**, start one scan, and confirm the request returns immediately while real processed/total progress changes. Repeat the click and confirm it follows the same active run. Cancel and retry a v6 run, then confirm a new linked run is created, the original stays unchanged, every row returns to pending, a new all-quote capture envelope is sealed, and the full universe is recomputed rather than retaining clean rows.
20. On a disposable fake-provider run, include SH/SZ/BJ, ST/new, suspended, short-history, and failed-source rows. Confirm only successful rows receive stable ranks, coverage uses `success + missing`, each market retains at least 90% eligible rows, mass skipping blocks publication, and page/filter/sort views remain bounded.
21. Simulate a deleted active scan and repeated poll failures; confirm the UI returns to latest with bounded backoff, resets page/results when a different run appears, retries immediately after `online`, keeps one request/timer at a time, and announces progress/result milestones through one live region.
22. With disposable dual processes, confirm only one owns `<SQLite path>.runtime-leader.lock`, scheduler and scanner never split ownership, and standby takes over both after the leader exits. Confirm restore refuses the unified lock and both legacy compatibility locks.
23. Confirm invalid symbols stay in the query panel, while a failed valid-stock load clears the prior stock content and shows an explicit failure; then check desktop/mobile layouts for console errors.
24. Generate enough disposable workbench/provider/task/scan observations to cross each sample floor. Confirm `/api/system/reliability` reports the documented 7/30-day targets, returns `insufficient_data` before the floor, excludes cancelled work and retry durations as documented, and keeps bucket rows hourly rather than request-specific.
25. In an explicitly network-enabled environment, run `tools/provider_canary.py` with disposable or default SH/SZ/BJ representatives. Confirm stdout is sanitized JSON, the configured runtime database is untouched, quote/five-row daily-K/stock-pool summaries are present, and the exit status distinguishes complete, partial, and unavailable results.
26. In **全市场选股**, switch among `preopen`/`official`/`intraday` while another mode has the active task. Confirm the browser labels both identities, shows the mismatch, loads only the selected-mode latest/history, and never compares rank changes across mode/scope/rule. With a fake Shanghai clock, verify 08:00 defaults to `preopen`, the server binds both dates to the previous completed session, rejects weekend/holiday or 09:15 starts, and stops a running pre-open scan at 09:15. Select an older published run and verify filters, paging, Excel, snapshot evidence, and queue provenance all retain that run ID after refresh and online recovery; verify probability-source capture and future-range evidence remain unavailable for the `preopen` batch.
27. Expand **查看扫描快照** and verify score components, tie-break chain, dates, sources, adjustment, fallback/degradation, rule, and run ID match the persisted API row with no new request. Use **打开当前个股分析** separately and verify the live-data warning remains visible.
28. Exercise multiple markets/industries, all six production/market ranges, the frozen confidence/risk/tradability ranges, keyword and three sorts. Expand **可信筛选工作台** and verify it makes no request while closed, then independently returns the selected frozen run's breadth, sequential funnel, matched-condition reasons, missing/exclusion reason codes, bounded near misses and same-`(mode, scope, rule_version)` delta. Confirm breadth selects only five columns, evaluation does not deserialize full population payloads, and the final detail query de-duplicates and hydrates only the current page plus near misses, never more than 300 symbols. Confirm null stays `--`, a literal `%`/`_` keyword does not broaden the result, no provider/scoring call occurs, and an active probability threshold explicitly blocks only the v2 funnel rather than being ignored. Compare optimized/full-hydration canonical outcomes, result rows/order and the Excel `ScreenSpecV2` JSON/digest. Switch all five column views on desktop/mobile without a new request or horizontal page overflow. Create, edit, export, import, and reapply a v2 plan; migrate a v1 plan to `overview` without changing its old revision/checksum meaning; bulk enqueue current/all filtered pages in 100-symbol chunks. Record the same screen alert twice and verify identical digest plus `created=true` then `false`; change the expected revision and verify the shared domain-input `400` response; make a former match currently `pending`, `missing` or `skipped` and verify it is suppressed rather than reported as an exit.
29. Observe a disposable scan from fewer than 20 rows through completion. Confirm ETA says `估算中` initially, then becomes bounded; every quote chunk is frozen before K-line acquisition begins; the sealed capture count matches the universe; and terminal rows expose `quote_observed_at`. Exercise the exact 20-minute boundaries for capture duration, global observation span, and each market's event span. Confirm a larger SH/SZ/BJ cross-market event-time offset remains diagnostic only, while a real blocker prevents publication. In the browser, confirm **发布阻断**, **已通过门禁**, and **数据源告警** remain separate and a passed score-distribution gate is not rendered red.
30. Run `tools/benchmark_market_scan.py` twice and compare cold/warm medians plus result-equivalence flags. Run `tools/evaluate_market_scan.py` on a copied/known database and verify source bytes do not change, future bars are never used to regenerate ranks, and under-floor cohorts say `insufficient_data` rather than claiming effectiveness.
31. In the strategy laboratory, parse the documented Chinese example. Confirm the original text, defaults, ambiguous trend/risk clauses, structured amount/listing/industry/holding conditions, all five board labels, and dry-run plan are visible before save; confirm save remains blocked until ambiguities are explicitly accepted through the structured editor.
32. Save revision 1, execute the latest official scan, then modify one condition into revision 2. Confirm the old execution still reports revision 1 and the same strategy/rule/data/cost fingerprints; run an exact historical replay and confirm its date/run cannot drift to today's latest snapshot.
33. Page a real approximately 5,500-candidate execution at 50 rows, independently sort objectives, inspect one candidate's hard-filter failures, marginal contributions, minimum changes, and ±10% rank sensitivity, and confirm the DOM never contains the full result set. Verify `no_trade` remains visible when no evidence-qualified portfolio exists.
34. Refresh evidence and confirm the response returns from the retained offline report rather than starting an evaluator, shows baseline generation time/digest and custom-execution digest status, separates official/intraday cohorts, and blocks manual-review eligibility while independent dates, integrity, or PBO are insufficient. Confirm there is no automatic-promotion action.
35. Create a version-pinned disabled test schedule, evaluate it twice against one published run, and confirm at most one successful execution plus fingerprinted events. Generate a simulation plan and confirm it states paper-only/no broker; export/import user data into a disposable database and confirm strategy revisions survive ID collision without duplication.
36. Run the probability evaluator against a copied known database and a separate artifact directory. Confirm source bytes remain unchanged, the artifact persists all 1/5/20-session and both-target records, current underpowered cohorts expose only `probability=null`, corruption fails closed, threshold filtering returns `422`, and the production leaderboard order is identical before and after artifact generation.
37. Run the future-range evaluator against a static database copy and separate artifact directory. Confirm D comes from frozen evidence, D+1/D+2/D+3 match trusted fixed sessions, HLC3 is labelled non-VWAP, a suspended/missing bar is not shifted, D+1 execution is unavailable under T+1, D+2 maps to existing H=1, net/net-excess and MFE/MAE remain separate, and source bytes stay unchanged. Confirm underpowered results stay `insufficient_data`: descriptive mean/median/positive-rate values may remain labelled, but CI/pass/effectiveness conclusions are blank and missing data never becomes zero. Open the selected official run, verify the panel makes no request until expanded, switch offsets/groups/pages/symbols, then export XLSX and compare its evidence/status/digests with the API artifact.
38. Complete one disposable current action-eligible original-publication `official` v6/v5 scan. Verify immutable exact rule registration, canonical replay receipt, atomic typed outbox enqueue, lease/retry/restart recovery and unchanged rank. Confirm source-v3/snapshot-v3/coverage-v2 records reconstruct run skipped count and ALL/SH/SZ/BJ population/eligible/success/missing/skipped; require PIT v4, current v5, source digest and deterministic row replay. Verify source v1/v2/v3 and score v4/v5 readers preserve exact historical contracts, while new v3 writes only v5. Exercise authoritative query order and require equality among succeeded outbox archive digest, source integrity digest and any calibrated-result binding. Confirm the 224/232/262 formal floors and every OOS/joint/deployment gate remain unchanged, retained run 71/run 77 stay `legacy_unbound` and null, and non-blocking single-flight preserves its last-good snapshot.
39. Open two stocks in A-B-A order and verify the workbench and individual-probability routes retain symbol/time ownership, `no-store`, generic failure details and no advice persistence. Verify D+2/D+3/D+4 fixed proxy semantics and current assessment-v4 source intake from source-v3/snapshot-v3, PIT v4, canonical action receipt and current v5 score replay. Legacy source v2/score v4 remains exact audit-only. Keep the unchanged 288 overall and 284/286/288 per-horizon fold/bin/Brier/calendar gates; compact runtime evidence still projects count 0, null signal date, false selection and null values until locator-bound source replay exists.
40. Inspect the frozen strategy-template catalog. Confirm the 14-ID, 6/3/5, digest and compiler contracts remain exact, but treat its `full-market-score-v4` and two sessions as a historical dated identity. Loading a draft must not mutate current v5, historical v4, saved strategies, executions or SQLite.
41. Verify current v5 score correctness: continuous trend maps exactly to `[-4,+4]`, enters clamped base, `raw_score=base`, and only display score rounds. Verify historical v4 read/replay is unchanged and never rewritten. Exercise immutable exact rule registration on start/retry/TOP100/result/publication; current official `new_listing_insufficient_history` and `official_session_gap` require complete replayable skip evidence, while every other unavailable case is `missing`. For K-line skip-PIT parity only, confirm exact `YYYY-MM-DD` normalizes to `Asia/Shanghai` 00:00, while a future date, slash spelling, ordering violation, or direct artifact timestamp misuse fails closed. Preserve production `#86` at 5,389/154/0 success/missing/skipped and verify the read-only replay oracle finds exactly 146 typed skips (38 new-listing + 108 session-gap), 7,673 bound bars and expected new-run conservation 5,389/8/146 without publishing or resealing that run. Confirm distribution-v4 and canonical publication receipt fail closed on raw-only, incomplete, constant, pseudo-diverse, missing-rule or caller-supplied evidence.
42. Tamper every frozen-snapshot, rule-registry, receipt, outbox and source-digest invariant. Confirm one request-local query-only transaction performs one complete v2 seal hash and one fused canonical replay, with no second SQL score read, and returns its page from the same snapshot. Closing, cross-thread reuse, transaction restart, DML, DDL, schema or `query_only` mutation must revoke the capability. Snapshot and artifact corruption retain their two generic `409` classes and all related routes remain `no-store`. Run the exclusive synthetic 5,382-row case three times and require CPU under 5 seconds and wall under 12 seconds while asserting hash count 1 and replay count 1; treat timings as observations, not an SLA or evidence that run `#82` changed.
43. Build strict authorization fixtures and confirm only a verifier-produced opaque artifact-v1 token can pass type admission; ordinary mappings, copied pass flags, stale/future/unsealed time, altered OOS rows, summaries not equal to raw recomputation, drift reseals, incomplete joint corpus and changed session economics fail. Confirm deployment refit is separated from the last OOS fold and enforces calibration purge/replay/freshness, but the current executable-only conditional label fails the all-decisions joint-estimand gate before artifact publication. With no verified deployment artifact, run-level, per-row and new-prediction values must stay typed null.
44. Exercise joint v2 only after the fixed exit session has matured. Bind `{run_id}:{symbol}:{1|5|20}:{target}`, entry offset 1, exit offset `H+1`, an after-close aware generation time, raw entry/exit bars/rules/reference/capacity, frozen-universe benchmark and calibration identities. Confirm both permanent findings remain present, all five components stay null and action qualification stays false even with otherwise complete evidence, supplied probability values, pass booleans or a recomputed outer digest. Confirm the report is never read as a signal-D feature or one-report substitute for a multi-session OOS assessment.
45. Execute `latest_scan` around intraday/after-close/weekend boundaries using the trusted exchange calendar. Confirm the reference session and exchange-session gap—not calendar days—drive `strategy-execution-freshness-v2`; unavailable calendar, future data and over-limit age fail before save, while historical replay stays deterministic. Remove selected candidates through portfolio constraints and verify equal/risk-adjusted paths refill deterministically and recompute weights, custom paths never add a symbol, and replacement/pool-exhaustion/underinvestment evidence matches the actual final draft. Tamper the latest evidence row, reconstructed execution result or sealed simulation source identity/digest and confirm no older-row fallback or coherent reseal is accepted.
46. Load one stock workbench repeatedly across a mocked pre-open/intraday/after-close boundary and inspect `stock-workbench-v2`. Confirm `interactive_shadow`, all three production/advice effects, requested/observed/quote/every child symbol, quote event, signal date, daily cutoff and decision time agree; inject a wrong-symbol child or stale cohort and confirm generic server `409` plus no browser render/cache reuse. Confirm pre-open/intraday excludes an unpublished daily bar and volume confirmation stays neutral. Supply wrong-date/fallback breadth, wrong-symbol/date order book and wrong-symbol/date/fallback concepts and verify typed exclusion. Remove volume/structure/ATR evidence and verify it does not raise factor score or create target/stop/ratio. Finally compare advice history before and after repeated workbench and `/api/analyze` reads: it must not change; only an eligible after-close active-queue refresh may append the formal snapshot.
47. Freeze one current review plan into **模拟交易**, then replay a disposable continuous PIT window containing a normal entry, entry-day exit signal, next-session suspension, locked sell, and later sellable open. Confirm the plan revision/digest/payload is rechecked inside the write transaction and rebound again on strategy read/run; suspension neither fills nor changes the open position's last tradeable valuation; the exit remains latched and writes one blocked event per blocked session; T+1 exits only at the later sellable open; benchmark/equity skip non-trading sessions; and the complete execution/suspension/corporate-action fields change the market fingerprint. Remove one fixed session or PIT/execution field and confirm `data_unavailable`, not a synthesized fill. Mutate a stored result/trade/equity/event/run field and confirm dashboard/export rejects the v3 output digest; remap only surrogate IDs and confirm the digest is stable; import a complete plan-ID collision that changes embedded identity and confirm the target transaction recomputes and verifies affected runs. Then try account-only or strategy-only partial merges against existing Paper history and confirm atomic rejection with unchanged state/digest. Change current account storage directly and confirm historical performance still uses the run's frozen initial cash; ordinary account update must already reject after any strategy or run. Feed malformed dashboard/run/compare payloads—including orphan/cross-symbol trades and non-conserving counts—and confirm the browser does not mutate state or DOM. Compare/export both immutable runs and confirm every review/paper success and failure is `no-store`, a 5xx is generic, and the UI retains Research Shadow/no-broker language.
48. On a disposable copy containing pre-seal publications and strategy executions, start the new schema once. Confirm seal columns exist before trigger creation; all old publications become digest-valid `legacy_backfill`; all five triggers exist; the snapshot migration marker is written only after verification; surviving execution sources bind matching digest/origin as `verified`; deleted-run orphans remain null/null/`legacy_unverified`; strategy guards are restored; and both migrations are idempotent. Force a failure at each phase and confirm transaction rollback with no success marker. Reintroduce an orphan's old run ID after the marker and confirm startup fails safe rather than silently upgrading it. Audit a `legacy_backfill` through history/results/summary/breadth/evaluation, then confirm run/apply/enqueue/rank-change Discovery; current/previous delta; screen-alert persistence; new strategy/simulation/Shadow/evidence/automation; probability outbox/managed publication/future range; degraded retry-copy; and TOP100 refresh all reject it. Repeat with an original-publication exact-full-scope fixture while independently injecting a blocker, missing/duplicate/wrong-severity `score_distribution.pass`, score-distribution source warning, and other passed score gate; every action must reject while read-only browse/audit stays visible. Confirm a clean diagnostics-v1 fixture passes and a v6 full-recompute retry remains available without preserving old rows. Verify run #80 projects the same nonauthorization boundary. Never use the production database for this smoke.
49. On a disposable database, create three digest-valid published graphs with `task_run_id` owners and set the keep window to one. Confirm preview and commit identify/delete exactly the two unreferenced oldest result/run graphs, leave the newest graph immutable, and allow old task rows to become independently eligible. Repeat with each dynamic-FK and declared Discovery/strategy reference family, pending/processing versus terminal probability-outbox states, probability result/source/outcome/fit artifacts, future-range artifact/summary, primary individual assessment, and canonical-project versus custom-database docs fallback; confirm only live outbox work and explicit recursively declared verified-file run IDs pin, while sparse fit membership does not expand a numeric through-range. Exercise legacy `payload` and current `generated_at+payload` large probability streams, a no-manifest singleton, a manifest crossing the read boundary, equal-length tamper, scalar-ID escape, generation-time mismatch and file mutation. Build a multilevel old-publication → failed → retained-publication → active chain for each of `queued`, `running`, and `cancelling`; confirm every reachable candidate ancestor remains, then confirm an unreferenced three-run chain still converges to the configured one. Hold publication against cleanup in separate spawned processes and confirm serialization from first scan through commit. Inject unknown/non-regular/malformed/digest-mismatched/changed catalog state, missing explicit transaction, snapshot tamper, delete failure, count mismatch and trigger restoration failure; confirm no graph is partly removed and the exact five triggers remain installed. Treat the digest as an integrity check only, never as a backup. Never run this smoke against production SQLite.
50. In a disposable project-shaped tree, exercise all six managed writers as one- and multi-file batches. Confirm canonical database/path binding, same-database nested target/run subsets, fixed owner-only lock root, sidecar/database-parent rechecks, and exact source action-gate revalidation. Inject body, final namespace and release failures after one or more publishes; every new exact path/size/SHA-256 file must be stream-removed while identical pre-existing files remain, and one rollback failure must not prevent later journal cleanup. Reject aliases, links, hard-linked databases, mixed managed/external batches and cross-database nesting; allow a genuinely external offline destination without runtime-managed semantics. Then attempt restore against an existing and initially missing target while each managed directory, fixed summary and applicable docs fallback is populated; replacement must not occur. Confirm a custom database does not inherit project fallback. This smoke assumes cooperating same-UID processes and must not be described as protection against a malicious same-UID root rename.
51. Copy an intact historical outcome-v1 artifact whose only current-replay difference is the exact old degraded rule-profile shape. Confirm direct load raises typed `ProbabilityOutcomeSemanticDriftError` only after filename/payload digest, envelope, source/cohort/calendar, all siblings, quality and limitations verify, and binds exact run/source/digest/as-of/generated identity. Place an older valid outcome beside a newer-or-equal matching drift, restart maintenance twice, and confirm both summaries have `due_count=0`, one skip, no K-line read, no outcome publication and no fit. Add a strictly newer valid outcome and confirm ordinary due work/publication recovers. Preload must succeed without changing artifact bytes, omit the drift outcome and every cumulative dependent fit, and project `outcome_evidence_status=legacy_semantic_drift_excluded`, `pipeline_stage=source_archived`, `fit_status=not_started`, `selection_qualified=false`. Independently mutate source binding, terminal identity, digest, one ordinary outcome field and one sibling record, plus create an equal-order drift conflict; each must remain hard/non-authorizing with the previous last-good index preserved. Never edit or re-sign the retained artifact.
52. Run the two CPU-boundary regressions without the production database. For industry plates, require one HTTPS request with `allow_redirects=false`, exact status `200`, no body/decode after a redirect or oversized `Content-Length`, a 512 KiB streamed cap and deadline, `total == len(diff) <= 100`, unique well-formed `BK` codes/names and descending finite rows; confirm an advertised second page, incomplete/duplicate/malformed data or timeout fails without entering AKShare SDK pagination or replacing the previous cache. For the automatic scanner, begin with one genuinely sealed terminal same-day run, execute 100 one-second ticks while writing unrelated monitor events, and confirm the whole-graph verifier runs exactly once and no scan/action is created. Tamper only a result: the pre-deadline scheduler shortcut must neither authorize nor publish, an immediate repository/API read must fail closed, and the tick at five minutes must re-audit and fail closed without K-line/provider work. Also change the market-scan header/database identity and leadership epoch independently and confirm each invalidates the shortcut.

53. Read frozen run `#82` without writing the production database. Confirm the probability projection is `source_scan_action_ineligible`, no outbox/archive is fabricated, and UI does not collapse it to generic “尚未生成研究证据”. On a disposable new current official v6/v5 run, confirm `pending`/`processing` renders capture-in-progress, polling counts failures and stops by attempt 60 or terminal/run transition, `succeeded` with matching three-way digest renders source sample accumulation, and `skipped`/missing outbox remains distinct. All percentages must remain null until the existing research gates pass. In browser/controller tests, a fresh terminal failed/cancelled/interrupted run without publication (including the `#86` shape) makes zero results calls, clears `aria-busy`, keeps probabilities null and filtering disabled, while queued/running remains busy and a prior published selection is not mixed. Force a results-read failure and require **证据读取失败·等待重试**, blank limitations/values and successful retry recovery. Feed direct published intraday/TOP100/legacy-seal `ineligible_run_contract` plus `probability_requires_published_official_full_market_run` and require generic contract-ineligible copy, null/disabled state, and zero capture polling rather than a false “批次未发布” assertion.

## 5. Request And Browser Budgets

| Flow | Expected additional requests |
| --- | ---: |
| Cold stock load, including SSE | 14 |
| Each stock switch, including SSE | 5 |
| Daily chart range switch | 0 |
| Each new minute interval | 1 |
| Repeated active minute interval | 0 |
| Complete valid 6-digit input | 0 stock-search requests |
| Non-complete user input, after debounce and on cache miss | 1 stock-search request |
| Repeated cached autocomplete query | 0 stock-search requests |
| Daily/minute pointer, touch, or keyboard inspection | 0 |
| Local research-activity filter switch | 0 |
| Individual D+2/D+3/D+4 probability | 1 companion request per cold stock load/switch; 1 only on explicit retry; no provider or request-time fit |
| Fixed-condition watchlist scan | 1 |
| Opening the full-market workspace | 1 global latest-task request plus 1 selected-mode history request; if the task is absent/active/failed/other-mode, at most 1 selected-mode latest-published request; then 1 result request for the chosen published run |
| Active full-market scan | 1 progress request per 2-second poll; no overlapping poll |
| Probability source capture pending | 1 same-run request per polling interval; at most 60 attempts including failures; stop on terminal state or run transition |
| Full-market result page/filter change | 1 request, capped at 100 rendered rows |
| Trustworthy screening workbench | 0 while collapsed; up to 3 independent frozen-read requests on first expansion or explicit refresh (`breadth`, `screen/evaluate`, `delta`); an active probability threshold rejects evaluation locally, so only 2 are sent |
| Opening future-range evidence | 0 while collapsed; 1 request on first expansion, then 1 per D+ offset, exact-symbol, or 20-row page change; follow-up pages set `include_research=false` |
| Executable-candidate Shadow | 0 on workspace/Strategy Lab activation or use-current; 1 only per explicit submit; cancel/run change/lab close/page hide aborts or suppresses stale work |
| Strategy execution candidate page/sort change | 1 request, 50 rows in the browser and hard API cap 200 |
| Strategy evidence refresh | 1 compact offline-artifact request; no provider or cross-date evaluator |
| Full-market Excel export | 1 request for the complete current filtered snapshot; no provider refresh or score recomputation |
| Expand/collapse frozen scan evidence | 0; persisted row fields only |
| Apply one discovery preset | 1 leaderboard request plus 1 bounded rank-change request |
| Record one saved-screen change event | 1 explicit idempotent write request; no provider or scoring request |
| Enqueue all filtered discovery rows | 1 leaderboard request per page, then 1 queue request per 100 unique symbols |
| Advice-review evaluation | 1 |
| Expanding one advice-review history for the first time | 1 |
| Opening the global review dashboard | 3 independent no-store branches: 2 fixed reads (`summary`, `due`) plus at least 1 plan-list page per 100 active plans, capped at 100 pages; one branch failure must not suppress the other two |
| Paper dashboard / explicit simulation | 1 read on activation or refresh; 1 write only per explicit run; no broker request |
| Each Tools-tab cleanup preview | 1 |
| Enabling browser notifications | 1 immediate baseline page; later 30-second polls use as many 50-event keyset pages as needed, capped at 200 pages |

Stock-search requests are not part of the four-request stock-switch baseline: only a debounced, uncached, non-complete user query may trigger one. A selected suggestion then follows the ordinary stock-load budget. The latest recorded Chromium desktop/mobile plus Firefox/WebKit browser matrix is **147 passed, 49 skipped**; the skipped cases are intentional project/device-exclusive scenarios recorded by Playwright rather than treated as passes.

Regression-sensitive boundaries include Python/Node/npm declaration drift, host-timezone independence, Shanghai-to-UTC audit migration, monotonic durations, checked dependency direction and mypy-scope shrinkage, cancellation propagation, health admission semantics, reliability sample floors/cardinality/exclusions, isolated provider-canary state, runtime/dev dependency audits, redacted complete-history secret scanning, reproducible SBOMs, immutable Action pins, core-workbench-first cold-load dispatch under browser connection limits, equal-event quote quality priority and legacy UTC ordering, `qfq`/legacy K-line isolation, quote/minute session freshness, provider request-key single-flight/orphan isolation plus daemon-worker process exit, the `preopen` 09:15 call-auction stop, the 15:15 daily publish threshold, full-market mode/date/close alignment and independent cohorts, atomic task attachment and scan/task terminals, owned terminal-failure recovery, structured degradation and unified retry plans, normalized atomic stock-pool replacement, quiescence-delayed single runtime leadership and whole-service takeover, set-based retention with retry-lineage convergence, cross-process backup leases and explicit rotation/restore guards, scan contract/backoff/latest recovery and ARIA state, server-current-time handling for today's review/scan, rendered-symbol ownership, single-use import previews, latest-owner browser state, serialized backup-before-import, automatic user-history exclusion, review-linked retention, successful write reconciliation, comparable-change unread watermarks, notification cursor advancement, stale companion responses, and request-budget drift.

## 6. Latest Test Report

Each row describes the exact worktree scope verified by its command; older records remain for traceability. Automated verification uses an isolated locked Python environment, fake providers/HTTP clients, temporary databases, and no credentials or live-provider dependency unless a row explicitly says otherwise. Historical rows that recorded `2/288` or called assessment v3 “current” describe only their then-current checkpoint and are now explicitly superseded: the current builder is assessment v4, v3 is rejected, and the tracked v2 runtime projection is 0/288 with `signal_date=null`.

| Date | Worktree State | Environment | Command | Scope | Result | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-08-15 | Market-scan heavy-read busy recovery UX hotfix | Project Python 3.12 `.venv`; Node 24.14.1; desktop/mobile Chromium, Firefox and WebKit; provider-free fixtures; no scan trigger, outbox retry or production database write | `pytest tests/test_frontend_api_format_workbench.py tests/test_market_scan_frontend.py`; static/API/market-scan API focused pytest; `npm run check:js`; focused four-project Playwright | HTTP `Retry-After` preservation; capacity-one verifier busy classification; no failure-count escalation; last-good榜单/概率证据保留；无缓存等待态；按服务端节奏自动重试；跨 horizon 状态不串线 | Frontend/request `49 passed`; static/API `128 passed`; JS syntax passed; Playwright `4 passed` | Busy stays distinct from deterministic `409` integrity failures and ordinary network failures. Static cache token is `20260815-market-scan-busy-local-1`; the earlier ordered-12 manifest predates this post-deployment patch. |
| 2026-08-15 | Horizon single-owner UI/export controlled rollout and online acceptance | Frozen ordered-12 worktree; production single-worker Uvicorn; one in-app browser tab; supported SQLite backup; no provider mutation, scan trigger, outbox retry or production-row edit | Verify ordered manifest; `tools/runtime_data.py backup` + `verify`; graceful SIGTERM; run the exclusive 5,382-row integrity/performance node three times without coverage/concurrency; approved screen/Uvicorn start; live/ready/task checks; lock-aware `mode=ro + query_only + BEGIN` quick-check/FK/state audit; one ordinary-page H1→H5→H20 and Excel-export acceptance; post-close CPU/task audit | Deployment identity, offline performance, #86/#87 immutability, source/outbox binding, local horizon reprojection, heavy-read request counts, probability fail-closed state, single export, browser console and post-close resource drain | Backup integrity `ok`, SHA-256 `3c39c6acb1a4861b06516a6d337744acc077ca21a3031e7f67ff7524ac4eab9a`; offline SLA 3/3 passed in 57.35s / 56.26s / 56.72s; new PID 54301 live/ready leader; quick-check `ok`, FK 0, `total_changes=0`; initial #87 page 5,389 rows and `source_archived / insufficient_data`; rapid horizon changes caused zero results/latest reads; request ledger latest 1 / latest-published 1 / results 1 / export 1 / server errors 0; post-close CPU 0.2% and all tasks terminal | Excel generation returned HTTP 200 but completed just after the browser harness's 60-second download-event deadline; UI returned non-busy with no error and no second export was sent. #86 stayed failed 5,389/154/0; #87 stayed degraded 5,389/8/146 with source outbox attempt 7 succeeded and digest `fb6af8b7…64a6d`; max run remained 87 and #88 was not created. |
| 2026-08-15 | Horizon single-owner UI/export closure; formally verified, not deployed | Project Python 3.12 `.venv`; Node 24.14.1/npm 11.11.0; provider-free temporary SQLite fixtures; Chromium desktop/mobile, Firefox and WebKit; no production database, provider, artifact, outbox, service restart or scan | Complete `tests/test_market_scan_frontend.py`; focused and full four-project Playwright; full pytest with app/tools line+branch coverage; loopback-authorized Uvicorn smoke; exclusive 5,382-row integrity/performance test three times; `npm run check:js`; full Ruff and mypy; static/architecture/tool/typing guards; regenerate/check function inventory and check API inventory; `git diff --check`; ordered-12 SHA-256 verification | Same/cross-context deterministic 409 fail closure; single-owner promise tail and last-intent races; local unfiltered horizon projection; exact filtered-query rebuild; visibility/history/surface/poll/query/terminal ownership; export fetch receiver binding; generated inventory and full release regression | Frontend 38 passed; horizon Playwright 12 passed plus desktop Chromium repeat 5/5; export four-project 8 passed; final browser matrix 147 passed / 49 intentional skips; Python 4,294 passed plus loopback smoke 2 passed and 68 subtests; combined line+branch coverage `90.14407527197883%`; static/architecture/tool/typing 39 passed; three exclusive 5,382-row gates passed; JS, Ruff, mypy (255 files), both inventories and patch check passed | Full-suite sandbox run's only two failures were `bind(127.0.0.1, 0)` permission denials; the exact two tests passed when loopback was authorized, so there were zero product failures. The browser final gate exposed and closed an extracted-`window.fetch` `Illegal invocation` in Excel export. Ordered manifest `ordered-12.sha256` has SHA-256 `96a4fcd631de385a6fe82f119a36809315718991013949b1db4b86453bb99662` and verified 12/12 files. No deployment, backup, runtime write or online acceptance was performed. |
| 2026-08-15 | Run `#87` weekend probability-source temporal closure | Project Python 3.12 `.venv`; provider-free current-v5 PIT fixtures, temporary content-addressed artifact stores, trusted bundled exchange calendar; production `#87` inspected immutable/read-only only; no service restart, retry, signal, scan, provider or production write | Probability/individual/mode focused pytest; capture/source-research/query focused pytest; architecture/repository-structure/tool/typing guards; documentation/configuration/typing/architecture/exception guards; `npm run check:py`; full Ruff; full mypy; regenerate function inventory and check both generated inventories; `git diff --check` | Current source-v3 latest-completed official-session binding; Friday source with weekend aware decision/capture; pre-15:15, next-session stale, future/reverse, naive and calendar-coverage closure; frozen source-v1/v2 and previous-v2 calendar isolation; 100-row three-market current-v5 projection through source snapshot, individual assessment seal and content-addressed store | Probability/individual/mode group 405 passed in 46.23s; capture/source-research/query group 138 passed in 13.61s; architecture/structure/tool/typing guards 25 passed in 31.07s; documentation/configuration/typing/architecture/exception guards 82 passed in 49.98s; `check:py`, Ruff, API/function inventory checks and patch check passed; mypy found no issues in 255 source files | Read-only diagnosis found `#87` source/data date `2026-08-14`, decision `2026-08-15T00:38:35+08:00` and capture after the decision: the old natural-date equality alone caused the outbox error. The current contract now resolves that Saturday decision to Friday's latest completed official session without weakening the publication, coverage, score, PIT or action gates. Capture may be later availability, but a new matured session makes the old source stale. Frozen v1/v2 behavior remains isolated. No production row, outbox attempt, artifact, service or scan was changed or triggered. |
| 2026-08-14 | Interactive workbench concept-cache orphan containment | Project Python 3.12 `.venv`; temporary SQLite databases; fake concept providers and provider runtime; no browser, live provider, production database, service restart, signal, or scan | Focused metadata/workbench/function tests; broader API/DataHub/runtime/individual/local-lifecycle/theme/leadership/workbench group; documentation/configuration/typing/architecture/exception/metadata-structure guards; `npm run check:py`; `npm run check:js`; regenerate both inventories and run both checks twice; full mypy and Ruff; `git diff --check` | Ordinary workbench concept membership reads only fresh non-static provider cache; trimmed-source exclusion occurs before order/limit; missing/stale/static/read-error/cancel states are explicit unavailable context with zero provider/runtime/write side effects; legacy hubs cannot fall back to provider-capable APIs; fallback rows cannot score; explicit internal refresh compatibility remains separate | Focused group 94 passed in 5.72s; broader group 284 passed in 6.66s; static/structure group 86 passed in 51.54s; Python/JavaScript checks, both inventory checks twice, Ruff and patch check passed; mypy found no issues in 255 source files | The pre-fix isolated 80-board fake-provider reproduction returned a workbench timeout after 0.102s while its executor worker continued from 8 to 80 provider calls and finished after 1.159s. The fixed interactive path made zero provider calls for fresh/missing/stale/static/read-error cases and across 100 concurrent or cancelled reads. A directly injected legacy source `" 本地个股基础数据 "` proved plain `source <>` would occupy `LIMIT 1`; `TRIM(source) <> ?` returned the external row. This cache has no receipt/digest and authorizes no scan, probability, filter, action, or publication. API inventory remains 156 endpoints. Function inventory: app 404 files / 1,015 classes / 6,044 module functions / 1,393 methods / 135,212 lines; tests 230 / 199 / 3,535 / 882 / 115,370; tools 15 / 7 / 152 / 6 / 3,314. |
| 2026-08-14 | Non-authorizing market-scan polling identity P1 closure | Project Python 3.12 `.venv`; Node/npm; temporary SQLite repositories; fake browser/API fixtures; Chromium desktop/mobile, Firefox and WebKit; no provider, production database, service restart or scan trigger | Focused polling/API/repository/frontend pytest; four-project Playwright over market-scan flow/probability/future-range/screening plus responsive layout; `npm run check:js`; static/module/function/repository-structure/tool/typing guards; both generated inventory checks; full Ruff; full mypy; `git diff --check` | Token-only combined global-latest/mode-published identity; query-only header projection; 1,000 unchanged repository/UI ticks and 100 concurrent coalesced ticks; zero result SQL/full verifier on unchanged polls; trusted-selector recency; bounded identity-before/trusted/identity-after synchronization; active-batch token stability; tamper/database/schema/null-slot/parity boundaries; history/surface/mode/query supersession and timer liveness; published query/probability ownership; cross-browser recovery and terminal probability UI | Focused polling group 39 passed in 3.20s; final Playwright matrix 46 passed and 30 intentional project/device skips in 1.9m; JavaScript syntax passed; static/module/function/tool/typing group 43 passed in 32.25s; API/function inventories regenerated and both checks passed; Ruff passed; mypy found no issues in 254 source files; patch check passed | The 1,000-read repository trace used exactly 1,000 query-only snapshots and 2,000 run-header SELECTs, no `market_scan_result` SQL, no snapshot verifier, at most 7,000 small statements, one stable fingerprint and the existing under-10-second bound. Real `save_result_batch` progress changed `updated_at` without changing the active token; queued→running and terminal transitions changed it. Initial/forced/changed sync still called fully verified `latest` and mode-scoped `latest-published` before results; unchanged ticks made zero trusted calls. Result-only tamper remains deliberately outside the lightweight token and fails at the next trust boundary or five-minute scheduler audit. API inventory: 156 endpoints. Function inventory: app 403 files / 1,012 classes / 6,041 module functions / 1,380 methods / 135,031 lines; tests 230 / 198 / 3,514 / 880 / 114,426; tools 15 / 7 / 152 / 6 / 3,314. No production database/service/artifact was read or changed. |
| 2026-08-14 | Run `#85` UI compatibility and bounded heavy-read admission closure | Project Python 3.12 `.venv`; Node/npm; disposable API/repository fixtures; real `create_app` exception handler; Chromium desktop/mobile, Firefox and WebKit; live browser observation was read-only | `.venv/bin/python -m pytest -q -p no:cacheprovider tests/test_market_scan_api.py tests/test_market_scan_frontend.py tests/test_market_scan_repository.py tests/test_app_lifecycle_integration.py tests/test_api_container_modules.py`; four-project `tests/e2e/market-scan-flow.spec.js`; `npm run check:py`; `npm run check:js`; static/exception/container/typing/architecture/repository-structure/tool/config guards; regenerate and check both inventories; full Ruff; full mypy; `git diff --check` | Exact eligible-denominator progress parity and frozen pre-receipt `#85` read normalization; arbitrary/current mismatch closure; stable failed-fingerprint circuit; exact transient admission busy; capacity-one nonqueueing gate shared by four dashboard snapshot reads; cancel+GC strong ownership; create-task failure recovery; late-exception consumption; fresh post-release verification; real global-handler headers; atomic close/start tracking; cancellation-shielded shutdown drain before runtime/storage | Focused API/frontend/repository/lifecycle/container group 191 passed in 9.48s; final Uvicorn/lifecycle/API fixture reclose 146 passed in 9.84s; four-project Playwright 22 passed and 18 intentional skips in 1.6m; static/module/function/tool/typing group 126 passed in 54.29s; Python/JavaScript checks, both generated checks, Ruff and patch check passed; mypy found no issues in 255 source files | A stable deterministic failure generated one trusted selector attempt across 100 ordinary ticks; force and changed fingerprint each admitted exactly one new attempt, while the exact controlled `503` followed `Retry-After`. Busy requests across `latest`, `latest-published`, run detail and results entered the manager zero times; polling identity remained available. Caller cancellation plus deletion/GC retained active=1/worker=1, a cancelled close waiter still drained the real worker, late failure produced no loop warning, and worker/lease counts returned to zero. A real lifespan held runtime/workbench/DataHub open until the cancelled request's worker finished. Live read-only browser verification rendered published run `#85` at 5,497 results without the legacy coverage error. API inventory: 156 endpoints. Function inventory: app 404 files / 1,015 classes / 6,044 module functions / 1,391 methods / 135,170 lines; tests 230 / 199 / 3,524 / 882 / 114,897; tools 15 / 7 / 152 / 6 / 3,314. No production database, service, artifact, or scan was changed/restarted/triggered. |
| 2026-08-14 | Formal `#86` skip-PIT and probability-card hotfix documentation/inventory closure | Project Python 3.12 `.venv`; final frozen production/test/UI tree; no provider, runtime database or research-artifact access | Regenerate `tools/api_inventory.py` and `tools/architecture_inventory.py`; run both `--check` modes twice; `PYTHONDONTWRITEBYTECODE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_tool_inventory_modules.py tests/test_config_modules.py tests/test_typing_contract.py tests/test_architecture_boundaries.py tests/test_exception_safety.py`; rerun `tests/test_tool_inventory_modules.py`; full mypy with `--no-incremental`; full Ruff with `--no-cache`; exact skip-index count; `git diff --check` | Skip-PIT-only canonical date K-line `as_of` parity with future/non-canonical fail closure; immutable `#86` negative evidence and read-only typed replay oracle; terminal-unpublished, results-read-error/retry, and direct contract-ineligible probability UI semantics; generated inventory and documentation/configuration/typing/architecture/exception guards | Both generated checks passed twice; combined guards 82 passed in 51.42s; tool/index guard 14 passed in 31.95s; mypy found no issues in 254 source files; Ruff passed; the skip test index appeared exactly once; patch check passed | `#86` remains failed/unpublished at 5,389 success / 154 missing / 0 skipped. Its immutable replay oracle is 146 typed skips (38 new-listing + 108 session-gap), 7,673 bars, and expected new-run conservation 5,389/8/146; no production row was repaired or resealed. Frozen UI audit evidence remained frontend 24, Playwright 8 across four projects, and static/function/private 22 passed. API inventory: 155 endpoints. Function inventory: app 401 files / 1,009 classes / 6,030 module functions / 1,374 methods / 134,754 lines; tests 230 / 198 / 3,499 / 879 / 113,289; tools 15 / 7 / 152 / 6 / 3,314. This closure edited only five narrative documents plus generated API/function inventories; production code, tests, databases and artifacts were not modified. |
| 2026-08-14 | Formal current-v6/v5 documentation and generated-inventory freeze | Project Python 3.12 `.venv`; final frozen production/test tree; no provider, runtime database or artifact access | Regenerate `tools/api_inventory.py` and `tools/architecture_inventory.py`; run both with `--check`; `PYTHONDONTWRITEBYTECODE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_tool_inventory_modules.py tests/test_config_modules.py tests/test_typing_contract.py tests/test_architecture_boundaries.py tests/test_exception_safety.py`; rerun `tests/test_tool_inventory_modules.py`; full mypy with `--no-incremental`; full Ruff with `--no-cache`; `git diff --check` | README, requirements, design, operations, maintenance and test-plan contract closure; exact single skip-test index; generated API/function drift; documentation/configuration/typing/architecture/exception guards; 254-file typing scope; whitespace integrity | Both generated checks passed; combined guards 82 passed in 61.49s; tool/index guard 14 passed in 39.57s; mypy found no issues in 254 source files; Ruff passed; patch check passed | API inventory: 155 endpoints. Function inventory: app 401 files / 1,009 classes / 6,029 module functions / 1,374 methods / 134,739 lines; tests 230 / 198 / 3,496 / 879 / 113,039; tools 15 / 7 / 152 / 6 / 3,314. The closure changed only the six narrative documents plus generated `API_REFERENCE.md` and `FUNCTION_INVENTORY.md`; production, tests, runtime artifacts and databases were not modified. |
| 2026-08-14 | Formal current-v6/v5 probability-source, immutable-rule, canonical-replay and verified-read release audit | Project Python 3.12 `.venv`; provider-free temporary SQLite fixtures; isolated 5,382-row synthetic publication; production run `#82` opened URI read-only/query-only only | `PYTHONDONTWRITEBYTECODE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_market_scan_snapshot_integrity.py`; the same command over `test_market_scan_query_integrity.py test_market_scan_query_service.py test_market_scan_probability_source.py test_market_scan_probability_capture.py test_market_scan_skip_contract.py test_market_scan_repository.py`; the same command over `test_market_scan_scoring.py test_market_scan_release_boundary_coverage.py tests/test_market_scan_execution.py::test_manager_rejects_old_rule_retry_and_top100_before_preparing_child_runs`; focused repository-structure/tool/typing tests; full Ruff; full mypy; `git diff --check` | Current v5 formula and historical-v4 isolation; immutable exact rule contract; repository-owned publication receipt; two justified skip classes; source-v3/coverage-v2; typed outbox and three-way digest; request-local fused verified read; retry/TOP100 guards; read-only frozen-`#82` nonauthorization | Snapshot/verified-read 47 passed in 64.93s; query/source/capture/skip/repository 185 passed in 22.20s; scoring/publication/version 274 passed in 5.35s; structure/tool/typing 22 passed in 35.54s; independent v4/v5 isolation 9 passed in 0.93s; Ruff passed; mypy found no issues in 254 source files; patch check passed; release audit found no P0/P1 | Three exclusive 5,382-row observations were wall/CPU 3.585304834/3.558246, 4.509010792/4.475519 and 3.942041250/3.915483 seconds; each had hash=1, replay=1, SQL score reread=0, total=5,382 and page items=100, passing CPU&lt;5/wall&lt;12 test bounds. These are regression observations, not SLAs. Run `#82` remained byte-identity unchanged with `total_changes=0`, all five triggers, 5,543 total / 5,490 historical-v4 successes / 53 skipped, matching seal digest, and action eligibility false. No production row, test, runtime artifact or database was modified. |
| 2026-08-13 | Final byte-compatible streaming market-scan seal documentation/inventory closure | macOS, project Python 3.12 `.venv`; provider-free strict-canonical fixtures, temporary SQLite databases and frozen production benchmark evidence | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_market_scan_snapshot_integrity.py`; regenerate `.venv/bin/python tools/architecture_inventory.py`; run API and function inventories with `--check`; `.venv/bin/python -m pytest -q -p no:cacheprovider tests/test_tool_inventory_modules.py tests/test_config_modules.py tests/test_typing_contract.py tests/test_architecture_boundaries.py tests/test_exception_safety.py`; `.venv/bin/python -m mypy --no-incremental`; `.venv/bin/python -m ruff check --no-cache app tests tools`; `git diff --check` | Cursor-streamed strict canonical SHA-256 with `O(max canonical result row)` additional memory; exact materialized digest-v2 byte compatibility for Unicode/nested JSON/floats/order/empty results; no result `fetchall()`; strict malformed JSON closure; no migration/version bump/rewrite/reseal; current generated inventory and exact test index | Implementing slice freeze evidence: 724 passed plus 4 subtests; focused integrity rerun: 35 passed in 3.43s; documentation/configuration/typing/architecture/exception guards: 82 passed in 50.06s; mypy found no issues in 250 source files; Ruff, both generated checks and patch check passed | Frozen production observation: peak RSS about 1.067 GiB → 22 MiB and wall time about 3.65 s → 2.63 s with the same stored digest; this is not an SLA. API inventory remains 155 endpoints. Function inventory: app 394 files / 1,003 classes / 5,894 module functions / 1,338 methods / 130,789 lines; tests 229 / 193 / 3,409 / 849 / 109,327; tools 15 / 7 / 151 / 6 / 3,287. `app/db/market_scan_integrity.py` and `tests/test_market_scan_snapshot_integrity.py` are Goal-owned files even while untracked. This documentation closure did not edit production code, tests, runtime artifacts or databases. |
| 2026-08-13 | Final industry-plate, automatic-scan CPU and query-module structural documentation/inventory closure | macOS, project Python 3.12 `.venv`; provider-free HTTP fixtures, sealed temporary SQLite databases and source/guard inspection | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_data_sources.py tests/test_market_scan_automation.py tests/test_market_scan_scheduler.py tests/test_market_scan_snapshot_integrity.py tests/test_scheduler_modules.py`; `.venv/bin/python -m pytest -q -p no:cacheprovider tests/test_market_scan_repository_structure.py tests/test_typing_contract.py`; regenerate `.venv/bin/python tools/api_inventory.py` and `.venv/bin/python tools/architecture_inventory.py`; run both with `--check`; `.venv/bin/python -m pytest -q -p no:cacheprovider tests/test_tool_inventory_modules.py tests/test_config_modules.py tests/test_typing_contract.py tests/test_architecture_boundaries.py tests/test_exception_safety.py`; `.venv/bin/python -m mypy --no-incremental`; `.venv/bin/python -m ruff check --no-cache app tests tools`; `git diff --check` | One-page/no-redirect/exact-200/size/deadline/shape bounded Eastmoney industry plates without AKShare SDK pagination; no-action lightweight scan header/database/leadership fingerprint; ordinary monitor-write suppression; market-scan identity/five-minute full re-audit; immediate API fail closure; extraction of the scheduler-only non-authorizing row mapper into `market_scan_automatic_state.py` with `market_scan_queries.py` at its 500-line budget; exact test index and current generated inventories | CPU-focused regression: 274 passed plus 17 subtests in 4.85s; final structure/typing focus: 7 passed in 0.23s; documentation/configuration/typing/architecture/exception guards: 82 passed in 48.59s; mypy found no issues in 250 source files; Ruff, both generated checks and patch check passed | API inventory: 155 endpoints. Function inventory: app 394 files / 1,002 classes / 5,893 module functions / 1,337 methods / 130,772 lines; tests 229 / 193 / 3,403 / 849 / 109,168; tools 15 / 7 / 151 / 6 / 3,287. The lightweight scheduler state remains explicitly non-authorizing; unrelated monitor writes do not trigger rehash, while API reads always fully verify. This closure did not edit production code, tests, runtime artifacts or databases. |
| 2026-08-13 | Final restart-stable legacy outcome-drift terminal-catalog documentation/inventory closure | macOS, project Python 3.12 `.venv`; provider-free source inspection and guard fixtures | Regenerate `.venv/bin/python tools/api_inventory.py` and `.venv/bin/python tools/architecture_inventory.py`; run both with `--check`; `.venv/bin/python -m pytest -q -p no:cacheprovider tests/test_tool_inventory_modules.py tests/test_config_modules.py tests/test_typing_contract.py tests/test_architecture_boundaries.py tests/test_exception_safety.py`; `.venv/bin/python -m mypy --no-incremental`; `.venv/bin/python -m ruff check --no-cache app tests tools`; `git diff --check` | Typed drift preserved as a mechanically bound terminal catalog entry; source-bound newest-order due-0/skip/no-K-line/no-outcome/no-fit behavior across restart; strictly newer valid-outcome recovery; hard closure for conflicting/missing identity and ordinary invalid evidence; current inventories and exact test index | 82 guard tests passed in 49.67s; mypy found no issues in 249 source files; API and function inventories regenerated and both `--check` modes passed; Ruff and patch checks passed | Final function inventory: app 393 files / 1,001 classes / 5,878 module functions / 1,327 methods / 130,364 lines; tests 229 / 193 / 3,398 / 843 / 108,760; tools 15 / 7 / 151 / 6 / 3,287. Artifact bytes remain untouched. This focused closure did not edit production code, tests, runtime artifacts or databases. |
| 2026-08-13 | Final legacy probability-outcome semantic-drift compatibility documentation/inventory closure | macOS, project Python 3.12 `.venv`; provider-free source inspection and guard fixtures | Regenerate `.venv/bin/python tools/api_inventory.py` and `.venv/bin/python tools/architecture_inventory.py`; run both with `--check`; `.venv/bin/python -m pytest -p no:cacheprovider tests/test_tool_inventory_modules.py tests/test_config_modules.py tests/test_typing_contract.py tests/test_architecture_boundaries.py tests/test_exception_safety.py`; `.venv/bin/python -m mypy --no-incremental`; `.venv/bin/python -m ruff check --no-cache app tests tools`; `git diff --check` | Exact typed quarantine for an intact outcome-v1 degraded rule-profile semantic drift; direct replay and ordinary mechanical/digest/semantic failures remain closed; projection status/selection/fit exclusion; immutable no-reseal boundary; current API/function inventories and exact test index | 82 guard tests passed in 47.44s; API and function inventories regenerated and both `--check` modes passed; mypy found no issues in 249 source files; Ruff and patch checks passed | The compatibility path leaves retained artifacts byte-for-byte untouched and excludes rather than upgrades their outcomes and dependent fits. This focused docs/inventory closure does not replace full-suite coverage/browser gates and did not edit production code, tests, runtime artifacts or databases. |
| 2026-08-13 | Final full-market action-source, managed-artifact and calendar documentation/inventory closure | macOS, project Python 3.12 `.venv`; provider-free source inspection and guard fixtures | Regenerate `.venv/bin/python tools/api_inventory.py` and `.venv/bin/python tools/architecture_inventory.py`; run both with `--check`; `.venv/bin/python -m pytest -p no:cacheprovider tests/test_tool_inventory_modules.py tests/test_config_modules.py tests/test_typing_contract.py tests/test_architecture_boundaries.py tests/test_exception_safety.py`; `.venv/bin/python -m mypy --no-incremental`; `.venv/bin/python -m ruff check --no-cache app tests tools`; `git diff --check` | Frozen shared action-source authorization; read-only audit separation; cross-process artifact lease/path/batch rollback/stream verification/retention/restore; inclusive listing-session Paper boundary; exact test-module index; generated API/function drift; documentation/configuration/type/architecture/exception guards | 82 guard tests passed in 77.23s; mypy found no issues in 249 source files; API and function inventories regenerated and both `--check` modes passed; Ruff and patch checks passed | Every current `tests/test_*.py` module appears exactly once in the TEST_PLAN domain index. This focused documentation/inventory closure does not replace the full Python coverage or Playwright release gates. No production code, test fixture, runtime artifact or database was modified by this closure. |
| 2026-08-13 | Final full-worktree review-tool trust release gate | macOS, project Python 3.12 `.venv`, supported Node/npm, provider-free temporary state, and loopback-capable execution for Uvicorn/Playwright | `PYTHONDONTWRITEBYTECODE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term --cov-report=json:/tmp/ashare-review-release-cov.json`; `npm run test:e2e`; full Ruff; project-standard mypy; `npm run check:py`; `npm run check:js`; `pip check`; offline production npm audit; both generated-inventory checks; documentation/configuration/typing/tool guards; `git diff --check` | Entire shared worktree after append-only review ledgers, strict PIT/execution evidence, Paper output sealing/CAS, immutable portability, no-store APIs, strict frontend contracts and responsive E2E closure | Python: 3,715 passed plus 66 subtests, 0 failed in 398.19s; exact line-plus-branch coverage 90.19904387167887% with the 90% threshold unchanged; Playwright: 143 passed, 49 intentional project skips, 0 failed across desktop/mobile Chromium, Firefox and WebKit | All test databases and browser fixtures were disposable. Production SQLite was queried only through read-only/immutable connections. Its start SHA-256 `09860490620fbf3b727f51807b34aedbb480493ce285aecd58edf782d6074664` (mtime 07:34:09) and final SHA-256 `1937b122ad70bb8010e5fde86795cabebd2ef7088d811ff05a72353908e12bde` (mtime 10:33:58) differ while size remained 2,129,387,520 bytes; this concurrent byte drift was not attributed to the disposable test runs. Final `quick_check=ok`, `foreign_key_check` was empty, and scoped counts remained advice 496 / plan 1 / result 0 / strategy 0 / run 1 / trade 0. No formal advice, replay evidence or Paper history was fabricated to satisfy this gate. |
| 2026-08-13 | Final review-tool immutable-ledger, PIT, Paper and portability focused gate | macOS, project Python 3.12 `.venv`; provider-free fixtures, Node harnesses and temporary SQLite databases | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_advice_reviews.py tests/test_advice_review_window_contract.py tests/test_active_research_review_backend.py tests/test_api_review_routes.py tests/test_api_paper_trading_routes.py tests/test_api_error_modules.py tests/test_frontend_review_scan.py tests/test_frontend_paper_trading.py tests/test_paper_trading.py tests/test_local_data_portability.py tests/test_runtime_backup.py tests/test_schema_compat.py tests/test_review_modules.py` | Append-only plan/evaluation ledgers and evidence v2; latest-as-of/current-attempt summary; trusted sessions/15:15/suspension; Paper revision CAS/rebind/T+1/output v3/frozen principal/strict browser ownership and count conservation; portability immutable-history, remap and backup contracts | 281 passed and 4 subtests passed in 14.28s | Focused provider-free gate only. It used disposable databases and does not replace the pending full-worktree coverage/browser release gate. |
| 2026-08-13 | Final review-tool documentation, typing and generated-inventory closure | macOS, project Python 3.12 `.venv`; provider-free source inspection | Regenerate `tools/api_inventory.py` and `tools/architecture_inventory.py`; run both with `--check`; `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_tool_inventory_modules.py tests/test_config_modules.py tests/test_typing_contract.py`; `.venv/bin/python -m mypy --no-incremental`; `.venv/bin/python -m ruff check --no-cache app tests tools`; `git diff --check` | README/requirements/design/operations/maintenance/test/research contracts; exact test index and configuration documentation; API/function inventory drift; project typing/lint/patch integrity | 64 guard tests passed in 27.34s; mypy found no issues in 239 source files; both generated checks, Ruff and patch check passed | Generated `API_REFERENCE.md` and `FUNCTION_INVENTORY.md` match the frozen source tree. No full-suite or coverage result is inferred from this row. |
| 2026-08-13 | Third-round individual-research trust release gate | macOS, project Python 3.12 `.venv`, Node/npm project environment; provider-free fixtures and temporary databases; loopback-capable execution for Uvicorn smoke coverage | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-branch --cov-report=term --cov-report=json:/tmp/ashare-individual-round3-final-coverage.json --cov-fail-under=90`; full Ruff; `mypy --no-incremental`; `npm run check:js`; both generated-inventory checks; documentation/typing/function-budget guards; `git diff --check`; four-project `tests/e2e/individual-probability.spec.js` Playwright matrix; post-suite read-only tracked-artifact replay | Workbench-v2 full child/card owner and event-time cohort; interactive-Shadow/formal-advice separation; typed-unavailable research factors, price levels, risk/reward and execution calibration; sensitive individual API generic/no-store failures; current source-v2 full-market/score/time replay; assessment-v4 and strict public probability authorization; cache, Store, calendar, content-address and TOCTOU failure closure | Python: 3,627 passed plus 66 subtests, 0 failed in 350.69s; exact line-plus-branch coverage 90.15% with the 90% threshold unchanged; focused Playwright: 8 passed across desktop/mobile Chromium, Firefox and WebKit; Ruff, mypy (239 files), JavaScript, API/function inventories, documentation/typing/function budgets and patch checks passed | The tracked 8,733-byte assessment-v2 artifact remains audit-readable and unmodified; current-v4 projection is 0/288 with `signal_date=null`, selection false and D+2/D+3/D+4 probabilities null. No historical PIT evidence, production score, rank, formal advice or automatic-promotion rule was fabricated or relaxed. |
| 2026-08-13 | Third-round individual-research documentation, typing and generated-inventory closure | macOS, project Python 3.12 `.venv`; provider-free source inspection | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest tests/test_tool_inventory_modules.py tests/test_typing_contract.py tests/test_exchange_calendar_contract.py tests/test_individual_probability_store_failures.py -q -p no:cacheprovider`; `PYTHONNOUSERSITE=1 .venv/bin/python -m mypy --no-incremental`; regenerate and check `tools/api_inventory.py` plus `tools/architecture_inventory.py`; focused Ruff; `git diff --check` | Assessment-v4/current-source-v2, public probability, workbench child/time/formal-advice, typed-unavailable and sensitive-API documentation; exact Python test index; explicit typing scope and fail-closed calendar/store branches; generated API/function drift | 35 focused tests passed in 29.47s; mypy found no issues in 239 source files; both generated checks, focused Ruff and patch check passed | Focused quality result only, not a replacement for the root task's full Python/coverage/browser release gate. Historical v3 and 2/288 rows below are superseded checkpoint evidence; current tracked projection is 0/288. |
| 2026-08-13 | Second-round final full-market score/probability trust release gate | macOS, project Python 3.12 `.venv`, Node/npm project environment; provider-free fixtures and temporary databases; loopback-capable execution for Uvicorn smoke coverage | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term --cov-report=json:/tmp/ashare-round2-final-v2-coverage.json --cov-fail-under=90`; full Ruff; `mypy --no-incremental`; `npm run check:js`; both generated-inventory checks; documentation/typing/function-budget guards; `git diff --check`; focused four-project probability/individual-probability/executable-Shadow Playwright matrix; post-suite read-only frozen snapshot/artifact replay | Shared frozen-snapshot and TOCTOU integrity; PIT-v3 identity/score/session evidence; distribution-v4 components; deterministic strategy refill and persistence seals; authorization/deployment/joint-v2 fail-closed boundaries; explicit probability intervals; legacy source-v1 audit compatibility; API/Excel/browser error closure | Python: 3,416 passed plus 64 subtests, 0 failed in 329.63s; exact line-plus-branch coverage 90.0312281268508% with the 90% threshold unchanged; Ruff, mypy (237 files), JavaScript, API/function inventories, documentation/typing/function budgets and patch checks passed; focused Playwright: 18 passed and 2 intentional Chromium-only skips | Superseded historical checkpoint. Read-only replay accepted complete runs 71/77 as 5,542 rows (5,499 success/43 skipped) and 5,543 rows (5,494 success/49 skipped), respectively. Their formal H1/H5/H20 sources remained `legacy_unbound`/`insufficient_data`. Its then-current individual projection reported 2/288 but all D+2/D+3/D+4 values were null; current v4 semantics project the exact same tracked v2 artifact as 0/288. Production score-v4 ranking and automatic promotion were not changed. |
| 2026-08-13 | Second-round full-market score/probability trust closure and documentation inventory | macOS, project Python 3.12 `.venv`, Node/npm project environment; provider-free fixtures and temporary databases | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_joint_execution_probability_contract.py tests/test_market_scan_snapshot_integrity.py tests/test_market_scan_query_integrity.py tests/test_market_scan_feature_windows.py tests/test_market_scan_probability.py tests/test_market_scan_probability_artifact.py tests/test_market_scan_probability_capture.py tests/test_market_scan_probability_source.py tests/test_market_scan_probability_source_research.py tests/test_market_scan_query_service.py tests/test_strategy_execution.py tests/test_strategy_evidence.py tests/test_strategy_automation.py`; inventory/typing guard pair; full Ruff; `mypy --no-incremental`; `npm run check:js`; both generated-inventory checks; `git diff --check` | Frozen-snapshot/TOCTOU/generic-409 integrity; PIT-v3/session-coverage/distribution-v4 score evidence; strict authorization/deployment/joint-v2 null boundary and explicit intervals; source-v1 PIT-v2 compatibility without source-v2 relaxation; strategy freshness/refill/evidence/simulation seals; documentation test index, generated API/function inventories and explicit typing ratchet | Cross-slice regression: 301 passed in 7.30s; documentation/typing/function-budget guards: 18 passed in 28.63s; mypy: 237 source files clean; Ruff, JavaScript, both inventory checks and patch check passed | This is a focused trust-boundary and quality run, not a replacement full-suite coverage claim. Current executable-only conditional evidence cannot authorize the all-decisions joint estimand; joint v2 remains a permanent unavailable skeleton, there is no verified deployment artifact, and formal run/per-row/new-prediction probabilities remain null. Production score v4 formula and automatic-promotion policy were not changed. |
| 2026-08-13 | Final trustworthy full-market scoring and probability hardening worktree | macOS, project Python 3.12 `.venv`, Node 24.14.1/npm 11.11.0; provider-free fixtures and temporary databases; explicitly loopback-capable environment for the complete suite | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term --cov-report=json:/tmp/ashare-final-audit-v2-coverage.json --cov-fail-under=90`; full Ruff; ratcheted mypy; `npm run check:js`; both generated-inventory checks; documentation/typing/function-budget guards; `git diff --check`; focused four-project probability/individual-probability/executable-Shadow Playwright matrix | Shared 61-bar and mode-aware score windows; base/integer/final score-distribution diagnostics; H+1 probability labels, split and filter authorization; exact run/score-contract binding; safe preload and oversized legacy handling; D+2/D+6/D+21 plus calibration-bias semantics; estimator-bound individual assessment v2; read-only executable-candidate Shadow; strict artifact, API, export and browser failure closure | Python: 3,318 passed plus 64 subtests, 0 failed in 320.24s; exact line-plus-branch coverage 90.3876725700739% with the 90% gate unchanged; Ruff, mypy (233 files), JavaScript, API/function inventories, documentation/typing/function budgets and patch checks passed; focused Playwright: 18 passed and 2 intentional Chromium-only skips | Superseded historical checkpoint. Production v4 row/rank digests for runs 71 and 77 remained unchanged; formal H1/H5/H20 values stayed null. Its then-current individual projection reported 2/288 with all D+2/D+3/D+4 values null; current v4 semantics project the same 8,733-byte tracked v2 artifact as 0/288. No coverage threshold, production rank or automatic-promotion rule was relaxed. |
| 2026-08-12 | Superseded pre-estimator-binding individual D+2/D+3/D+4 Shadow probability checkpoint | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0; provider-free fixtures plus an explicitly loopback-capable Playwright environment | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_individual_probability.py tests/test_api_stock_routes.py tests/test_frontend_individual_probability.py tests/test_tool_inventory_modules.py tests/test_typing_contract.py`; `PYTHONNOUSERSITE=1 .venv/bin/python -m mypy`; focused Ruff; `npm run check:js`; regenerate and check both inventories; `git diff --check`; `npx playwright test tests/e2e/individual-probability.spec.js` | Earlier exact daily-K proxy target, typed null/display gates, artifact/store/API/UI failure closure, official source provenance and verified-byte isolation | Focused Python/quality group: 74 passed in 28.05s; mypy: 227 source files clean; Ruff, JavaScript, generated inventories and patch checks passed; Playwright: 8 passed in 9.5s | Superseded checkpoint only. Assessment v3 was later introduced and is now itself superseded by current v4; the tracked v2 projection remains 0/288 with `signal_date=null`. Use the newest individual-research row for operational decisions. |
| 2026-08-12 | Maintainability audit and second incremental composition/type/contract/E2E refactor | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0; provider-free temporary databases and local static Playwright server | `.venv/bin/python -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term --cov-report=json:/tmp/ashare-maintainability-round2.json --cov-fail-under=90`; `.venv/bin/python -m ruff check app tests tools`; `.venv/bin/python -m mypy --no-incremental`; `npm run check:js`; both generated-inventory checks; `git diff --check`; four-project `tests/e2e/market-scan-flow.spec.js` plus shared-navigation and ScreenSpec parity regressions | Composition-root service ownership, readonly Protocol compatibility, 214-file explicit app typing ratchet, total/source/target private dependency guards, disposable browser lifecycle, strict Python↔JavaScript `ScreenSpecV2` parity, ordinal-calibration extraction with fixed digests/module budgets, and full-market/Discovery E2E ownership split without increasing its pre-split line budget | Python: 3,087 passed plus 64 subtests in 307.65s; exact line-plus-branch coverage 90.0083% with the existing 90% gate unchanged; focused architecture/type/evaluation/static group: 70 passed; Ruff passed; mypy passed for 223 source files; JavaScript/inventory/diff checks passed; moved-flow Playwright: 22 passed and 18 intentionally skipped across desktop/mobile Chromium, desktop Firefox and desktop WebKit, plus 4 shared-navigation passes | No SQLite schema, production ranking, probability/research semantics, runtime data or user files were changed. `frontend-flow.spec.js` is 1,499 lines and `market-scan-flow.spec.js` 1,229, totaling 2,728 versus the former 2,747; budgets were lowered rather than relaxed. The audit found no P0/P1 blocker; ordered remaining work and explicit non-goals are recorded in `docs/research/ENGINEERING_MAINTAINABILITY_AUDIT_2026_08_12.md`. |
| 2026-08-12 | Optimized trustworthy full-market screening v1 against a verified static runtime backup | macOS, project Python 3.12 `.venv`; `/private/tmp/ashare-screening-bench-20260812/runtime.sqlite3` from a verified `runtime_data.py` backup; each mode ran in a separate process with no provider/network access | `PYTHONNOUSERSITE=1 .venv/bin/python tools/runtime_data.py verify /private/tmp/ashare-screening-bench-20260812`; `PYTHONNOUSERSITE=1 PYTHONPATH=. .venv/bin/python /private/tmp/benchmark_trusted_screening_optimized.py legacy`; `PYTHONNOUSERSITE=1 PYTHONPATH=. .venv/bin/python /private/tmp/benchmark_trusted_screening_optimized.py sequential`; `PYTHONNOUSERSITE=1 PYTHONPATH=. .venv/bin/python /private/tmp/benchmark_trusted_screening_optimized.py concurrent`; then repeat backup verification | Real published `official` full-market run `#71`; full-hydration semantic compatibility; five-column breadth; scalar screening plus page/near-only detail hydration; sequential/concurrent latency and process peak RSS; source immutability | Backup `runtime.sqlite3` SHA-256 `3a77a2ad0885aae10f37dfd26c239b64047d7ecf7de52c6380052e096d23015b` verified before/after. Legacy/full-hydration, optimized sequential and optimized concurrent modes all returned evaluation digest `30868975646c26e368cf1043f72a420c3409f1f4c1296cdf8fd03ba3be705cd8`; breadth digest remained `6eb5a0d8d9587b59fd2af88db99cb98c2cd262734cfa6759a541c40a4b13cd5f`. Optimized sequential: 0.2378 s, 70.23 MiB peak RSS, +37.41 MiB over baseline. Optimized concurrent: 0.2526 s, 66.27 MiB peak RSS, +32.80 MiB over baseline. | Every mode reported population 5,542, matches 691, hydrated page 100 and near misses 20. The prior concurrent full-population hydration/JSON-deserialization path was about 3.17 s with 1,110.5 MiB peak RSS. The `/private/tmp` script was a one-time read-only probe, not a repository tool or durable benchmark API; measurements are observations, not SLAs. |
| 2026-08-11 | Attested probability PIT capture plus isolated longer-history/replay backfill worktree | macOS, project Python 3.12 `.venv`; verified static `runtime_data.py` backup; explicitly network-enabled Tencent qfq acquisition only | `$PYTHON tools/backfill_market_scan_probability_history.py --source-database "$VERIFIED_BACKUP/runtime.sqlite3" --target-database data/research/market_scan_probability_history/tencent-qfq-360-20260811-attested.sqlite3 --output-dir data/research/market_scan_probability_history --symbol-limit 120 --timeout 15`; then `$PYTHON tools/backfill_market_scan_probability_replay.py --database data/research/market_scan_probability_history/tencent-qfq-360-20260811-attested.sqlite3 --output-dir data/research/market_scan_probability_replay --start-date 2025-05-21 --end-date 2026-07-13 --symbol-limit 96 --report data/research/market_scan_probability_replay/summary-20260811-attested-v2.json` | Fail-closed backup/market coverage, immutable 360-session history publication, sidecar-free SQLite `immutable=1` 279-date H1/H5/H20 replay, registered split/calibration gates, and non-official provenance | The 90-symbol probe failed Beijing coverage after about 19.2s and published no bundle. The attested 120-symbol run succeeded in about 28.6s with source backup verified before/after, 96 accepted symbols, 34,560 bars, `quick_check=ok`, and an 8,847,360-byte database; final replay succeeded in about 87.2s with 26,784 records, 100% label coverage, and a 68,553,345-byte artifact. | History manifest v2 `b30942ef…` and replay digest `045a11cb…` are the retained current evidence; `abe35f65…` is retained as `legacy_attested_v1`, `7fe6164b…` as `legacy_unattested`, and `118fc869…` plus earlier replay files are legacy only. All horizons had 279 dates and one 120/40/60 fold but remained `insufficient_data`/`probability=null`: their sparsest calibration bins cover only 5/5/3 independent dates against the 20-date floor. H5 AUC 0.4938, Brier Skill -0.00978, ECE 0.0980, non-monotonic bins, and no highest-bin lift show that the backfill did not validate the model; official/live cohorts and production ranks remain untouched. |
| 2026-08-11 | Final attested probability-source/history/replay, documentation, typing ratchet, and generated inventories | macOS, project Python 3.12 `.venv`, Node/npm project environment | `$PYTHON -m pytest -q -p no:cacheprovider <probability source/capture/source-research/history/replay plus market-scan frontend modules>`; `$PYTHON -m pytest -q -p no:cacheprovider <inventory/config/typing modules>`; `$PYTHON -m pytest -q -p no:cacheprovider <architecture-boundary module>`; `$PYTHON -m ruff check app tests tools`; `$PYTHON -m mypy`; `npm run check:js`; both generated-inventory checks; `git diff --check` | Source retention/restart semantics, verified-backup/WAL closure, immutable/sidecar-free replay, UI null states, documentation index/portability, architecture, lint, 179-file typing scope, JavaScript syntax, generated-document drift, and whitespace integrity | Functional regression: 55 passed in 6.08s; documentation/config/typing guards: 63 passed in 21.47s; architecture: 4 passed in 5.36s; all static, JavaScript, generated-inventory, and patch checks passed | The current loaders also deep-opened the attested history and final replay artifacts as 34,560 and 26,784 records; no production rank or official probability was populated. |
| 2026-08-11 | Final future-range implementation plus complete repository regression | Managed workspace Python 3.12 `.venv`; ordinary sandbox followed by an explicitly loopback-capable environment only for the two server smoke cases | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider`; then `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_uvicorn_smoke.py` in the loopback-capable environment | Entire provider-free Python suite, followed by isolated real-Uvicorn startup/static/API/SSE shutdown verification | Full run: 2,810 passed and 64 subtests passed; 2 Uvicorn cases failed solely on sandbox bind `EPERM`. Authorized loopback rerun: 2 passed | The environment-only bind denial was not treated as a product failure; both affected tests passed unchanged where loopback binding was permitted. |
| 2026-08-11 | Same final future-range integration worktree | Project Python 3.12 `.venv`, temporary SQLite and frozen fixtures | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_market_scan_future_range.py tests/test_market_scan_future_range_artifact.py tests/test_market_scan_future_range_store.py tests/test_market_scan_api.py tests/test_market_scan_export.py tests/test_frontend_market_scan_future_range.py tests/test_market_scan_frontend.py tests/test_frontend_market_scan_reliability.py tests/test_static_assets.py tests/test_trading_calendar_modules.py` | Engine/artifact/store/API/export/UI/calendar integration and existing market-scan regressions | 161 passed | Includes official/TOP100 eligibility, no-store API, semantic artifact tamper closure, null handling, execution-aware export/UI, asset wiring, and trusted future-session mapping. |
| 2026-08-11 | Real canonical official run 70 after transaction-snapshot concurrency handling | Live local SQLite opened `mode=ro`/query-only inside an explicit read transaction; separate temporary artifact directory | `.venv/bin/python tools/evaluate_market_scan_future_range.py --database data/ashare_radar.sqlite3 --output-dir /tmp/ashare-future-range-run70 --run-id 70 --minimum-sample-size 30 --minimum-session-count 20 --complete-run-coverage 0.95 --bootstrap-samples 1000` | Real 5,499-row point-in-time evidence, trusted fixed 2026-08-12/13/14 targets, artifact persistence, and live-writer isolation | Command completed; one 5,499-record artifact loaded and replayed; 0 exclusions; evidence coverage 100%; all three offset coverages 0%; `insufficient_data`; pre/post database SHA-256 matched | Target sessions were not yet ingested, so all outcomes correctly remained `not_mature` rather than being shifted or filled with zero; artifact digest starts `c6445b37…`. |
| 2026-08-11 | Final future-range code, documentation, mypy ratchet, and regenerated inventories | macOS, project Python 3.12 `.venv`, Node/npm project environment | `PYTHONNOUSERSITE=1 .venv/bin/python -m ruff check app tests tools`; `PYTHONNOUSERSITE=1 .venv/bin/python -m mypy`; `npm run check:js`; `PYTHONNOUSERSITE=1 .venv/bin/python tools/api_inventory.py --check`; `PYTHONNOUSERSITE=1 .venv/bin/python tools/architecture_inventory.py --check`; `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_tool_inventory_modules.py tests/test_typing_contract.py`; `git diff --check` | Full lint, 172-file typing scope, JavaScript syntax, generated API/function drift, documentation/test-index/function-budget/typing ratchets, and whitespace integrity | All static/generated checks passed; final rerun had 18 focused quality tests passed in 38.72s; mypy checked 172 source files | Every current Python test module is indexed exactly once; API reference includes `include_research` and the `record_page` wrapper. |
| 2026-08-11 | Fixed-session future-range backend, artifact, store/API/export, calendar, and frontend-contract worktree | macOS, project Python 3.12 `.venv`, temporary SQLite databases and frozen fake evidence | `PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q -p no:cacheprovider tests/test_market_scan_future_range.py tests/test_market_scan_future_range_artifact.py tests/test_market_scan_future_range_store.py tests/test_market_scan_export.py tests/test_frontend_market_scan_future_range.py tests/test_trading_calendar_modules.py` | Official-only D+1/D+2/D+3 engine, deep artifact replay/tamper closure, trusted calendar, read-only API/store, execution-aware XLSX, and static frontend integration | 70 passed in 2.45s | No live providers or runtime database writes; includes null/insufficient, T+1, H=1 compatibility, qfq overlap, semantic reseal rejection, and database-byte invariance. |
| 2026-08-11 | Future-range execution-evidence frontend worktree | Node/npm project environment plus Python 3.12 `.venv` | `npm run check:js && .venv/bin/python -m pytest tests/test_frontend_market_scan_future_range.py tests/test_market_scan_frontend.py::test_market_scan_frontend_contract_is_wired_into_workspace tests/test_static_assets.py::test_frontend_js_functions_stay_small_enough_to_review tests/test_market_scan_frontend.py::test_market_scan_modules_have_explicit_reviewable_boundaries -q` | JavaScript syntax, lazy fixed-session panel, separate MFE/MAE versus executable net/net-excess rendering, workspace wiring, and frontend size/boundary budgets | JavaScript check passed; 5 passed in 0.30s | D+1 unavailable and D+2/D+3 modelled execution values retain blank-null behavior. |
| 2026-08-11 | Same future-range browser worktree | Playwright desktop and mobile Chromium projects | `npx playwright test tests/e2e/market-scan-future-range.spec.js --project=desktop-chromium --project=mobile-chromium` | Lazy request ownership, D+1/D+2/D+3 switching, group/detail/probability evidence, executable net/net-excess values, null states, and responsive bounds | 4 passed in 3.1s | Two desktop and two mobile scenarios; production leaderboard order remains outside this independent research panel. |
| 2026-08-11 | Full-market Shadow probability frontend integration worktree | Playwright desktop/mobile Chromium, desktop Firefox, and desktop WebKit projects | `npx playwright test tests/e2e/market-scan-probability.spec.js` | Default 5-session net-excess evidence, 1/5/20 switching, calibrated-only filters, null insufficient states, immutable production ranks, frozen audit evidence, and responsive bounds | 4 passed in 9.8s | Exercises the probability UI itself rather than relying on static source assertions. |
| 2026-08-11 | Full-market score semantics plus audited probability research-v3/label-v2 worktree | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0 | `$PYTHON -m pytest -q` | Complete provider-free Python regression suite, including production-v4 preservation, cohort isolation, multi-fold OOS calibration, label-profile failure closure, per-result artifact persistence/replay, API/filter/export parity, frontend contracts, and architecture budgets | 2762 passed and 64 subtests passed in 106.27s | Production v4 remains unchanged; probability evidence is insufficient and no candidate is promoted automatically. |
| 2026-08-11 | Same final code and generated-inventory worktree | macOS, project Python 3.12 `.venv` | `$PYTHON -m ruff check app tests tools`; `$PYTHON -m mypy`; `$PYTHON -m pip check`; `$PYTHON tools/architecture_inventory.py --check`; `$PYTHON tools/api_inventory.py --check`; `git diff --check` | Formatting/lint, ratcheted typing, locked-environment compatibility, generated architecture/API drift, and whitespace integrity | All checks passed; mypy checked 162 source files and pip reported no broken requirements | Uses the project `.venv`; unrelated global Python packages are outside this verification boundary. |
| 2026-08-11 | Same final frontend worktree | Playwright desktop/mobile Chromium, desktop Firefox, and desktop WebKit projects | `npx --no-install playwright test tests/e2e/frontend-flow.spec.js --grep "full-market scan runs in background and renders a bounded responsive snapshot"` | Focused full-market score semantics, research-dimension filters, frozen evidence, responsive table, export parity, and current-analysis boundary | 4 passed in 33.8s | Covers all configured browser engines/device classes for the upgraded full-market path. |
| 2026-08-01 | Final StrategySpec audit worktree after executable weighting, buy/hold hysteresis, source-completeness, reproducibility, archive-safety, simulation-plan readback, and complete editor/history lifecycle fixes | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0 | `$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing` | Full Python suite and branch coverage across the strategy laboratory plus all existing application regressions | 2675 passed and 64 subtests passed in 211.76s; 90.09% coverage | Coverage gate is 90%; includes deterministic equal/risk-adjusted/custom allocation, held/new admission thresholds, immutable replay-selector parity, archived lifecycle guards, and provider-free evidence tests. |
| 2026-08-01 | Same final audited code worktree | macOS, Python 3.12, Node 24.14.1, npm 11.11.0 | `npm run check` | Runtime contract, Python compile/pyflakes, JavaScript syntax, and complete Python suite | 2675 passed and 64 subtests passed in 107.06s | Confirms the repository's packaged quality command against the final strategy-laboratory implementation. |
| 2026-08-01 | Same final browser worktree after correcting compile-state recovery and stale execution ownership | Playwright desktop/mobile Chromium, desktop Firefox, and desktop WebKit projects | `npm run test:e2e` | Full responsive browser regression across all configured projects | 105 passed, 43 intentional project/device-exclusive skips in 2.8m | Also followed by an authenticated real-browser StrategySpec v1/v2 compile, diff, point-in-time execution, paper simulation, evidence refresh, and 390x844 no-overflow verification with no console errors. |
| 2026-08-01 | Evidence-first StrategySpec/portfolio/evidence/lifecycle laboratory worktree | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0 | `$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing` | Full Python suite and branch coverage, including immutable strategies, point-in-time execution, A-share portfolio constraints, offline evidence provenance, automation, portability, API/frontend contracts, and existing regressions | 2671 passed and 64 subtests passed in 192.95s; 90.04% coverage | Coverage gate is 90%; no live provider, credential, LLM, or broker dependency. |
| 2026-08-01 | Same final code worktree | macOS, Python 3.12, Node 24.14.1, npm 11.11.0 | `npm run check` | Runtime contract, Python compile/pyflakes, JavaScript syntax, and complete Python suite | 2671 passed and 64 subtests passed in 96.57s | Confirms the repository's packaged local quality command after the strategy-laboratory integration. |
| 2026-08-01 | Same final browser worktree after synchronizing the second full-market ARIA live region assertion | Playwright desktop/mobile Chromium, desktop Firefox, and desktop WebKit projects | `npm run test:e2e` | Full responsive browser regression across all configured projects | 105 passed, 43 intentional project/device-exclusive skips in 2.6m | The first matrix run exposed only the obsolete one-live-region count; all four affected projects passed after the contract was updated to the two independent scan/strategy status regions. |
| 2026-07-29 | Current full-market explainability/history/filter/observability/evaluation worktree | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0 | `npm run check` | Runtime declarations, Python compile/pyflakes, JavaScript syntax, full Python suite, architecture/typing/documentation guards, and provider-free temporary databases | 2562 passed and 64 subtests passed in 84.79s | Covers mode isolation, frozen evidence, advanced shared filters/sorts, revisioned discovery and bulk queue, observability, batched K-line cache reads, read-only evaluation, and preserved transaction/module-size boundaries. |
| 2026-07-29 | Same current worktree | macOS, Playwright Chromium desktop and Pixel 7 projects | `npm run test:e2e` | Full desktop/mobile browser regression, including mode/history binding, frozen/current evidence separation, progress diagnostics, advanced discovery editing, and page/all-filtered bulk queue | 55 passed, 15 intentional device-exclusive skips in 1.8m | All 70 project cases completed; skips are the expected device-exclusive scenarios. |
| 2026-07-29 | Current shared worktree with top-level work areas, official/intraday full-market modes, and filtered XLSX leaderboard export | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0, Asia/Shanghai host timezone, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing` | Full Python suite with branch coverage, including XLSX parsing, formula-text safety, route parity, and real Uvicorn loopback smoke tests | 2539 passed and 64 subtests passed in 237.35s; 90.82% coverage | Coverage gate is 90%; `market_scan_export.py` reached 98% and the complete suite remained credential/provider independent. |
| 2026-07-29 | Same current worktree | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0 | `npm run check` | Runtime declaration, Python compile/pyflakes, JavaScript syntax, and full pytest suite | 2539 passed and 64 subtests passed in 94.83s | Confirms the documented local quality command after frontend/backend export integration. |
| 2026-07-29 | Same current worktree | macOS, Playwright Chromium desktop and Pixel 7 projects | `npm run test:e2e` | Full responsive browser regression, including top-level navigation and filtered Excel download | 51 passed, 13 intentional device-exclusive skips in 1.5m | Confirms download filename/query parameters/busy state plus all existing responsive, chart, discovery, recovery, and notification workflows. |
| 2026-07-28 | Provider-risk hardening worktree with capability-filtered priority overrides, bounded full-market preflight, persisted automatic retry, adaptive provider-pressure control, independent canary timeouts, and synchronized documentation | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0, Asia/Shanghai host timezone, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing` | Full Python suite with branch coverage, including real Uvicorn loopback smoke tests | 2417 passed and 64 subtests passed in 187.24s; 91.13% coverage | Covers provider capability filtering, SH/SZ/BJ preflight, restart-safe retry cadence, cancellation exclusions, AIMD concurrency/backoff, low-cardinality diagnostics, scheduler due-state reporting, dedicated stock-pool canary timeout, and existing application regressions. |
| 2026-07-28 | Same provider-risk hardening worktree | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0 | `npm run check` | Runtime declaration, Python compile/pyflakes, JavaScript syntax, and full pytest suite | 2417 passed and 64 subtests passed in 144.03s | Confirms the documented local quality command after integration and documentation synchronization. |
| 2026-07-28 | Same provider-risk hardening worktree after installing the Playwright 1.61.1 Chromium runtime | macOS, Playwright Chromium desktop and mobile projects | `npm run test:e2e` | Full responsive browser regression, including discovery, scan/retry/recovery, charts, accessibility, online recovery, and cross-page notifications | 45 passed, 9 intentional device-exclusive skips in 1.1m | The first invocation was an environment-only failure before test execution because the matching Chromium build was absent; the browser runtime was installed and the unchanged suite then passed. |
| 2026-07-28 | Research-loop worktree with historical evidence isolation, active watchlist refresh, structured review evaluation, score replay/publication gates, saved discovery presets, and frontend reliability fixes | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0, Asia/Shanghai host timezone, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing` | Full Python suite with branch coverage, including real Uvicorn loopback smoke tests | 2360 passed and 64 subtests passed in 206.01s; 91.21% coverage | Covers immutable historical evidence, provenance-aware advice refresh/review, score-spec hash and replay, SH/SZ/BJ publication gates, resumable deadlines, discovery CRUD/rank changes/queue provenance, capability labels, and maintainability guards. |
| 2026-07-28 | Same research-loop worktree after browser-regression fixes | macOS, Playwright Chromium desktop and mobile projects | `npm run test:e2e` | Full responsive browser regression, including explicit 360/390/430/desktop discovery layouts | 45 passed, 9 intentional device-exclusive skips in 1.0m | Confirms scan re-entry and global-progress ownership, historical read-only evidence, online retry targets, compact mobile header/focus order, discovery workflows, charts, notifications, and no narrow-screen overflow. |
| 2026-07-28 | Same final worktree | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0 | `npm run check` | Runtime declaration, Python compile/pyflakes, JavaScript syntax, and full pytest suite | 2360 passed and 64 subtests passed in 88.49s | Confirms the documented local quality command after all browser-regression fixes and documentation updates. |
| 2026-07-27 | Final engineering-quality convergence worktree with isolated Security tooling and symmetric scan/retry publication guards | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0, Asia/Shanghai host timezone, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing` | Full Python suite with branch coverage, including real Uvicorn loopback smoke tests | 2272 passed and 64 subtests passed in 156.91s; 91.51% coverage | Includes fixed-width UTC migration, configurable legacy timezone, old-bundle import normalization, frontend Shanghai rendering, SLI sample semantics, strict canary contracts, architecture guards, provider-free security-lock contracts, and retry prevention before daily bars are publishable. |
| 2026-07-27 | Same final worktree | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0 | `npm run check` | Runtime declaration, Python compile/pyflakes, JavaScript syntax, and full pytest suite | 2272 passed and 64 subtests passed in 91.94s | Confirms the documented local quality command in the default host timezone. |
| 2026-07-27 | Same final worktree | macOS, Playwright Chromium desktop and Pixel 7 projects | `npm run test:e2e` | Full responsive browser regression | 33 passed, 5 intentional device-exclusive skips in 49.0s | Confirms request ownership, scan lifecycle, charts, research activity, responsive layout, online recovery, and cross-page notification coordination. |
| 2026-07-24 | Engineering-quality convergence worktree and synchronized documentation | macOS, project Python 3.12 `.venv`, Node 24.14.1, npm 11.11.0, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider <architecture, clock, exception-safety, provider-canary, reliability, supply-chain, and typing-contract modules>` | Runtime-independent engineering contracts with fake providers and temporary SQLite | 53 passed in 10.31s | Confirms dependency direction/acyclicity, UTC/Shanghai/monotonic semantics, legacy audit migration, reviewed cancellation boundaries, temporary-DB quote/five-row-daily-K/stock-pool canary behavior, SLO math/cardinality, supply-chain workflow contracts, and non-shrinking mypy scope. |
| 2026-07-24 | Same current worktree after engineering documentation update | macOS, project Python 3.12 `.venv` | `$PYTHON -m pytest -q -p no:cacheprovider tests/test_tool_inventory_modules.py tests/test_config_modules.py tests/test_supply_chain.py` | Documentation path/index/configuration plus supply-chain guardrails | 65 passed in 15.79s | Confirms every current Python test module appears exactly once in the test index, all app/tool `ASHARE_RADAR_*` variables are documented, no machine-specific path is embedded in guarded documentation, and Security declares runtime/dev audits, redacted current/history scans, reproducible SBOMs, SHA-pinned actions, and no persisted checkout credentials. |
| 2026-07-22 | Final provider-reliability and full-market recovery worktree | macOS, project Python 3.12 `.venv`, Node 22.23.1, `TZ=UTC`, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing --cov-report=xml` | Full Python suite with branch coverage under the GitHub quality-job runtime contract | 2177 passed in 95.27s; 91.67% coverage | Coverage gate is 90%; includes host-timezone-independent cache fixtures, explicit notification-lock test doubles that do not depend on Node 24 globals, provider-chain state/capacity, Tencent/Sina SH/SZ/BJ daily-K fallback, zero-volume and stale suspended-stock handling, bulk-response truncation protection, pending-only recovery, cancellation/write transactions, real Uvicorn/SSE shutdown, multiprocess coordination, and persistence regressions. |
| 2026-07-22 | Same final worktree | macOS, project Python 3.12 `.venv` | `npm run check` | Python compile, pyflakes, JavaScript syntax, and full pytest suite | 2177 passed in 64.10s | Confirms the convenient local quality command against the final source and test inventory. |
| 2026-07-22 | Current shared worktree after provider-chain pending/retry documentation and Tencent/Sina daily-K reliability changes | macOS, project Python 3.12 `.venv`, fake providers/HTTP clients | `$PYTHON -m pytest -q tests/test_config_modules.py tests/test_sina_client.py tests/test_datahub_runtime_modules.py <targeted daily-K, market-scan outage/recovery, and Tencent endpoint tests>` | Reliability-focused configuration, provider-chain, fallback, and scan regression subset | 135 passed in 1.60s | No outbound network; confirms exact environment-variable documentation, chain-state classification, false-missing prevention, pending-only recovery, safe Sina `qfq` parsing, and the Tencent SH/SZ/BJ endpoint contract. |
| 2026-07-19 | Current worktree after stock-pool atomicity, structured degradation, scan/task transaction recovery, unified quiescent runtime leadership, bounded retention/backup leases, module splits, frontend recovery, and runtime-lock hygiene | macOS, isolated locked Python 3.12 environment, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing --cov-report=xml` | Full Python suite with branch coverage | 2000 passed in 83.28s; 91.57% coverage | Coverage gate is 90%; includes real Uvicorn/SSE shutdown, multiprocess leadership/backup, 5,500-symbol retention, and scan consistency regressions. |
| 2026-07-19 | Same current worktree | macOS, isolated locked Python 3.12 environment, `PYTHONNOUSERSITE=1` | `npm run check` | Python compile, pyflakes, JS syntax, full pytest suite | 2000 passed in 52.69s | Confirms the convenient local regression command against the final source and test inventory. |
| 2026-07-18 | Audited full-market baseline with SH/SZ/BJ background scanning, per-market pool guards, deterministic ranking, immutable derived retry, explicit fallback degradation, persistence, API, and frontend workspace | macOS, isolated locked Python 3.12 environment, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing` | Full Python suite with branch coverage | 1899 passed in 66.91s; 91.59% coverage | Coverage gate is 90%; predates the current runtime-leadership, structured-degradation, retention, and module-split changes. |
| 2026-07-18 | Audited baseline after review-plan deletion, alert id-cursor/retention alignment, notification enable/disable and bounded backlog draining, scheduler degraded outcomes, and metadata freshness diagnostics | macOS, isolated locked Python 3.12 environment, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing` | Full Python suite with branch coverage | 1787 passed in 58.24s; 91.92% coverage | Coverage gate is 90%; no live provider, credential, persistent runtime-data, or outbound-network dependency. |
| 2026-07-17 | Current shared worktree after review/scan time ownership, local-data import/cleanup concurrency, alert readback, unread, notification, security, and provider-runtime hardening | macOS, isolated locked Python 3.12 environment, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider --cov=app --cov=tools --cov-report=term-missing` | Full Python suite with branch coverage | 1759 passed in 112.01s; 91.87% coverage | Coverage gate is 90%; installed from `requirements-dev-lock.txt` with `--require-hashes`; no live provider, credential, persistent runtime-data, or outbound-network dependency. |
| 2026-07-16 | Historical source after autocomplete, chart inspection, and local activity; before the current feature set | macOS, Python 3.12, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider` | Full Python suite without coverage | 1633 passed in 35.06s | Retained as a baseline, not a result for the current shared worktree. |
| 2026-07-15 | Historical baseline before the current three frontend additions | macOS, Python 3.12, `PYTHONNOUSERSITE=1` | `$PYTHON -m pytest -q -p no:cacheprovider` | Full Python suite without coverage | 1608 passed in 33.58s | Does not include the four new Python frontend test modules indexed above. |
| 2026-07-10 | Local dirty worktree during state-consistency hardening | macOS, project Python 3.12, `PYTHONNOUSERSITE=1` | `npm run check` | Python compile, pyflakes, JS syntax, full pytest suite | 1142 passed | Historical regression record retained for traceability. |

Recent targeted checks kept for traceability:

| Date | Command | Scope | Result | Why It Was Run |
| --- | --- | --- | --- | --- |
| 2026-07-29 | Live `GET /api/market-scans/18/export.xlsx` plus `openpyxl` readback; repeated with BJ/quality/score filters and compared against paged JSON results | Real persisted 5,532-symbol intraday snapshot, XLSX transport/structure, complete filtered row set, and sort parity | 5,482 successful rows exported as a 1,122,946-byte workbook in 2.23s; two sheets, 30 columns, zero formula cells; filtered API and workbook both returned 309 rows with matching first-page order | Confirms the production path reads the published snapshot only and that browser-equivalent filters/sorting produce the same complete Excel ranking. |
| 2026-07-28 | `$PYTHON tools/provider_canary.py` | Live SH/SZ/BJ representative quote/five-row daily-K contracts plus refreshed three-market stock-pool contract in isolated temporary state | exit `0`; all 3 markets succeeded without cache/fallback; stock pool 5533 rows (SH 2310, SZ 2892, BJ 331) in 9.44s | Confirms the canary now applies the ordinary 8-second budget only to representative market probes and the dedicated 60-second configured budget to the larger stock-pool refresh, all under the 20-second overall deadline. |
| 2026-07-27 | Live HTTP liveness/readiness/workbench checks, pre-15:15 retry request, post-15:15 full-market run `#9`, ranked-result readback, and post-run SQLite checks | Real service lifecycle, daily publication guard, current SH/SZ/BJ universe, deterministic ranking persistence, and database integrity | readiness `leader`; `600519.SH` workbench quality 88; pre-boundary retry HTTP 400; 5532/5532 processed, 5487 ranked, 45 skipped, 99.19% coverage, 517336 ms; ranks 1-5 contiguous; `quick_check=ok`, no foreign-key rows | Confirms the core runtime against current providers without accepting mismatched-date quotes; external AKShare/Eastmoney disconnects and two Tencent bulk omissions were isolated, with one fallback-derived result recorded as degraded rather than hidden. |
| 2026-07-24 | Protected online backup of the active 458 MiB SQLite database, followed by `SQLiteCache` initialization and `PRAGMA quick_check; PRAGMA foreign_key_check;` on the copy | Existing-database UTC-v2 upgrade path without modifying live data | backup manifest integrity `ok`; migration 7.79s; post-migration checks `ok` | Confirms the fixed-width timestamp and schema-migration rebuilds are practical and structurally valid on approximately 1.42 million daily-K rows. |
| 2026-07-27 | Runtime/dev/security `pip-audit --require-hashes --disable-pip`, clean wheel-only security-lock install, simulated CPython 3.12 `manylinux2014_x86_64` wheel resolution, `npm audit --audit-level=high`, two independent SBOM runs with recursive diff, and checksum-verified Gitleaks 8.30.1 scans | Isolated audit-tool installation, locked dependency vulnerabilities, reproducible CycloneDX output, and redacted current-source plus complete-history secret scanning | 47-package security environment installed with `pip check` clean; 50 Linux wheels resolved; no known Python vulnerabilities; 0 npm vulnerabilities; SBOM directories byte-identical; no Gitleaks findings | Confirms Security does not install optional provider SDKs and all local workflow gates pass against the final source and repository history. |
| 2026-07-27 | Identical clock/cache/migration/frontend-audit regression command under `TZ=UTC` and `TZ=Asia/Shanghai` | Host-timezone independence for business, persisted audit, import, diagnostics, and cache semantics | 91 passed and 4 subtests passed in both environments | Confirms the same focused boundary suite and result count under both supported host timezone settings. |
| 2026-07-24 | `$PYTHON tools/runtime_contract.py` | Installed runtime plus `.python-version`, `.node-version`, and `package.json` declaration consistency | passed on Python 3.12, Node 24.14.1, npm 11.11.0 | Confirms the current environment is inside the supported Python 3.12 / Node 22-or-24 / npm 10-or-11 contract. |
| 2026-07-24 | `git diff --check` plus targeted machine-path and common credential-pattern scans over the six changed engineering documents | Patch integrity and documentation privacy | passed; no findings | Confirms the changed Markdown contains no whitespace errors, machine-specific home paths, common API-token/private-key forms, UUID-shaped credential values, or user information. |
| 2026-07-22 | Node 22 and Node 24 runs of `$PYTHON -m pytest -q tests/test_frontend_notifications.py` | Deterministic notification polling, shared-lock coordination, cursor, retry, disable/re-enable, and delivery behavior across supported local/CI Node runtimes | 15 passed on Node 22.23.1; 15 passed on Node 24.14.1 | Confirms notification tests inject their browser lock dependency instead of accidentally borrowing a Node-version-specific global Web Locks implementation. |
| 2026-07-22 | `npm run test:e2e` | Full desktop and mobile browser regression, including full-market run/retry/recovery and multi-page notification coordination | 33 passed, 5 skipped in 47.4s | Confirms responsive workbench behavior, strict response contracts, request budgets, scan lifecycle ownership, online recovery, accessibility state, and cross-page alert de-duplication. |
| 2026-07-22 | `$PYTHON tools/api_inventory.py --check`, `$PYTHON tools/architecture_inventory.py --check`, then `$PYTHON -m pytest -q tests/test_tool_inventory_modules.py tests/test_config_modules.py` | Generated API/architecture references and documentation/configuration guardrails | passed; 55 tests passed in 11.48s | Confirms generated references match the final source tree and runtime settings remain documented without machine-specific paths or credentials. |
| 2026-07-27 | `$PYTHON -m ruff check --no-cache app tests tools`, `$PYTHON -m mypy`, both generated-inventory checks, and focused architecture/typing/supply-chain guards | Static analysis, expanded type scope, generated documentation, dependency direction, and supply-chain contracts | passed; mypy checked 74 source files; 29 focused tests passed | Confirms the final lint, type ratchet, generated references, acyclic boundaries, and workflow guardrails are clean. |
| 2026-07-19 | `npm run test:e2e` | Full desktop and mobile browser regression including scan failure recovery and online resynchronization | 28 passed, 4 skipped in 43.7s | Confirms strict response contracts, bounded one-timer polling, latest-run recovery, new-run pagination reset, responsive charts/layout, request budgets, and ARIA state. |
| 2026-07-19 | `$PYTHON -m pip check`, `$PYTHON -m ruff check app tests tools`, `$PYTHON -m mypy`, both generated-inventory checks, and `git diff --check` | Dependency, static-analysis, type, generated-document, and patch-integrity gates | passed; mypy checked 44 source files; 14 inventory guard tests passed | Matches the quality job gates and confirms current Node 24 GitHub Action pins, portable documentation, complete test indexing, and ignored runtime data. |
| 2026-07-18 | `npm run test:e2e` | Full desktop and mobile browser regression including the full-market background scan workspace | 24 passed, 4 skipped in 34.3s | Confirms immediate task progress, unpublished cancellation, derived-run retry, terminal degraded snapshots, bounded 100-row pagination, sorting/filters, keyboard scrolling, responsive layout, and existing workflows. |
| 2026-07-18 | `npm run test:e2e` | Desktop and mobile browser regression after alert/review/runtime changes | 20 passed, 4 skipped in 19.3s | Confirms request budgets, responsive layouts, chart inspection, local activity, timeline, queue, and stale-response behavior remain intact. |
| 2026-07-18 | `$PYTHON -m pytest -q <alert, review, scheduler, freshness, cleanup, notification, and inventory modules>` | Focused behavior and maintainability regressions | 180 passed in 13.22s; independent audit 132 passed | Confirms id-cursor retention, bounded notification backlog, partial degraded outcomes, plan deletion, metadata diagnostics, and generated-document guardrails. |
| 2026-07-17 | `$PYTHON -m pytest -q <provider runtime, quote, K-line, metadata, scheduler, sampling, lifecycle, and workbench modules>` | Provider admission, request-key single-flight, cancellation/orphan isolation, and downstream concurrency regressions | 363 passed in 3.67s | Confirms concurrent stocks no longer reject healthy foreground calls and true background calls remain bounded. |
| 2026-07-17 | `$PYTHON tools/api_inventory.py --check` and `$PYTHON tools/architecture_inventory.py --check` | Protected generated API/function references after regeneration | passed | Confirms both generated references match the final source tree. |
| 2026-07-17 | `npm run test:e2e` | Desktop and mobile browser regression | 20 passed, 4 skipped in 17.3s | Confirms core-first request order, request budgets, responsive layouts, chart inspection, local activity, timeline, queue, and stale-response behavior. |
| 2026-07-17 | `$PYTHON -m pytest -q -p no:cacheprovider <documentation and feature modules>` | Documentation index/config plus adjustment, review, scan, portability, backup, diagnostics, notifications, and workspace-preference regressions | 105 passed in 10.46s | Confirms the current documentation contracts and focused implemented-feature behavior. |
| 2026-07-16 | `$PYTHON -m pytest -q -p no:cacheprovider -k tool_inventory` | Documentation and generator guardrails | 13 passed | Historical guardrail result retained for traceability. |
| 2026-07-16 | `$PYTHON tools/api_inventory.py --check` and `$PYTHON tools/architecture_inventory.py --check` | Generated API/function references | passed | Both generated references match the current source tree. |
| 2026-07-16 | `npm run test:e2e` | Desktop and mobile browser regression | 20 passed, 4 skipped | Covers code/name search, exact chart inspection, local activity, request budgets, and existing desktop/mobile workflows. |

Final live-provider verification completed on 2026-07-22 against the current SH/SZ/BJ universe. The terminal derived run processed **5529/5529** symbols: **5491 success**, **0 missing**, and **38 skipped**, for **99.31% ranked coverage**. The skipped set was audited as 34 recent listings with fewer than the required 60 completed daily bars plus 4 possible suspensions: one stale daily sequence and three current BaoStock rows with zero volume plus unavailable/zero-volume Tencent quotes. No provider-chain outage was converted into bulk per-symbol missing data; three successful symbols retained explicit fallback or metadata degradation provenance. SQLite `quick_check` and `foreign_key_check` passed after the run.

## 7. Coverage Gaps

- Browser automation covers selected desktop/mobile workflows but has no full visual-regression baseline.
- There are no SSE load tests or provider timeout-cascade performance tests.
- SQLite compatibility helpers are covered, but there is no committed replay fixture for every historical database version.
- Route-level response contracts focus on high-risk paths rather than every stock report endpoint.
- Reliability math and persistence are covered with synthetic data, but no long-running representative dataset yet validates the provisional SLO targets or supports burn-rate alerting.
- Required CI remains hermetic; live provider canary behavior depends on external endpoints and is intentionally evidenced only by an explicit optional run.
- Supply-chain tests cover locks, audits, history scanning, immutable action references, and reproducible SBOMs, but there is no signed release artifact or generated/verified SLSA provenance.
